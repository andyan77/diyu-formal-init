# UI-03 正式 React 产品骨架与内容主链执行包

- 当前状态：`ACTIVE`
- 启动日期：2026-07-26
- 启动基线：`5285f7d13cc498eee8c0efff6b5425644119c84b`
- 唯一执行端：当前 WSL 主仓执行端
- 前置：UI-02 已按“静态产品合同与旅程原型成立”的有限范围 `CLOSED`

## 唯一目标

在不切换生产默认 UI 的前提下，用现有正式 React 应用和正式 API 交付公共首页、三入口隔离壳及
统一创作端内容主链。租户内容用户能够自然生成 V1、自然修改为 V2、完整回读 V1、返回当前版，
并从同一当前版复制或导出。总部、区域、代理商和门店共用一个创作壳，页面身份和能力只消费
服务端可信会话、发布账号、表达身份、组织资格与当前平台。

## 承重合同

1. 公共首页实现 A“种子找到声音”，总时长 6—8 秒，朱砂只承担一次方向转折，最终交接正式
   VI SVG 并静止约 0.8—1 秒；跳过、键盘和减少动效成立。
2. `/login`、`/tenant-admin/login`、`/ops/login` 分别进入自己的产品空间；错误入口显示人话
   恢复页，API 继续使用 JSON 401/403。
3. `/content` 只消费服务端解析的 `current_target`、发布账号、表达身份和组织资格；平台/账号
   载体切换通过显式导航重新解析，不在客户端猜账号。
4. 普通聊天不创建内容任务。创作请求、自然修改、历史版本、复制与导出复用现有正式任务和版本
   API，不创建平行业务。
5. 创作方向由服务端五轴目录提供；题材、风格、形式优先展示，讲法和连续方式渐进披露。五轴
   均可不选，可保留自然语言自定义，可仅在本人明确选择时保存默认。
6. 体型相关方向默认隐藏，只在本人主动启用后出现；实验、未支持、明确不做、来源缺口与内部
   稳定 ID 不进入普通面板。
7. 未明确选择的素材不进入任务；不选素材不阻断无素材依赖的创作。普通生成、修改和平台表现
   不自动改写个人默认、账号画像、品牌基线、目录或知识。
8. 透明品牌转译保留用户原选择，并在成品导读前用一句人话说明收敛方式。

## 能力和资产结论

- 精简目录继续为 25 项：默认可见 21 项，体型主动启用 4 项。
- UI-03 正式内容消费者覆盖其中 24 项；“连续系列”的完整系列对象与管理旅程仍由 UI-04 承接，
  UI-03 不把单次方向选择冒充已经建立系列。
- 当前内容消费者所需直接关联资产已经包含在现有运行激活集合中。
- 预期激活增量：`0`；41 条运行激活继续为 20 条知识、21 条方法。角色模板保持 0 条激活，
  H 类规则不塞入普通生成。
- 若实现现场证明需要新增激活，必须先完成治理真源、真实消费者、运行留痕、正向/相邻反证与
  可读投影同步；不得按前端菜单批量激活。

## 验证和停止线

开发期只跑定向测试。候选稳定后串行执行一次：

```text
git diff --check
make lint
make typecheck
make golden
make test
make frontend-lint
make frontend-typecheck
make frontend-build
make frontend-test
```

补齐 UI-G01、UI-G02、UI-G05、UI-G06 和必要 CAP-G01—CAP-G05 的自动化或直接证据；至少一条
正式生产模式会话＋正式 API＋PostgreSQL 的 V1→V2→V1 纵向链，以及一条极小真实 Chromium
旅程。产品/UI 与工程/安全各做一份有界审查。只有本地完整门与唯一远程 CI 绿色、两审无阻断、
Git 干净且未部署时，才把本执行包和根里程碑置为 `REVIEW`。

## 明确不做

不部署，不切换生产默认 UI，不接入完整 DM01、系列、租户管理、运维或素材管理，不新增认证、
权限模型、数据库迁移、微前端、状态机、设计系统网站、模型、重试层或审查平台；不补齐 39 项
缺口，不激活候选债，不重生成 M7-2B 六篇，不启动 UI-04。

## 当前唯一下一动作

完成正式 React 骨架与内容主链、定向证据、两份有界审查、一次完整门及唯一 CI；达标后进入
`REVIEW`，等待主控独立终审。
