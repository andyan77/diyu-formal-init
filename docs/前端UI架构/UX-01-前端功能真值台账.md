# UX-01 前端功能真值台账

> 初始核验基线：`354d1f6135815b7437fc7b802593c54649f8493b`；UX-02 正式工程复核
> 基线：`c17f1f6541da73ddfc032605db8cdbc085a214b0`；主控有界返工最终运行基线：
> `1abd0dbc9088ae8ac3127986cd7fcd0d347ef821`。UX-01 的目标原型不等于生产
> 实现；下表“当前状态”以 UX-03 Gate D 最终本地运行实现
> `06669c8cc46e412227ca188cc85745d4040c0c03` 上，按正式
> React、API、OpenAPI、本地 PostgreSQL、pytest、交互测试和隔离浏览器纵向证据逐项
> 重算。生产部署结论仍以 UX-02 最终运行锚点为准；Gate A 本地成立不冒充生产已经替换。

状态口径：`真实可用`、`有缺陷`、`纯占位`、`重复或应删除`、`尚无法证明`。

## UX-01 历史基线与 UX-02 工程复核

UX-01 关闭时的正式产品基线为：真实可用 `50`、有缺陷 `6`、纯占位 `2`、重复或应删除
`3`、尚无法证明 `3`。UX-02 已把其中 8 项直接缺口接入正式消费者；3 项没有真实消费者的
假能力从正式产品界面保持删除。UX-03 Gate A 首次冻结历史口径为 `45/13/0/6/0`；主控
有界返工后的最终 Gate A 真值为：真实可用 `44`、有缺陷 `14`、纯占位 `0`、重复或应删除
`6`、尚无法证明 `0`。Gate B 已关闭 FT-027—FT-032；Gate C 的 FT-039、FT-040、FT-041、
FT-045 已形成同一最终本地候选。FT-041 的商品—组织官方素材绑定、正式 resolver、
React/API/PostgreSQL P5 纵向、证据 UUID／digest、P2 自然商品取舍、P5 与 UUID／数据库
顺序无关的冻结主辅及 artifact 独立人审均已成立，当前恢复为 `真实可用`。Gate D 又以正式
商品版本／执行组织 resolver、13 条规则束、V1→V2 冻结重放、逐 SKU 守恒和有 freshness 的
公共状态投影关闭 FT-050—FT-052 与 FT-058。首次 Gate D 完成随后因 SKU 合同、finalize
异常边界、请求级 4xx 状态污染和 core／provider 组合真值四个接缝被主控退回；当前实现已以
结构化商品版本选择、事务 finalize＋租约回收和统一状态投影关闭并完成反证。当前真值为
`58/0/0/6/0`。该统计
不证明真实员工采用、真实发布或市场效果。Gate A、Gate B 已经主控
最终终审 `PASS`；Gate B 首次终审曾因 FT-031／FT-032 失败而诚实退回
`48/10/0/6/0`，相关历史保留。Gate C 首次候选及其 `54/4/0/6/0` 自报作为历史保留；
FT-041 首次候选和媒体资源责任返工失败均保留为历史；最近一次“七卡通过”又因 Runner
手工注入登记商品资源、没有正式 React/API/PostgreSQL 消费者而被主控退回。

| ID | 角色 | 页面/路由 | 用户工作 / 控件 | 当前消费者 | 当前状态 | 可重放证据与结论 | UX-01 目标 / UX-02 消费者 |
|---|---|---|---|---|---|---|---|
| FT-001 | 访客 | `/` | 跳过/重播首页动效 | `PublicHome` 本地状态 | 真实可用 | 约 7.2 秒，支持跳过、键盘和 reduced motion | 保留叙事，首屏只留一个实心动作；UX-02 复刻 |
| FT-002 | 访客 | `/` | 开始创作 | `/login` | 真实可用 | 正式路由存在 | 保持主动作；UX-02 |
| FT-003 | 访客 | `/` | 品牌管理/笛语运维入口 | 两个独立登录路由 | 真实可用 | `Root` 与服务端入口隔离 | 降为安静入口；UX-02 |
| FT-004 | 全角色 | 三类登录 | 用户名/密码登录 | 正式 HTML POST | 真实可用 | 租户用户、管理员、运维三入口；运维另有 TOTP | 保持三空间；UX-02 视觉产品化 |
| FT-005 | 新成员 | `/activate/:token` | 设置/重设密码 | 正式一次性 token | 真实可用 | 两次输入均至少 12 位；不一致时浏览器不 POST、服务端不消费 token，纠正后同一 token 可成功；旧 token 失效和重设撤销旧会话保持 | 激活成功后给清楚入口；UX-02 |
| FT-006 | 新成员 | 激活 | 在另一浏览器会话打开 | token 无会话依赖 | 真实可用 | `TestClient` 新会话可 GET/POST token | 原型展示跨会话完整流程；UX-02 |
| FT-007 | 全角色 | 错入口 | 自然恢复 | 服务端 HTML 403/303 | 真实可用 | 管理员拒绝 `/content`，租户用户拒绝 `/tenant-admin` | 给正确入口一个主动作；UX-02 |
| FT-008 | 全角色 | 会话过期 | 回到对应登录 | 正式认证中间件 | 真实可用 | 失效会话返回对应登录 | 保留未提交输入的恢复提示；输入恢复属 UX-02 |
| FT-009 | 管理员 | 概览 | 查看近 7 日真实摘要 | `team-usage` + `readiness` | 真实可用 | 两个真实聚合 API | 只呈现待办与近期变化；UX-02 |
| FT-010 | 管理员 | 概览 | “查看全部”待办 | 前端切换能力诊断 | 真实可用 | 可观察 section 变化 | 保留次动作；UX-02 |
| FT-011 | 管理员 | 成员 | 查看成员与状态 | operators API | 真实可用 | 正式列表 + 交互测试 | 任务导向列表；UX-02 |
| FT-012 | 管理员 | 成员 | 创建成员 | `POST /tenant-management/users` | 真实可用 | synthetic 交互提交正确角色/资格 | 分步但不建审批流；UX-02 |
| FT-013 | 管理员 | 创建成员 | 选择管理员/租户用户互斥入口 | 同一创建 API | 真实可用 | 管理员不带内容/陈列资格 | 保持职责互斥；UX-02 |
| FT-014 | 管理员 | 创建成员 | 分配内容/DM01 资格 | 创建/授权 API | 真实可用 | capabilities 实际写入 | 命名为工作资格；UX-02 |
| FT-015 | 管理员 | 创建成员 | 绑定逻辑发布账号 | 创建/授权 API | 真实可用 | publishing identity IDs 实际提交 | 与平台载体分开解释；UX-02 |
| FT-016 | 管理员 | 创建成员 | 获得完整 HTTPS 激活链接 | `activation_url` additive API 字段 | 真实可用 | URL 只由可信公开域名配置生成；恶意 Host 不能改变结果 | 正式 React 显示、打开和复制同一完整值 |
| FT-017 | 管理员 | 创建成员 | 点击激活链接 | 正式 `<a>` + 激活页 | 真实可用 | 隔离浏览器 A 创建、浏览器 B 点击并完成设置密码 | 点击、复制和显示值一致 |
| FT-018 | 管理员 | 成员详情 | 复制激活链接 | Clipboard API + `aria-live` | 真实可用 | Clipboard 成功显示“链接已复制”，拒绝时提示手动选择可见链接，不假报成功或发送 | 对完整 HTTPS 值复制；UX-02 |
| FT-019 | 管理员 | 成员详情 | 更新入口与账号资格 | `PATCH .../grants` | 真实可用 | 交互测试覆盖，要求重新登录 | 聚合保存并显示影响；UX-02 |
| FT-020 | 管理员 | 成员详情 | 生成重设密码链接 | `POST .../reset` | 真实可用 | 新 token 使旧 token 失效 | 保持一次性；UX-02 |
| FT-021 | 管理员 | 成员详情 | 复制/点击重设链接 | `reset_url` + 正式 `<a>`/Clipboard | 真实可用 | 新链接使旧链接失效，双重密码输入后撤销旧会话；完整 URL 可点击、可复制，成功／失败反馈真实且不声称已发送 | 保持一次性且不声称已发送 |
| FT-022 | 管理员 | 成员详情 | 停用成员 | 本地风险确认 + `POST .../disable` | 真实可用 | 首击和取消不调用 API；明确确认后最多调用一次并撤销会话与资格，不能停用自己 | 风险动作二次确认；UX-02 |
| FT-023 | 管理员 | 发布账号 | 创建逻辑发布账号 | 正式创建 API | 真实可用 | 包含 ContentRole 结构与五段画像 | 用户语言只说“发布账号/账号画像”；UX-02 |
| FT-024 | 管理员 | 发布账号 | 声明机构/个人 IP | speaker-kind API | 真实可用 | 结构化真源，不按名称猜 | 收进账号设置；UX-02 |
| FT-025 | 管理员 | 发布账号 | 添加多个平台目标 | platform-carriers API | 真实可用 | 同账号新增不同 target，画像不复制 | 一张账号卡内管理多个平台；UX-02 |
| FT-026 | 管理员 | 发布账号 | 查看/维护五段画像 | 版本化 profile API | 真实可用 | 权限、负责团队和版本均有真实消费者 | 五段人话编辑；UX-02 |
| FT-027 | 管理员 | 品牌资料库 | 新增文字资料 | brand-library preview/version/lifecycle API | 真实可用 | 预览不落库；确认形成 V1，V2 不改写 V1；停用、恢复、历史及任务冻结均由正式纵向证明 | 单一新增入口和当前版本详情已成立 |
| FT-028 | 管理员 | 品牌资料库 | 品牌全员/总部/指定区域 | 显式组织层级 + 控制组织作用域 | 真实可用 | 总部只绑定公司级组织；指定区域覆盖登记后代，不覆盖兄弟区域、其他品牌或租户；不按名称猜测 | 范围修改随新版本保存，运行只读账号控制组织 |
| FT-029 | 管理员 | 品牌资料库 | 维护商品事实 | preview + brand-products version API | 真实可用 | 手工、粘贴表格和 CSV 预览；明确确认、V1→V2、停用恢复、SKU 按需加载及旧任务冻结成立 | 空字段保持待补，不把整库送入无商品任务 |
| FT-030 | 管理员 | 品牌资料库 | 上传组织官方素材 | materials/version/lifecycle API | 真实可用 | 原件、人工说明、来源、范围和版本绑定；停用恢复成立，只有本次明确勾选才进入任务 | 私人素材仍归本人，兄弟区域和未选素材不进入上下文 |
| FT-031 | 管理员 | 团队使用 | 切换 7/30 日 | team-usage API | 真实可用 | 系列续写只统计成功提交完整 V1 的后续篇：`series_position > 1`、非平台派生任务、同 task 的 succeeded run 与 V1 同时成立，并按 V1 提交时间进入窗口；首篇、failed/running、无 V1、普通 V2、平台改编和窗口外续篇不计，同一 task 只计一次 | token 仅称已记录用量，不冒充账单 |
| FT-032 | 管理员 | 能力诊断 | 看依据/缺口/影响/去补 | readiness v3 API | 真实可用 | 六类工作由同一 tenant／brand 下内部一致的账号、组织、资料路径裁定；跨账号平台、跨区域商品和跨组织 DM01 条件不能拼接，evidence 返回真实对象、版本、范围、更新时间、缺口、影响和入口 | 诊断合同为 `ux03-readiness-v3`；局部缺口不升级为全局不可用 |
| FT-033 | 管理员 | 全局 | 进入内容创作 | 无入口且服务端拒绝 | 重复或应删除 | 管理空间不应出现创作动作 | 原型完全移除；UX-02 保持 |
| FT-034 | 租户用户 | `/user` | 选择内容/DM01 工作 | bootstrap capabilities | 真实可用 | 只展示获准能力 | UX-02 收敛成工作入口 |
| FT-035 | 租户用户 | `/content` | 选择逻辑发布账号 | bootstrap + query | 真实可用 | 多账号不默认第一项 | 保持显式选择；UX-02 |
| FT-036 | 租户用户 | `/content` | 选择平台与形式 | 账号获准 targets | 真实可用 | 平台/形式选择不改变账号 | 变为轻量上下文条；UX-02 |
| FT-037 | 租户用户 | `/content` | “发送”继续交流 | content stream + gate | 真实可用 | 普通交流可返回 conversation 且不成稿 | 作为单独次动作；UX-02 |
| FT-038 | 租户用户 | `/content` | “生成内容”显式成稿 | `interaction_mode=generate` | 真实可用 | 正式输入区并列“发送/生成内容”；浏览器反证发送后任务/版本仍为 0 | 已与普通交流分离 |
| FT-039 | 租户用户 | `/content` | 可选创作方向 | expression catalog + task snapshot | 真实可用 | 五轴均可不选、搜索、清除和开放自定义；选择、origin、清除项、目录版本、原话和透明转译冻结进任务，修订重放原快照；正式模型成品证明方向改变表达 | 目录不替代自然输入，不打开面板仍可生成 |
| FT-040 | 租户用户 | `/content` | 真实生成阶段 | ContentService NDJSON stream | 真实可用 | 六阶段只在实际节点发出；`completed` 位于原子提交、digest 校验和正式回读之后；commit/readback 失败无 completed、版本 +0、永久 running 0 | 不显示伪百分比、token 或未验证草稿 |
| FT-041 | 租户用户 | `/content` | 完整成品 V1 | creative-kernel-v4 + MediaCapabilityEnvelopeV2 + product-media resolver + product-value-contract-v1 + delivery-compiler-v4 | 真实可用 | 商品媒体绑定、headquarters 精确作用域、正式 P5 `+1/+1/+1`、版本冻结及证据 UUID／digest 均成立；P2 以自然正文表达商品专属理解、用户取舍和成立条件，不暴露验收标签；P5 主辅由本次素材顺序冻结，不依赖 UUID／返回顺序，并由首图、图序和制作提示共同执行；人审必须引用最终 artifact。同 SHA 七卡、`495 passed, 2 skipped` 与显式 Chrome 通过 | 保持资源闭世界和抽象编排／可选补拍降级；当前等待 Gate C 主控独立终审 |
| FT-042 | 租户用户 | `/content` | 自然修改 V2 | revision API | 真实可用 | 新版本且旧版保留 | 主动作改为“修改成 Vn+1”；UX-02 |
| FT-043 | 租户用户 | `/content` | V1→V2→V1/当前版 | versions API | 真实可用 | 交互测试覆盖 | 版本位置更清楚；UX-02 |
| FT-044 | 租户用户 | `/content` | 复制/导出当前查看版 | Clipboard/Blob | 真实可用 | 两者消费同一投影 | 保持次动作；UX-02 |
| FT-045 | 租户用户 | `/content` | 系列 | series API + frozen writer context | 真实可用 | 第一篇无前情，第二篇读取第一篇，第三篇按 position 读取前两篇；task/version/outline/body 进入 Writer 与快照，跨租户／品牌／账号／系列诱饵失败关闭，最终三篇人工可读出承接 | 不把系列管理冒充系列内容消费，不以普通 V2 或派生版增加篇次 |
| FT-046 | 租户用户 | `/content` | 我的素材 | materials API | 真实可用 | 上传/编辑/删除/本次选择 | 退入侧栏抽屉；UX-02 |
| FT-047 | 租户用户 | `/content` | 429/失败后保留输入 | 正式失败卡片 + 幂等重试 | 真实可用 | 429、网络和生成失败均保留输入与既有成品；重试复用 request ID，不重复成版 | 人话解释并可原地继续 |
| FT-048 | 租户用户 | 账号/平台切换 | 保护未提交输入 | sessionStorage + 目标冲突选择 | 真实可用 | 直接切换与目标冲突路径均保护草稿；用户决定保持或切换目标 | 切换不改变登录身份或账号画像 |
| FT-049 | 租户用户 | 加载/无数据 | 空状态 | 多处前端分支 | 真实可用 | 历史、素材、方案均有空态 | 每个空态一个主动作；UX-02 |
| FT-050 | DM01 用户 | `/display` | 生成纯文字参考方案 | structured product version selection + formal resolver + `DM01RuleBundleV1` | 真实可用 | React 提交稳定 product version ID＋正整数数量，服务端重新核验当前版本与作用域并保留 SKU 原值；`abc-123`、`ABC123`、`123456`、`款号一`、`GD-UP-01` 均纵向通过。V1 实际消费 11 条 generation 规则，任务冻结 13 条完整生命周期规则；预检失败 0/0/0，全程纯文字、模型调用 0 | legacy 文本兼容但不授权；不暗示绘图，不加载商品整库 |
| FT-051 | DM01 用户 | `/display` | 局部修改/历史/复制 | frozen context + transactional finalize + revision/versions/clipboard | 真实可用 | 正式 React/API/PostgreSQL 与 Chrome 完成 V1→V2→历史 V1→当前 V2；V2 重放冻结商品、门店、库存和规则束，不读取当前版本／activation，复制与当前查看版本一致。V1/V2 finalize 失败无新版本且 run failed；连接中断遗留只由 900 秒同作用域租约回收 | 模糊反馈只问一次且不建 run/version；失败保留输入与已有成品 |
| FT-052 | DM01 用户 | `/display` | 库存守恒可见 | `dm01-plan-v2` + deterministic body compiler | 真实可用 | plan、正文和逐 SKU 汇总由同一校验结果生成；每项显示“输入 = 上墙 + 不上墙”，禁止负数、超量、遗漏、输入外 SKU、超容量和错误挂杆，缺陈列属性商品只对账不上墙 | 每版 artifact 摘要绑定并在读取时校验 |
| FT-053 | DM01 用户 | `/display` | 陈列示意图 | 无实现、无绘图能力 | 重复或应删除 | DM01 仅纯文字 | 原型不出现图像；UX-02 不建设 |
| FT-054 | 运维 | `/ops` | 开通租户及首位管理员 | ops tenants API | 真实可用 | 保留相对链接兼容字段，并返回可信公开域名生成的完整 `activation_url` | 保留运维工作，不并入租户管理 |
| FT-055 | 运维 | `/ops` | 完整 HTTPS 激活链接 | `activation_url` additive API 字段 | 真实可用 | 运维开通首位管理员与租户管理使用同一可信公开域名边界 | 正式页面可点击、可复制 |
| FT-056 | 运维 | `/ops` | 停用/恢复租户 | ops API | 真实可用 | 有状态和可观察结果 | 保持；UX-02 |
| FT-057 | 运维 | `/ops` | 处理能力反馈 | ops unmet request API | 真实可用 | 分类、回复、状态更新 | 保持独立运维；UX-02 |
| FT-058 | 公开 | `/status` | 查看服务状态 | shared public projection + normalized provider observation | 真实可用 | HTML fallback、React 与 API 共用投影且 no-store；core 不可用时三类能力均不可用，core 可用时 DM01 可用，内容生成仅按 900 秒内 observation 映射。请求级 4xx 不污染全局状态；429 degraded，5xx／传输／认证／权限 unavailable；空、未来、过期为 unknown | 页面与重新检查不调用模型；不暴露供应商、模型、密钥、原始错误或内部依赖 |
| FT-059 | 管理员 | 概览 | “已有资料”等无解释数字 | 现有摘要统计 | 重复或应删除 | 数字不能直接支持下一工作 | 改成有原因的待办/变化；UX-02 |
| FT-060 | 全角色 | 错误状态 | 网络错误后的自然恢复 | 各空间正式错误与重试状态 | 真实可用 | 正式交互覆盖会话失效、网络/429/生成失败、输入保留和原地重试 | 不伪造成功，不清空已有工作 |
| FT-061 | 管理员 | 激活交付 | 受保护渠道交付状态 | 无发送/交付集成 | 重复或应删除 | 正式页面只提供打开和复制，不出现“已发送”或无消费者动作 | 当前明确不建设邮件/短信发送 |
| FT-062 | 租户用户 | 生成 | 逐 token 草稿 | 无正式消费者 | 重复或应删除 | 正式链只显示真实生命周期，服务端校验完成后原子呈现成品 | 当前明确不建设未经校验草稿 |
| FT-063 | 全角色 | 账户恢复 | 忘记密码自助入口 | 管理员生成 reset | 重复或应删除 | 登录页没有假按钮，只提示联系品牌管理员获取一次性重设链接 | 当前明确不建设自助找回服务 |
| FT-064 | 租户用户 | `/content` | 移动双工作面 | 前端 tab | 真实可用 | 对话/成品 tab 已存在 | 重做移动布局，不压缩三栏；UX-02 |

## 统计与直接裁决

- 真实可用：`58`
- 有缺陷：`0`
- 纯占位：`0`
- 重复或应删除：`6`
- 尚无法证明：`0`
- 合计：`64`

Gate D 的 `58/0/0/6/0` 仅表示 64 项正式功能在本地候选均已有明确真实消费者或明确删除边界；
承重实现为 `06669c8cc46e412227ca188cc85745d4040c0c03`，Golden/OpenAPI
`517 passed, 2 skipped`，显式三视口 Chrome、四项直接 mutation 与两份有界审查通过。该结果尚未 push、CI、部署
或连接生产，不证明生产新能力、真实采用、真实发布、市场效果、企业 SLA 或 `20/55/44`
全组合稳定支持。

UX-02 已关闭 FT-016/017/021/038/047/048/055，并以统一错误恢复正式关闭 FT-060。管理员
可以从正式 React/API 获得完整 HTTPS 一次性链接并在隔离新会话完成激活；租户用户可以明确
区分交流与生成、保护草稿、幂等重试并完成 V1→V2→V1。FT-033/053/059/061/062/063
属于明确删除或不建设的假能力：管理员创作入口、DM01 图像、无解释数字、虚假发送状态、
逐 token 草稿和无消费者的自助找回均不得重新出现在正式界面。上述 `58/0/0/6/0`
已由最终运行 SHA `1abd0dbc9088ae8ac3127986cd7fcd0d347ef821` 的生产正式消费者、
三项 mutation、定向 synthetic 旅程和清理后复核承重；该轮结束时 UX-02 重新进入
`REVIEW`，不等于真实员工或品牌已经采用。

最后一轮界面层返工没有改变 64 项分类，只关闭了既有正式消费者的两个验证假绿。最终运行
SHA `3dfe648571b679f74e7dbb4d7c9927de77f03000` 将激活／重设复制反馈放入当前成员抽屉，
并在取消或 Escape 后把焦点精确返回重新挂载的真实停用按钮。反馈移回抽屉外、恢复旧
`event.currentTarget` 聚焦两项 mutation 均使新 oracle 真实变红；1440×900、390×844
Chrome、CI `30537611606` 和生产定向旅程全绿。因此当前真值仍为
`58/0/0/6/0`，不是通过修改台账掩盖缺陷。主控最终独立终审结论为 `PASS`，UX-02 现为
`CLOSED`；该关闭不等于真实员工或品牌已经采用。

UX-03 Gate A 另以全新租户、正式 React/API/PostgreSQL 和三个视口浏览器重新核对身份入驻
主链，并修复原先会 422 的账号／平台请求、品牌基线死路、画像冷启动、权限耦合、生命周期和
身份投影。Gate A 没有机械保留 UX-02 的总体数字：FT-027—032 中尚未闭环的项目、
FT-039—041、045、050—052、058 共 14 项按后续 Gate 未完成的产品目标降为“有缺陷”，
形成 `44/14/0/6/0`。其中 FT-032 虽已有对象存在性、依据、缺口、影响和入口，仍未关闭
来源可信度、时效、冲突及与当前工作的完整关联。这既不否认已有正式消费者，也不把局部
API 存在冒充全链路已完成。

UX-03 Gate B 首次以冻结实现 `a67f44bdc4e73a2a8386cdfa311659220aefc0af` 宣告关闭
FT-027—FT-032：品牌文字资料、商品事实和组织官方素材具备正式预览确认、不可变版本、停用
恢复、历史读取与运行消费；三级组织范围从逻辑发布账号控制组织裁定；7/30 日使用口径和六类
能力诊断完成。新任务只读取当前启用、范围匹配且本次相关或明确选择的资料，任务修订重放原
快照。三个视口正式 Chrome、Golden/OpenAPI `422 passed, 1 skipped` 及两审通过。
剩余有缺陷项精确为 FT-039—FT-041、FT-045、FT-050—FT-052、FT-058。

主控随后确认 FT-027—FT-030 保持成立，但判定 FT-031 把系列首篇误算为续写、FT-032 会
把不同账号／组织的独立数量拼成 `available`，首次 Gate B 终审因此 `FAIL`，真值暂退为
`48/10/0/6/0`。有界返工实现 `4a49e0d912e17a10965e840e89c1ef7f03bca3f6` 改用真实续篇
位置和共享完整可执行路径，并统一指定区域只能保存 region。定向纵向、三项 mutation、
Golden/OpenAPI `422 passed, 1 skipped`、前端四门和显式三视口 Chrome 全绿后，FT-031、
FT-032 才重新标为真实可用，当前真值恢复 `50/8/0/6/0`。

UX-03 Gate C 首次候选 `4d797d35614216c92a7e62ae7a62d7456165d70b` 当时自报关闭
FT-039—FT-041 和 FT-045：方向从 UI 到任务快照及修订重放一致；六个生成阶段对应真实
服务端节点，成品只在提交、digest 校验和正式回读后原子出现；P1—P5 媒体原生成品与三篇
系列经最终同 SHA 模型套件和人工全文审阅通过。方向映射、系列 Writer 输入、commit 后回读、
v3 compiler 路由和商品媒体资源归属的 mutation 均真实变红，恢复后转绿。该阶段真值为
`54/4/0/6/0`，剩余有缺陷项精确为 FT-050—FT-052、FT-058；该历史结论已被主控 FT-041
`FAIL` 取代。当前 `1f0dffc…` 虽关闭精确包装协议缺口，但新的同 SHA 全文审阅仍发现
未登记媒体资源和 P2 未冻结媒体语义，故该阶段真值曾保持 `53/5/0/6/0`；这段失败历史
继续保留。

主控随后冻结确定镜头、可选补拍建议和抽象编排三层责任。实现
`fc45c11a5beb021d8c4ff69aee61a19f035eba77` 以
`MediaCapabilityEnvelopeV1` 和封闭 `MediaProgram` 把媒体资源与程序在 Writer 前冻结：
确定指令只使用正式登记、启用、作用域合法、本次选择且版本冻结的资源；可选补拍为
Compiler 所有的条件建议，不进入 required refs；没有资源时的抽象成品不依赖建议也能直接
使用。最终实现 `0c4f0d48f2d89daf0082c063c22c4f14aa6ea7e5` 再将 P2／P5 商品 Writer
输入收敛为去对象化判断顺序，阻止从原题材和账号领地扩写商品属性、用途和体验。

最终同 SHA P1—P5、series2、series3 各调用一次并形成七种 MediaProgram，七卡人工全文
均通过；P2 三条冻结事实逐字且无新增商品语义，P3 咖啡原句与账号观察路径同时成立，P5
只消费两个本次冻结的登记商品资源。Golden/OpenAPI 为 `475 passed, 2 skipped`，file SHA、
raw SHA、正式 `visible_digest` 与人工审阅引用可复算一致。FT-041 因此恢复为
`真实可用`，该阶段真值为 `54/4/0/6/0`；不证明真实采用、真实发布或市场效果。

上述候选随后又经历正式消费者、headquarters 作用域、P2／P5 主价值与证据独立性审计退回；
失败历史不改写。当前运行实现 `ec3dbe45d485816c7a49f9c801cd2baa2fe58c04` 最终使 P2 的
双面选择、商品／用户取舍和成立条件以自然正文呈现，使 P5 主辅角色由本次选择顺序冻结并由
首图／图序／制作提示共同承载；UUID 重建、数据库插入顺序交换、等尺度媒体和 snapshot-only
人审均有直接反证。同 SHA 七卡、可复算 artifact 人审、`495 passed, 2 skipped` 及显式
三视口 Chrome 通过后，FT-041 当前恢复为 `真实可用`，最新真值为 `54/4/0/6/0`。
