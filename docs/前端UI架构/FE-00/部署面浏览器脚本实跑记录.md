# 部署面浏览器脚本实跑记录 / Deployment-surface browser scripts

监理审查发现 5 指出：8 个部署面浏览器脚本「EXE-01 一个都没跑、零运行产物」。
本轮**逐个实跑**，全部对准 **loopback + 一次性可销毁本地库**，**未触碰生产 /
Qdrant / ECS / Dify / 任何外部服务**。本文记录每个脚本的真实结果——包括跑不通的。

**先说结论**：跑起来之后发现的第一件事，不是脚本的问题，是**EXE-01 把服务端渲染的
页面换成了 React SPA，8 个脚本全部还在按旧 DOM 的时序等待**。这是一处共因缺陷，
已修（见第二节）。修完 1 个脚本转绿，4 个继续在更深的位置失败，3 个缺私有真源无法
启动。**没有任何一个脚本因为本包（EXE-01R）的改动而失败——但这句话目前只有间接证据，
见第四节的诚实标注。**

---

## 一、八字段表

「一次性 DB」= pytest fixture 每次新建库、跑完即销毁；「loopback」= `127.0.0.1`
临时端口，`DIYU_RUNTIME_MODE=production` 但数据、密钥、外部服务全是本地占位值。

| # | 脚本 / script | 本地 base URL | DB 隔离 | 结果 | 断言/进度 | 缺失环境 | 为何无法仓内 fixture 构造 | 是否覆盖本包变更面 |
|---|---|---|---|---|---|---|---|---|
| 1 | `ux03-gate-b-browser.mjs` | `http://127.0.0.1:<临时端口>` | 一次性 DB（`test_ux03_gate_b.py`） | **PASS** | 全程通过，`failures: []` | 无 | — | 否（租户管理面） |
| 2 | `ux03-gate-d-browser.mjs` | 同上 | 一次性 DB（`test_ux03_gate_d.py`） | **FAIL** | 登录已过；停在 `无法点击 陈列搭配：{"ok":false,"items":[]}` | 无 | — | 否（陈列 DM01 面） |
| 3 | `ux03-gate-c-browser.mjs` | 同上 | 一次性 DB（`test_ux03_gate_c.py`） | **FAIL** | 登录与五轴已过；停在 `等待超时：原子完整成品` | 无 | — | **是**（创作面） |
| 4 | `ux03-product-media-browser.mjs` | 同上 | 一次性 DB（`test_ux03_gate_c.py`） | **FAIL** | 停在 `等待超时：P5 原子成品` | 无 | — | **是**（创作面素材） |
| 5 | `ux03-gate-a-browser.mjs` | 同上 | 一次性 DB（`test_ux03_gate_a.py`） | **BLOCKED** | 未启动 Chrome | TENANT-01 私有 source root | `test_ux03_gate_a.py:282` 明写 `explicit TENANT-01 browser run requires its private source root`；该语料是租户私有真源，不在仓内，也不应进仓 | 否 |
| 6 | `tenant01-formal-browser.mjs` | 由 `src/tool/run_tenant01_formal_browser.py` 拉起 | 该工具自建 | **BLOCKED** | 未启动 | `TENANT01_BROWSER_CREDENTIALS`、`TENANT01_CANDIDATE_SHA`、`TENANT01_CONTEXT_EVIDENCE` | 三者都是 TENANT-01 正式纵向的产物（凭据、候选冻结 sha、上下文证据），由 `setup_tenant01_local_formal.py --source-root` 从私有语料生成 | 否 |
| 7 | `ux02-formal-browser.mjs` | — | — | **BLOCKED** | 未启动 | `UX02_BASE_URL`、`UX02_ADMIN_USERNAME/PASSWORD`、`UX02_NEW_USER_PASSWORD`、`UX02_EXPECTED_ACCOUNT_NAME`、`UX02_EXPECTED_PLATFORM` | **仓内没有任何 driver**：`grep -rn ux02-formal-browser --include=*.py` 零命中。UX03 五个脚本都有 pytest fixture 负责建库、开户、激活凭据、起 uvicorn；UX02 两个脚本从未有过。补一个等于新写一整套 UX02 正式纵向 fixture，不在 R1—R5 任何一项内 | 否 |
| 8 | `ux02-responsive-browser.mjs` | — | — | **BLOCKED** | 未启动 | 同上，另加 `UX02_EXISTING_ACCOUNT_ID`、`UX02_EXISTING_TARGET`、`UX02_EXISTING_USER` | 同上 | 否 |

复跑命令（`scripts/exe01/run_gates.sh` 的同款环境，外加逐个开关）：

```bash
DIYU_RUN_UX03_BROWSER=1              pytest tests/test_ux03_gate_a.py::test_formal_react_new_tenant_gate_a_journey
DIYU_RUN_UX03_GATE_B_BROWSER=1       pytest tests/test_ux03_gate_b.py::test_gate_b_brand_scope_usage_and_readiness_journey
DIYU_RUN_UX03_GATE_C_BROWSER=1       pytest tests/test_ux03_gate_c.py::test_formal_creator_gate_c_browser_journey
DIYU_RUN_UX03_PRODUCT_MEDIA_BROWSER=1 pytest tests/test_ux03_gate_c.py::test_formal_product_media_binding_creates_and_freezes_p5
DIYU_RUN_UX03_GATE_D_BROWSER=1       pytest tests/test_ux03_gate_d.py::test_formal_dm01_v1_v2_uses_product_versions_and_frozen_rules
```

---

## 二、跑起来之后发现的共因缺陷（已修）

**现象**：五个能启动的脚本，全部在第一步登录就死，报 `找不到输入控件
input[name="username"]`。

**测量**（不是推断）——用 CDP 直接读同一个页面的 DOM，同一次加载的两个时刻：

| 时刻 | `#root` 子节点 | `input[name=username]` | 页面文字 |
|---|---|---|---|
| `readyState === 'complete'` | 1 | **false** | `正在进入你的工作空间……` |
| 再等 1500ms | 1 | **true** | `品牌管理 / 登录笛语 / 用户名 / 密码 / 登录` |

**原因**：EXE-01 把 `/tenant-admin/login` 等页面从服务端表单改成了 React SPA
（`render_spa_shell`），旧表单退到 `<noscript>` 里——开了 JS 的 Chrome 根本看不见它。
真正的表单由 `AppRouter` 在异步引导完成后才渲染，而在那之前它先渲染
`LoadingPage`（`.page-loading`）。8 个脚本的 `navigate()` 只等
`document.readyState === 'complete'`，那一刻页面上只有加载占位符。

**修法**（一处共因，一处修）：等待条件补上「SPA 引导已结束」——

```js
`document.readyState === 'complete' && !document.querySelector('.page-loading')`
```

对非 SPA 页面向后兼容：`.page-loading` 不存在时条件退化成原来那条。

**效果**：`ux03-gate-b` 由 FAIL 转 **PASS**；其余四个越过登录、失败点前移到各自更深
的位置。`assert_test_reconciliation` 仍绿（断言数 549 → 646，只增不减）。

---

## 三、剩余四处失败：不是同一个原因

| 脚本 | 失败点 | 性质 |
|---|---|---|
| gate-d | 点不到「陈列搭配」导航项（`items: []`） | 脚本按旧 DOM 找导航项，SPA 的导航结构已不同 |
| gate-c | `等待超时：原子完整成品` | 创作流跑到成品阶段没等到 |
| product-media | `等待超时：P5 原子成品` | 同上 |
| gate-a | 私有语料缺失，Chrome 未启动 | 数据阻塞，非代码缺陷 |

前三处都指向同一类问题：**脚本编码的是 EXE-01 之前的 DOM 契约**。把它们改对，等于
按新 SPA 重写三条部署面走查，需要先确定新 DOM 契约是什么——这是设计/监理裁决，不是
R1—R5 任何一项，**故本轮不做**。

---

## 四、诚实标注：gate-c / product-media 的归属**尚未查清**

这两个脚本**覆盖本包的变更面**（创作面），所以「是不是 EXE-01R 弄坏的」必须问。
本轮**没有查清**，理由如下，不做任何倾向性结论：

- 试过在 `357d17c`（EXE-01R 基线）建 worktree 跑 gate-c 做对照，两次都撞上
  `Chrome 提前退出 null`——这是本机同时跑多个 Chrome 的环境干扰，**不构成可用的对照
  结果**。不拿它当证据。
- 已知的间接证据，只说到这里为止：这两个脚本在 EXE-01 期间**一次都没跑过**（发现 5
  原文），所以它们与真实 UI 的偏离**至少**积累自 EXE-01 的 SPA 改造；本包新增的 35
  条创作面断言、UI-04 全程走查、以及 920 条 golden 全绿。**这些都不等于「本包没弄坏
  它」**——覆盖面不同。

**建议的查清方式**（一条命令，需一台不与其他 Chrome 争用的机器）：

```bash
git worktree add /tmp/base357 357d17c   # 只把本文第二节的 navigate 修法一并带上
DIYU_RUN_UX03_GATE_C_BROWSER=1 pytest tests/test_ux03_gate_c.py::test_formal_creator_gate_c_browser_journey
```

基线也失败 → 归 EXE-01；基线通过 → 归 EXE-01R，必须回退或修复。

---

## 五、安全边界

- 全部实例：`127.0.0.1` 临时端口，`--host 127.0.0.1`，无对外监听。
- 全部数据库：项目自带 PostgreSQL（`var/postgres`，`listen_addresses = ''`，
  **没有 TCP 端口**），pytest fixture 每次新建、跑完销毁；另建的探针库
  `diyu_exe01r_probe` 已 `DROP DATABASE`。
- 全部外部依赖置为占位值：`DEEPSEEK_API_BASE_URL=https://example.invalid`、
  S3 指向 `127.0.0.1:9000`、生成器 `stub`。**全程未读 `.env`**，未连生产 / Qdrant /
  ECS / Dify。
