# 笛语前端 UI 架构资料

本目录是前端设计与交互文档的唯一主仓真源。UX-01 第一次执行侧自报的 `3/3 Gate /
REVIEW` 经主控独立终审判定 `FAIL_WITH_BOUNDED_REWORK`，页面可打开式 `15/15` 验证不能
证明交互成立；该历史保留。UX-01 已在不修改正式 React、API、数据库或生产的边界内完成
有界返工；主控重新独立终审 `PASS`，UX-01 现为 `CLOSED`：64 项
[前端功能真值台账](UX-01-前端功能真值台账.md)、四类角色与信息架构冻结，以及覆盖
公共与认证、租户管理员、租户用户创作和纯文字 DM01 的
[连续高保真原型](产品化HTML原型_UX-01/review/index.html)。该原型是 UX-02 的产品输入，
现有 37 个状态，并由 30 项真实操作检查验证。UX-02 现为 `REVIEW`：两条正式
React/API/PostgreSQL 承重旅程、纯文字 DM01、64 项真值复核、响应式／可访问性、生产默认
前端替换、清理和不降级回退均已成立；主控有界返工又以最终运行
`1abd0dbc9088ae8ac3127986cd7fcd0d347ef821` 关闭双重密码输入、停用二次确认和 Clipboard
成功／失败逻辑；最后一轮运行 `3dfe648571b679f74e7dbb4d7c9927de77f03000` 又关闭复制
反馈位于抽屉遮罩后、取消停用聚焦已卸载按钮两个界面假绿。承重 CI `30537611606`、桌面／
移动真实 Chrome 和生产定向验证均通过。等待主控最终独立终审，不启动 UX-03。

UI-04 已于 2026-07-27（UTC）通过主控独立终审
并保持 `CLOSED`；创始人真实使用后出现的产品语义新证据由 successor UI-05 单独承接。
UI-05 Gate A 返工候选已通过产品裁决，Gate B 正式 React/API 与 Gate C 生产替换已经完成。
2026-07-27（UTC）的主控关闭结论保留为历史；关闭后新发现的三个生产 UI 缺口已经以前向
修复完成唯一 CI、同 SHA 部署与定向生产复验。随后 UI-05—UI-11 的创作真实性探索及失败
历史完整保留并由 UI-12 承接。UI-12 已把新任务主链收敛为服务端 Writer 前预分配
`trusted_fact + creative_expression`，Compiler 对事实和创作范围作确定性投影，概率
Reviewer 退出保存授权。两次主控终审发现的版本载荷 UPDATE/DELETE、Writer 正式 heading
注入、legacy ASCII 冒号漂移与默认不可见字符残余，最终由 schema 32 的应用权限＋
UPDATE/DELETE trigger、统一摘要、legacy 原投影和共享 Unicode 防伪关闭。最终
`9c5b243…` 的 Golden/OpenAPI `417 passed`、前端四门、唯一 CI `30500580180`、同 SHA
生产兼容冒烟、备份恢复及旧镜像不降级回退均通过；主控第三次、也是最终一次独立终审
`PASS`，UI-12 现为 `CLOSED`。

- [UI-03 正式 React 产品骨架与内容主链执行包](../UI-03-正式React产品骨架与内容主链执行包.md)：
  已在不切换生产默认 UI 的边界内完成公共首页、三入口隔离壳与统一创作端，现为 `CLOSED`；
- [UI-04 全业务接入与生产替换执行包](../UI-04-全业务接入与生产替换执行包.md)：
  已把纯文字 DM01、系列、素材、租户管理、运维和状态页接入正式 React 与 API，并完成生产
  默认 UI 切换、冒烟和回退核验；主控独立终审 `PASS`；
- [UI-05 租户管理与创作体验语义重构执行包](../UI-05-租户管理与创作体验语义重构执行包.md)：
  当前为 `SUPERSEDED → UI-06`。Gate A 首次候选未通过主控产品审查并保留为历史记录；返工候选
  `438e904…` 通过后，正式系统已实现管理员／租户用户入口互斥、逻辑发布账号与同账号多平台、
  自然交流，以及信息不足时每次只问一个最有价值的问题并避免重复追问；五轴开放控制、真实
  生成进度、失败恢复、品牌资料三级范围、团队使用和能力诊断成立。关闭后的极小返工又关闭
  首页临时图形与正式 Logo 叠显、品牌管理员缺少账户安全入口、运维 TOTP 文案含混三项缺口。
  本轮候选 A `8d591fac…` 删除级联层，候选 B `f0261e74…` 只补共享事实来源／拍摄资源
  约束；最终 CI `30322971553` success 和同 SHA 部署成立，但真实 G4 一次修复后仍为
  `invented_actuality`，人工 G3 又发现“婆婆带孩子”等未提供情境。没有候选 C；生产未
  downgrade 数据库并回到最后一份有文档证明健康的 `845f632…`，readiness 正常。资产激活
  增量为 0；Gate A、Gate B、Gate C 不重复执行；
- [UI-06 自然创作与叙事真实性闭环执行包](../UI-06-自然创作与叙事真实性闭环执行包.md)：
  当前为 `SUPERSEDED → UI-07`。恢复基线 `dc86b95…` 与 CI `30325920857` 成立；唯一候选
  `e77099f4…` 的本地门、五项 mutation 和 CI `30328125186` 绿色，但真实 G1 将普通交流
  冻结为 `general_observation` 并误建任务，Writer 随后失败。G1 未重跑，G2—G7/H1/D1
  未执行；临时数据已精确清理，生产与远端运行树回 `845f632…` 安全内容。原 `UI06-D1`
  已被 UI-07 前置提交门裁决取代，历史原文保留；
- [UI-07 自然创作提交门与叙事真实性闭环执行包](../UI-07-自然创作提交门与叙事真实性闭环执行包.md)：
  当前为 `SUPERSEDED → UI-08`，此前 `BLOCKED` 事实保留。`creation-intent-gate-v1`
  在任何业务写入和 Writer/Reviewer 前只接受
  明确文本、主动“直接生成”或可信任务内修改；普通交流无虚假进度，模型 proposal 不授权。
  门后承接 Frame、四模式 Block、独立 Reviewer、确定性对账和一次块修复，不新增迁移或
  整体更换 DeepSeek。无持久化预检的 G1—G6/H1/D1 intake 结果成立，但 G3 Writer 仍补造
  未提供微事件和品牌信念，唯一块修复后仍失败；未 push/CI/部署；
- [UI-08 创作计划、事实依据与叙事观察分离执行包](../UI-08-创作计划事实依据与叙事观察分离闭环执行包.md)：
  当前为 `SUPERSEDED → UI-09`。`CreativePlanV2`、fact/constraint 分离、服务端 block/scene 骨架及
  abstract/situated/institutional 三类观察已实现，golden `173 passed`。当前
  `deepseek-v4-flash` 与唯一候选 `deepseek-v4-pro` 在同输入、同 Prompt、同参数的
  G3/G4/D1 单次 Writer-only 预检中均失败；未运行 Reviewer 资格、CI 或部署，失败历史
  不改写为成功；
- [UI-12 来源、语态、主体绑定与服务端证据裁决执行包](../UI-12-来源语态主体绑定与服务端证据裁决闭环执行包.md)：
  当前为 `CLOSED`。新任务由服务端预分配可信事实与四类创作表达单元，Writer 只能填写
  既定创意 unit，Compiler 负责逐字事实、自然范围和全部平台出口；Reviewer 不再参与版本
  放行。旧候选及两次主控退回证据保留；最终 schema 32 实现已经关闭版本 UPDATE/DELETE、
  恢复 legacy 原投影并关闭 Writer heading／Unicode 伪装，完成最终 CI、生产兼容与回退；
  主控最终独立终审为 `PASS`；
- [UX-01 前端产品定义、功能真值与逐屏冻结执行包](../UX-01-前端产品定义功能真值与逐屏冻结执行包.md)：
  当前为 `CLOSED`，主控重新独立终审 `PASS`。第一次页面存在性验证假绿及独立终审退回
  历史保留；有界返工后已把正式
  现状与目标体验分开记录，冻结管理员／租户用户／访客／运维职责、登录身份／逻辑发布账号／
  平台载体关系和低种子创作工作台，并交付 37 状态、30 项真实操作检查承重的连续评审原型；
  未修改正式 React、API、数据库或生产，不启动 UX-02；
- [产品化HTML原型_UX-01](产品化HTML原型_UX-01/)：UX-01 的连续评审壳、裸产品页面、
  共享设计 token 与静态验证入口；打开
  [review/index.html](产品化HTML原型_UX-01/review/index.html) 可离线审阅四条旅程；
- [产品化HTML原型_UI-05](产品化HTML原型_UI-05/)：UI-05 Gate A 的管理员与租户用户两条
  独立旅程及可重放验证。它继续作为历史产品合同与产品裁决证据，不冒充正式实现；打开
  [review/index.html](产品化HTML原型_UI-05/review/index.html) 即可离线回看；

- [产品化HTML原型_UI-02](产品化HTML原型_UI-02/)：租户管理、统一创作、笛语运维三套隔离
  产品空间，公开与认证、租户管理、内容生产和纯文字 DM01 四条完整旅程，以及独立评审壳；
- [UI 与首期能力黄金合同](UI与首期能力黄金合同_V1.md)：冻结 UI-G01—UI-G07、
  OPS-G01 与 CAP-G01—CAP-G06 的产品合同，不建设自动化测试平台；
- [UI-01R 候选基线](笛语产品化UI重构候选基线_UI-01R_v0.1.md)与
  [产品语言词典](笛语产品语言词典_V1.md)：保留已确认的产品叙事、用户语言和方向裁决；
- [产品化HTML原型_UI-01R](产品化HTML原型_UI-01R/)：UI-01R 已关闭的三套首页叙事动效、
  六张代表性裸产品页面与独立评审壳；首页正式方向采用 A“种子找到声音”，B/C 留作历史候选，
  六页不是正式 React 复制模板；
- `笛语交互UI完整架构设计_v1.1.md`、同名 HTML 与
  `笛语HTML高保真状态模型_v1.1.html`：保留为历史设计输入，不自动宣布继续有效；
- `产品化HTML原型_v0.1/` 与 `笛语产品化UI重构候选基线_v0.1.md`：UI-01 的 19 页覆盖
  盘点与历史反例，已被 UI-01R 取代。

V1.0、评审意见和 PNG 预览同样只作历史参考；`prototype_verified` 只说明原型可审阅，
不代表生产前端、认证或真实租户已上线。

关联资料：

- 架构裁决在 [../架构决策](../架构决策/)；
- M7-2A 的账号五段人话画像、单一可选创作方向面板、透明品牌转译和个人控制，上位裁决见
  [ADR-027](../架构决策/ADR-027-内容表达空间目录与用户控制边界.md)，实际落地的界面增量见
  [M7-2A内容控制面增量规范](M7-2A内容控制面增量规范.md)。该增量已在 React 工作壳实现
  （`document_verified`），未形成 V1.2 三件套，也不代表真实用户已验证；
- 正式 VI 只来自 [../../assets/brand/diyu-vi/README.md](../../assets/brand/diyu-vi/README.md)；
- 精简 V1 的逐项消费者、证据与后继边界见
  [../能力资产台账/精简V1能力消费者与黄金证据映射_V1.md](../能力资产台账/精简V1能力消费者与黄金证据映射_V1.md)；
- 当前实施与验收边界以根目录 `MILESTONE.md` 为准。UI-02 已按静态产品合同与旅程原型的有限
  范围 `CLOSED`；UI-03 正式 React 骨架与内容主链为 `CLOSED`，没有切换生产默认 UI；
  UI-04 已正式 `CLOSED` 并完成剩余首期业务接入与生产替换。UI-04 关闭时的四段 UI 路线
  已完整收口；随后由真实使用证据触发的 UI-05 已完成 Gate A 产品裁决、Gate B 正式工程化
  和 Gate C 生产替换。UI-05 的历史关闭结论之后又出现三个直接生产 UI 缺口，现已完成极小
  前向修复；后续创作责任合同恢复命中候选 B 结构停止线并由 UI-06 取代。UI-06 唯一生产
  候选又被真实 G1 否决，现已 `SUPERSEDED → UI-07`。UI-07—UI-11 的结构成果与失败
  均由 UI-12 承接；UI-12 当前用前置双轨与确定性 Compiler 取代概率 Reviewer 的生产
  放行并已 `CLOSED`。关闭不证明前端用户体验已经产品化；UX-01 已作为独立产品定义
  里程碑在有界返工和主控重审后 `CLOSED`。UX-02 已将该定义工程化并完成生产默认前端替换，
  当前为 `REVIEW`；唯一下一动作是主控最终独立终审 UX-02。
