# M7-2B 真实品牌内容协作试点执行包

- 状态真源：根目录 `MILESTONE.md`
- 当前里程碑：M7-2B（M6-3 successor）
- 启动状态（历史）：`ACTIVE`
- 当前状态：`REVIEW`（2026-07-26 主控终验唯一有界收口已达标；唯一真源仍为根 `MILESTONE.md`）
- 启动日期：2026-07-25（UTC）
- 唯一执行端：当前 WSL 执行端
- 起始 Git：`872c657a54e37abf90fb709b512c9b17d404e821`

> **主控终审返工（2026-07-25）**：上一轮 `REVIEW` 在
> `c95330daf257a8531591abc781a75d0d4a3b9f62` 被退回 `ACTIVE`。本轮不重建六篇、不删除
> 历史版本，也不改变 P1—P5、事实、隐私或租户隔离；只关闭 H2/S2/S3/H3/总部小红书的成品
> 一致性、页面/复制/导出/历史的统一人话投影，以及一次性激活链接的日志和失效语义缺口。
> 完成本地门、唯一最终 CI、备份、同 SHA 部署和生产反证后才能重新进入 `REVIEW`。

> **主控终验唯一有界收口（2026-07-26）**：基线
> `e1579783af31ca1beb05d3eef4ecec054087383b` 从 `REVIEW` 如实恢复为 `ACTIVE`。生产继续
> 运行，不回退、不重建六篇、不删除旧版本。本轮只允许：S2、S3 沿各自原任务形成下一版；
> 以 H3 V6 为唯一源新建总部小红书和视频号平台任务并保留旧任务；正式与 maintenance Nginx
> 四个 `/activate/` location 关闭位置级 access/error 日志、增加 `no-referrer`，且 HTTP
> 跳转不携带原始 URI。稳定后只做一次完整门、一次远程 CI、一次备份和同 SHA 生产定向复验，
> 达标即回到 `REVIEW`，不启动 M7-3。

## 一、唯一业务结果

> **当前裁决（2026-07-25，取代本包旧资料前置口径）**：M7-2B 使用
> `synthetic_business_fixture / 等深模拟业务资料`，通过正式生产代码、部署环境、数据库、
> FORCE RLS、正式 API 和真实 DeepSeek 证明完整内容协作软件能力。真实自然人、真实在售商品、
> 真实门店经营资料和真实平台账号是软件可用后的独立入驻动作，不再阻塞本里程碑。

用等深的笛语服饰演示资料和正式直连 DeepSeek 证明：一个总部岗位型表达身份和一个门店人物型
表达身份，能够分别依靠自己的五段画像、演示种子、合法作用域内的演示商品资料和连续前情，
自然协作完成多题材、多风格、可修改、可跨平台重编译的演示内容；两者同属一个品牌语境，但
不会说成同一种人。

进入 `REVIEW` 的最小交付固定为：

1. 总部、门店各一个等深演示表达身份及当前五段画像；
2. 两个连续系列各至少三篇，共六篇源成品；
3. 两个账号各至少一条 V1→V2，V1 保留可读；
4. 六篇中演示商品承重的 P1、P2、P5 各至少一篇；
5. 总部、门店各选一篇当前版，分别形成抖音、小红书、微信视频号版，共六份平台成品；
6. H1/S1 同题异身份、系列前情实际进入生成及一次透明品牌转译有可复核证据；
7. 两份有界人工审查、既有本地工程门、唯一远程 CI、新鲜备份、同 SHA 部署、生产健康、
   失败原子性、AIGC 披露、RLS 与回退均成立。

## 二、启动现场与交接差异

2026-07-25 只读核验结果：

- 本地与 `origin/main`：`872c657a54e37abf90fb709b512c9b17d404e821`，工作树干净；
- ECS 部署仓库与运行镜像：`436ac8923e45263a687ee8d2ce0b758371b1fb08`；
- 运行镜像摘要：`sha256:81cbada1c468fc6cc59c340225fd33afed4919231cee9c5e1c1b46f45373413c`；
- schema：`20260727_21`；本机、公网 readiness 正常，公网首页 `303`；
- 历史 DIFY 未进入本应用运行链路，正式模型仍为 `deepseek-v4-flash`；
- 笛语服饰已有“笛语服饰管理组织”和“浙江分公司”，但只有一个启用的抖音发布账号；
- “笛语服饰品牌官方账号”的控制组织值虽指向管理组织，来源仍为 `inferred`，不授予画像维护资格；
- 该真实账号五段画像为 `0`，真实长期商品为 `0`，连续系列为 `0`；
- 现有负责人对该账号的 `can_maintain_expression_profile=false`；租户管理员仍可在把控制组织
  明确声明为自身组织后，通过既有管理入口维护总部画像；
- 生产不存在可冒充本试点门店人物的启用发布账号；M7-1 的已停用验收身份不复用；
- 笛语服饰历史内容任务/运行/版本为 `196/196/157`，仅作历史证据，不冒充本试点成品。

交接基线无提交、镜像、schema 或健康漂移；真实资料缺口与 Prompt 预期一致。生产完整 SHA
不同于用户给出的短摘要只是完整展开，不构成差异。

## 三、最小工程补口

### 3.1 连续系列

- 复用 `content_series`、`content_series_items`、任务版本和既有重放能力；
- 新任务只有在用户明确选择系列时才读取该账号、该系列的必要前情；
- 支持继续下一篇、跳到指定集、在中间补一篇及重排；
- 冻结系列标识、系列修订、目标集次及实际读取的前情版本；V2 只重放任务快照；
- 平台重编译继承源任务冻结的系列前情，但不自动把平台任务加入源系列；
- 普通聊天、浏览、复制、导出、无关生成和失败生成不改变系列；
- 无真实发布记录或用户明确前提时，不得写成受众已经看过上一期。

### 3.2 平台重编译

- 复用 `recompile_task` 和 ADR-015；
- 用户从当前版本选择“另做平台版本”，明确选择目标平台载体；
- 源版本保留，目标账号、平台、媒体、制作条件、源画像与系列前情冻结；
- 服务端按正式会话和明确载体解析作用域，不再按同渠道账号名称排序隐式取第一条；
- 只创建本次需要的单个载体关系，不建设账号组、矩阵、发布连接器或平台 Agent。

### 3.3 真实商品事实

- 复用 `brand_products`，只新增本轮 1—3 件真实商品所需的人话入口和版本/来源元数据；
- 每件商品至少包含自然名称，以及任务真正需要的品类、颜色、材质或结构、轮廓、可观察特征；
- 记录租户、品牌、责任来源、当前版本、适用范围和状态；
- 任务冻结实际使用的商品事实版本；修改和平台重编译不得重读后来变化的商品事实；
- 商品匹配只在当前租户、当前品牌合法集合内进行，不依赖固定 `ZX-*`、`DIYU-*` 格式；
- DM01 快照、候选商品文档和视频推断不得自动升级为长期商品事实。

### 3.4 画像、身份与前端

- `inferred` 控制组织继续不授权；
- 总部画像由有资格的真实主体建立；门店人物账号、组织、本人资格与画像由真实主体建立；
- 五段画像沿用 ADR-027，不增加技术字段；
- 自然语言继续是主入口，创作方向面板仍为可选；普通用户只看到成品、自然修改、系列和平台版本；
- 不建设设置页，不展示 P1—P5 编号、GenerationInput、主张分类或运行轨迹。

### 3.5 R/O 边界

- R 是可人工审阅的制作质量：缺顾客、家庭成员、模特、复杂场地或道具时优先省略、替代或降级，
  可替代资源缺失不得让整篇确定性失败；
- O 继续区分当前立场与现实经历：`主张/希望/建议/尊重`可以表达，未经来源的既往询问、讨论、
  观察、服务或执行不得生成；真实本店做法只在本店作用域表达，不扩大为全国政策；
- 硬商品事实、权限、隐私、真人未成年人原件与租户隔离继续失败关闭；
- 不恢复 M6-3 候选，不加样本词表、固定成稿、第二编译器或随机重跑。

## 四、历史资料节点（已被等深模拟业务裁决取代，不再阻塞）

本节保留原渐进入驻设计作为历史，**不得再据此等待用户、标记 BLOCKED 或把真实资料入驻当作
M7-2B 软件验收前置**。真实客户决定正式入驻时仍可复用下列预填能力，但本里程碑已经改用
等深模拟业务资料继续验收。

旧口径曾要求：全部不依赖真实资料的工程与定向验证完成后，按渐进入驻原则保留一次资料节点，
且不要求用户从空白整理可由系统起草的资料：

1. 系统依据现有笛语品牌基线、声纹、账号角色和候选资料，为“笛语服饰品牌官方账号”预填五段
   画像；页面同时把现有 `inferred` 控制组织作为待确认预选项。品牌方只需纠正并确认，确认前
   不授予维护资格、不形成画像版本；
2. 系统从候选商品主档只提取视频可直接观察的 V 级最小字段，预填
   `DIYU-CSPU-001/006/009` 三件可编辑草案；候选名称、推断轮廓、面料、价格、性能和专业取舍
   均不自动成为事实。品牌方可纠正并确认其中本轮需要的 1—3 件，只有确认动作才写入
   `brand_products` 当前品牌事实；
3. 系统按现有真实表达账号、明确操作者和缺失渠道预填抖音、小红书、微信视频号内部内容载体
   草案；品牌方只需确认必要草案。载体不连接平台、不索取账号、密码或凭据；
4. 真实门店人物的身份、所属组织、本人一次激活、门店人物型账号与五段画像，以及一句真实工作、
   生活或门店观察，仍必须由该真实自然人完成；系统不得虚构、代激活或用临时验收自然人替代；
5. 总部默认制作条件也预填为一人、一部手机、普通室内或现有门店空间、现有商品，并优先降级为
   口播、持衣演示、局部特写、旁白或字幕；品牌方只在不准确时纠正。

这只是首次账号与资料配置，不形成逐篇授权、确认人、责任人、确认日期或审批。执行端不索取、
读取或代持用户密码、MFA、Cookie、Token 或明文密钥；需要本人登录时只给一个页面和一组动作。
除真实自然人身份、激活和本人真实种子外，不再把系统可以预填的事项退回用户手工整理。

## 五、真实内容顺序

1. H1：总部系列第 1 篇，种子为“走进门店只想自己看看，这种沉默是不是也应该被尊重？”；
2. S1：门店人物从自己的真实位置回应同题；
3. H1、S1 分别自然修改形成 V2，V1 保留；
4. H2：真实商品 A 的容易误解取舍，验收 P2；
5. H3：真实商品 A 或 A+B 的可见穿着可能，验收 P5；
6. S2：一家三口合照先统一颜色还是先保留舒服穿法，结合真实商品验收 P1；
7. S3：门店人物的一句真实小事经系统充分增益，不强行商品化、励志化或品牌宣言化。

六篇只做代表性组合，至少覆盖三种题材/机制、三种风格、三种可执行表现形式和一次透明品牌
转译，不跑 20/55/44 矩阵。

## 六、验证与收口

- 定向反证覆盖账号/画像/系列/偏好/素材/商品不串用，系列前情与旧任务重放，商品来源追溯，
  失败原子性，无自动回写，AIGC 披露，DM01 无回归，以及匿名/错误作用域失败关闭；
- 审查 A 只看用户成品、账号差异、受众价值、可说可拍、P1/P2/P5 与平台原生性；
- 审查 B 只看事实、作用域、版本、失败原子性、无自动回写、AIGC 与 RLS；
- 最多两份审查，不调用已禁用的系统级审查测试验证 Skill；
- 先跑定向测试，再跑 `git diff --check`、`make lint`、`make typecheck`、`make golden`、
  `make test`、前端 lint/typecheck/build/test；
- 本地全绿后只推送一个候选，读取唯一 CI；部署前做新鲜有界备份，迁移只 expand-first；
- 最终证据来自同一 SHA；通过后将根状态置为 `REVIEW`，停止且不启动 M7-3。

### 6.1 工程补口与本地证据（2026-07-25）

不依赖真实品牌资料的薄连接已经落盘：

- 新任务只有在用户明确选择系列后才读取该账号、该系列最多三条必要前情；最终
  `GenerationInput`、任务快照和输入回执同时冻结系列修订、目标位置和实际前情版本。首次
  生成成功才原子加入系列，失败不产生内容版本或系列项；V2 只重放冻结快照；
- 中间插入、跳集、重排及下一篇均复用现有系列对象；平台重编译继承源前情但不加入源系列，
  浏览、复制、导出、普通生成和修改均不自动改系列；
- 每个表达身份只有一个根发布账号；跨平台只能使用由管理员显式关联、且同一真实操作者已有
  资格的单个平台载体。服务端已删除“同渠道排序取第一条”的隐式选择；
- `brand_products` 增加责任来源、事实版本、适用范围和当前状态；只匹配当前租户、当前品牌
  的启用商品，并按任务冻结实际商品版本；运行时删除固定 `ZX-*` / `DIYU-*` 和门店默认商品
  回退；
- 推断控制组织继续不授权；新建真实发布账号时，画像维护资格独立于账号操作资格显式赋予。
  管理壳增加真实组织、1—3 件本轮商品和最小平台载体入口，普通内容壳只增加可选系列与
  “另做平台版本”；
- 可替代制作资源不再导致确定性失败；未知真人身份、硬商品事实、现实经历、隐私和租户隔离
  仍失败关闭。未增加第二模型、裁判模型、审核 Agent、第二编译器或重试层。

定向反证证明：第二篇实际收到第一篇不可变版本；系列重置后，旧任务 V2 仍重放原前情；
商品当前事实从 V1 更新到 V2 后，旧任务修改仍使用 V1；失败生成留下失败运行，但版本和
系列项均为 `0`；跨平台作用域只解析显式载体。随后完整本地门一次通过：

- `git diff --check`；
- `make lint`；
- `make typecheck`；
- `make golden`（含 OpenAPI 同步）；
- `make test`：`140 passed`；
- 前端 lint、typecheck、build、interaction test 全部通过。

以上仍只是工程能力和合成反证，不冒充真实品牌试点内容；真实身份、商品、六篇源成品与六份
平台版本仍只在一次真实资料节点后形成。

### 6.2 最终候选、部署与真实资料节点（2026-07-25）

- 首个远程运行 `30168689335` 在新库种子中暴露遗留商品展示名未同步；已只修复种子幂等写入。
  运行 `30168876707` 通过；
- 首次生产迁移尝试在有真实商品行的强制 RLS 表上暴露迁移回填未逐租户设置可信上下文；
  事务完整回滚，旧应用继续运行，schema 与数据未形成半迁移。回填改为逐租户执行后，运行
  `30169012400` 通过并成功迁移；
- 唯一资料动作前再移除商品入口中会要求用户指出“责任人”的文案，只保留自然的资料来源
  说明；最终同 SHA CI `30169197514` 通过；
- 当前本地、`origin/main`、生产部署仓库与运行镜像均为
  `52a70348255f64cce6efbe5c082542ecadd2180e`；schema `20260728_22`，镜像摘要
  `sha256:6e215b58ff04f635dc3f0ec3866be2be956ca0b3a8a676db7d56e518eb81534d`；
- 部署前备份 `20260725T181554Z-predeploy` 含 schema `20260728_22`、成功内容版本
  `185` 和对象 `1`；前一已知可用镜像 `436ac8923e45263a687ee8d2ce0b758371b1fb08`
  保留。回退不做 schema downgrade；
- 回环与公网 readiness 均为 `ready`，正式首页 `303`，近五分钟应用日志没有
  `ERROR/Traceback/Exception`；生产部署仓库干净，历史 DIFY 容器未改动且未进入应用配置。

最终只读复核仍为：总部账号控制组织来源 `inferred`、画像 `0`；真实商品 `0`、系列 `0`；
没有启用的真实门店人物账号。此时工程已经没有可继续代做的真实资料工作，正式到达第四节
规定的唯一一次合并资料节点。

### 6.3 渐进入驻减负纠偏（2026-07-25）

用户进一步裁定：唯一资料节点保留，但系统必须先使用现有资料起草，总部账号画像、候选商品和
平台内容载体不得继续要求用户从空白整理。实现据此增加一个版本化
`m7-2b-progressive-prefill-v1` 草案，不新增状态机或商品表：

- 总部五段画像草案只在品牌名、账号名和 `ContentRole` 精确匹配且尚无当前画像时展示；其来源
  和“确认前不是事实”边界同时可见，现有画像不会被覆盖；
- 三件商品草案只包含候选主档中的 V 级画面可见信息；草案经独立管理端点读取，不进入商品匹配
  或 GenerationInput。商品写入契约要求本页明确确认，保存后来源类型为
  `brand_user_confirmed`，未确认请求不产生商品行；
- 内部平台载体草案冻结原表达账号、目标渠道和唯一明确操作者；已存在的渠道不会重复建议。确认
  后才建立内部载体，且契约明确它不连接或登录真实平台；
- `inferred` 控制组织只作为页面待确认预选值，仍不授予画像维护资格；真实门店自然人仍不预填、
  不代激活、不虚构。

定向验证新增三类证据：候选画像/商品加载器保持 `review_candidate`；商品未显式确认返回
`422` 且数据库行数为 `0`；平台载体草案只使用明确源账号和操作者。管理端真实交互测试证明首件
商品和平台载体自动预填、确认请求携带明确边界、总部控制组织已预选且五段草案可见。完整本地门
更新后为后端 `143 passed`，前端内容交互与管理端渐进预填交互均通过。

唯一候选 `0ec4964d6e9c6a2f6daa5a420b5268510347ddf6` 的 CI `30182867956` 绿色；部署脚本先生成
备份 `20260726T012805Z-predeploy`（schema `20260728_22`、成功版本 `185`、对象 `1`），随后
生产部署同 SHA。运行镜像摘要为
`sha256:9de6f421dd530de52328007a4e9ebf2388e2e13687ea609b63f92167aa24006f`，回环与公网
readiness 均为 `ready/200`，首页 `303`，新预填端点匿名访问 `401`，近十分钟应用错误数 `0`；
上一镜像 `52a7034…` 保留可回退，schema 无变化且无需 downgrade。运行容器内实读草案版本为
`m7-2b-progressive-prefill-v1 / review_candidate`，总部五段草案存在、三件商品候选齐全；
生产数据库仍是控制组织 `inferred`、画像/真实商品/系列 `0/0/0`，证明部署没有把草案静默升级
为事实。历史 DIFY 容器未改动。

### 6.4 主控资料前置替代裁决与等深模拟业务夹具

主控随后明确撤销“等待用户填写真实资料”前置，旧资料节点只保留历史。M7-2B 改以
`synthetic_business_fixture` 证明真实软件路径，真实员工、商品和企业采用转为产品可用后的独立
业务动作。生产通过正式组织、账号、画像、商品、系列、任务、运行和版本对象建立：

- 笛语服饰管理组织、浙江区域演示组织、柯桥门店演示组织；
- 总部品牌内容运营演示账号、柯桥门店人物演示账号及各自抖音、小红书、微信视频号内部内容载体；
- 两套实质不同的五段人话画像 V1；
- 男童亮黄短袖、女童白色连衣裙、女童牛角扣学院外套三件“演示商品事实”，只登记本轮实际需要
  的品类、颜色、结构/轮廓与肉眼可见特征，不补价格、库存、性能、销量、设计动机或在售状态；
- 一句符合门店工作逻辑、但明确为模拟夹具的门店人物种子；无真实姓名、手机号或私人经历。

夹具执行器只有显式 `DIYU_M7_2B_EXECUTE=synthetic_business_fixture` 才运行；它使用现有租户管理
和内容服务、正式数据库与 RLS、正式 DeepSeek，不使用 stub、固定成稿、隐藏接口或 DIFY。演示
操作者使用随机一次性激活信息完成正式认证路径，明文只在进程内存在，运行结束撤销会话；证据
文件权限为 `0600`，不输出密码、Cookie、Token 或密钥正文。

### 6.5 六篇源成品、自然修改与系列前情（历史阶段；“当前”均为当时当前）

该阶段正式 DeepSeek 当时的当前成品如下；其后续当前版以 §6.9、§6.10 为准。完整正文和
历史版本见
[演示内容验收索引](M7-2B-演示内容验收索引.md)及生产人类界面。

| 系列 | 集次 | 当前成品 | 版本链 | 主要价值 |
|---|---:|---|---|---|
| 总部｜让选择保留余地 | H1 | 走进门店只想自己看看，这种沉默应该被尊重吗？ | V1、V2、当前 V3 | 当前品牌立场；不声称门店已执行 |
| 同上 | H2 | 牛角扣学院外套：这些结构能确认什么，不能确认什么 | 当前 V1 | P2 商品事实与不能推出的边界 |
| 同上 | H3 | 两个人不必同色，也能通过主次保留各自 | V1—V4；当前 V4 | P5 三段完整商品空间关系中的双人穿衣配色可能 |
| 柯桥门店人物｜在现场留一点余地 | S1 | 如果有人只想安静看看 | V1、当前 V2 | 本人当前理解与建议；不是本店做法 |
| 同上 | S2 | 一家三口拍合照，先统一颜色还是先各自舒服？ | V1、当前 V2 | P1 条件性选择 |
| 同上 | S3 | 关店前安静下来的几分钟 | 当前 V1 | 非商品化的一线小观察 |

H2/S2 的任务快照各冻结一篇必要前情，H3/S3 各冻结两篇；两条系列的实际前情版本、画像 V1、
商品事实版本和私人偏好模式均进入不可变任务快照。旧任务修改只重放原快照。失败运行没有内容
版本或系列项；浏览、复制、导出、平台重编译和表现记录没有修改系列、画像、目录、知识或偏好。

H3 选择“幽默玩梗”后，系统公开说明“保留幽默，收成克制的冷幽默”。最终有界复核否定了旧
V1 的 P5 结论：只改变取景框露出面积不足以证明新的穿着可能。系统沿同一正式任务自然修改，
V1—V3 保留，当前 V4 从第一视觉入口开始让两件商品完整可见，以同距离、白色靠前、亮黄靠前
三段空间关系形成“两个人不必同色，也能通过主次保留各自”的穿衣构图；移除视觉关系后主要
价值消失，故当前 V4 通过 P5。该结论只证明可执行演示构图，不扩大为真人、合身或上身效果。
S2 当前 V2 全文无桌子、桌面、纸、纸笔、笔或马克笔，只使用一人、一部手机和两件演示商品；
一条脱离当前系列的旧失败候选曾违反该要求，历史保留但不冒充通过。

### 6.6 六份平台当时当前版与有界审查（历史阶段）

- 总部抖音：`两个人不必同色，也能通过主次保留各自`，视频 V4；
- 总部小红书：`合照不必穿同色`，图文 V2；改为封面加三段的独立阅读链；
- 总部视频号：`合照里，颜色不用排队`，视频 V1；先给白色靠前结果，再回完整并列，最后以
  亮黄靠前收束；
- 门店抖音：`如果有人只想安静看看`，视频 V2；
- 门店小红书：`有些沉默不是拒绝`，图文 V2；改为四图距离与等待信号阅读链；
- 门店视频号：`开口前先停一秒`，视频 V3；以一秒静默开场并重排口播与结尾提问。

审查 A（用户成品与市场竞争基础）最终版=`PASS`：去掉账号名称后总部/门店仍不同；六篇有
受众问题、判断或关系回报，没有退化为同一模板；一人手机条件可说可拍；S2/H2/H3 当前版
分别通过 P1/P2/P5。H3 V1 的错误绿灯已撤销，以 V4 为当前证据；两个抖音版是源平台成品，
四份跨平台版在标题、开头、结构与媒体组织上实质重编译，不是平台名替换或机械截短。

审查 B（事实、作用域和回写）=`PASS`：商品主张来自冻结演示商品版本；没有编造人物、顾客、
真实门店做法、设计动机、性能、价格、库存、在售或销售事实；两账号、两画像、两系列、商品、
私人偏好与素材无串用；失败原子性、AIGC 披露、FORCE RLS、跨租户失败关闭与无自动回写成立。
这是本轮仅有的两份人工审查，未调用已禁用的系统级审查测试验证 Skill。

### 6.7 用户可见演示入口

租户管理员通过 `https://diyuai.cc/tenant-admin?section=demo` 进入稳定的人话验收索引。未登录先到
租户管理员登录，登录后直接回到该索引。页面只读投影正式生产对象，展示两个身份、两套画像、
六篇源成品的全部版本、系列前情使用数量和六份平台当前成品；正文没有复制到第二张表或写死在
前端。页面明确标示等深模拟边界，不显示技术 ID、GenerationInput、P1—P5 编号或规则轨迹。

管理员可在同页为两个演示操作者分别生成一次性安全进入链接；验收者设置独立密码后进入内容
工作台，可以继续生成、自然修改、读取旧版、继续系列和另做平台版。系统不交付共享密码，不连接
平台，也不要求先填写真实个人或企业资料。该只读投影由租户管理正式会话和 tenant + brand
FORCE RLS 共同限定；普通租户用户访问管理端点返回 `403`，匿名访问先进入管理员登录。

### 6.8 上一轮 REVIEW 证据（历史，已被主控终审返工取代）

- `git diff --check`、`make lint`、`make typecheck`、`make golden`、`make test`、前端
  lint/typecheck/build/test 全绿；后端 `150 passed`，两个前端交互套件均通过；
- 本里程碑唯一远程 CI 为 `9fbac56ab916ab5fd4aaaaa3c2732cf506d921f9` 的 run
  `30184924584`，结果 `success`；后续提交均 `[skip ci]`，没有拼接第二个候选 CI；
- 已验收的承重代码版本为 `418442081c26096ddb4fc5adbe004e74f3de9c9a`，其镜像摘要
  `sha256:f7082fec4d35626671f0e63310369808028c752912253d69a5bd7ec0a2afc15e`，
  schema `20260729_23`；最终运行镜像以本 REVIEW 状态同步提交的生产仓库 `HEAD` 为标签，
  该提交只改状态与证据文档，不改变承重代码；
- 新鲜备份 `20260726T045107Z-predeploy` 包含 schema `20260729_23`、成功内容版本 `242`、
  完整链 `214` 和对象 `1`，校验和通过；前一镜像 `6f5ee18…` 及 `2413541…` 保留，回退
  不做 schema downgrade；
- 回环与公网 readiness `200`，演示入口匿名请求转管理员登录并保留 `next=demo`，匿名管理 API
  `401`，当前容器 `ERROR/Traceback/Exception=0`；历史 DIFY 未修改；
- 最终生产验收索引证据由同一运行镜像通过正式管理 API 只读导出到
  `/var/lib/diyu-m5-4/m7-2b-review-<部署短SHA>.json`，权限 `0600`；实读为两个三篇系列、
  H3 V1—V4 和六份平台当前版。生产数据反证为演示根账号/画像/商品 `2/2/3`、前情
  `0/1/2 + 0/1/2`、六篇源任务模型均为 `deepseek-v4-flash`、14 个失败运行均无版本、
  关键表 FORCE RLS `true/true`、DM01 任务/运行/版本 `1/1/1`。

以上满足本轮完成门，根状态进入 `REVIEW`。唯一下一动作是主控与用户进行产品级人工验收；不
自动启动 M7-3。

### 6.9 主控终审返工最终收口（2026-07-25；历史，已被 §6.10 的当前版取代）

该轮没有重建六篇或删除旧版本，只沿既有正式任务、正式 API 和真实 `deepseek-v4-flash`
形成五份**当时的新当前版**；§6.10 已继续形成 S2 V4、S3 V7 和新的总部平台当前任务：

| 受影响成品 | 新当前版 | 有界结论 |
|---|---:|---|
| H2 `先看结构，再查性能` | V13 | 手机固定、一人始终持衣；只展示完整轮廓、牛角扣和学院外套结构；一句资料边界后给出“先看结构，再查性能”的 P2 方法 |
| S2 `一家三口拍合照，先统一颜色，还是先各自舒服？` | V3 | 手机先固定，创作者逐件持衣进入/退出；无桌子、纸笔、顾客或一家三口演员，原 P1 条件判断保留 |
| S3 `说一句话前，先停一秒` | V4 | 只有一名创作者、一部拍摄手机、空墙、呼吸和自然停顿；无顾客、衣架、问价、黑屏手机或真实关店事件 |
| H3 `同一对衣服，三种配色主次` | V6 | 三张静态画面，两件完整商品在同平面同距离形成等权、白色居中、亮黄居中三种关系；克制冷幽默、多图合集和透明转译一致，P5 主回报由画面关系承重 |
| 总部小红书 `四张图，三种配色主次` | V9 | 只有首图、第2张、第3张、第4张总结图一套顺序；首图方案、图序、正文和四条拍摄提示一致，无三/四张并存、局部露出、折叠或空白补图 |

同目标自然修改仍重放冻结画像、商品、系列前情、目录方向和私人偏好模式；同时把父版本正文作为
**待修改旧稿**提供给既有 DeepSeek 编写器，明确声明它不是事实来源。这样“只改一处、其他保留”
可在旧稿上落实，事实与资源继续只认冻结快照和可信来源；没有增加第二编译器、模型、审核 Agent、
关键词体系或重试层。失败修订继续原子关闭，不产生版本。

用户可见成品改为单一服务端投影：内部语义合同继续保存在任务快照、运行留痕和校验数据中，但
`账号观察/受众获得/账号关系/演示商品锚点/可见造型命题/画面成立条件` 等编译脚手架不再进入
新生成响应、当前版、历史版或演示验收页。用户看到自然概要、标题、完整正文/台词、画面/动作/
字幕/声音制作说明、发布配文和 AIGC 提醒；复制与导出消费同一正文投影并追加同一披露。生产只读
扫描的 37 份源、历史与平台可见版本全部无上述脚手架。

一次性链接边界同步关闭：

- Nginx 的 HTTP/HTTPS `/activate/` 均 `access_log off`，Uvicorn 关闭访问日志，应用请求日志
  只记 `/activate/:token`；
- 同一用户签发新链接时同事务失效所有旧未使用链接；成功使用时同事务失效其余链接并撤销旧会话，
  复用既有 `activity_events` 留痕；
- 生产 A/B 反证为 A 失败、B 只成功一次、B 再用失败、旧会话失效、新会话成立；本次 Nginx
  访问日志、错误日志和应用日志的原始 token 命中均为 `0`；
- 迁移 `20260730_24` 已失效生产演示操作者的既存未使用链接。历史访问日志不删除；其中 102 条
  唯一链接摘要均能对应数据库记录，当前仍有效且未使用的数量为 `0`。

最终一次完整本地门为 `git diff --check`、Ruff、mypy、Golden、后端 `153 passed`、前端
lint/typecheck/build/interaction test 全绿。唯一最终远程 CI、同一 REVIEW SHA 镜像、部署前
新鲜备份、生产 readiness、回退目标和权限/RLS/DM01 必要不回退证据随本 REVIEW 提交收口。

该轮根状态曾重新进入 `REVIEW`，随后又被 2026-07-26 主控终验退回 §6.10 的唯一有界收口；
本段不再表示当前状态。

### 6.10 主控终验唯一有界收口（2026-07-26）

本轮从上一轮 `REVIEW` 如实恢复为 `ACTIVE`，只关闭 S2、S3、H3 三平台父链和激活入口日志
四项，不重做 H2/H3 源成品、不重建六篇、不删除历史版本，也不改变 P1—P5、事实、隐私、
租户隔离或 DeepSeek 总体适配：

- S2 在原任务 `0007e8b3-b0f2-420b-9f9c-dc6c15c37dee` 形成当前 V4
  `8c4c39e2-902c-45c1-a6d2-ab2773dc1a93`。条件性 P1 判断继续成立；手机先固定，创作者
  逐件持衣各录一段并按本次目标回看，删除画面没有执行的“两件商品同框换位”承诺。没有顾客、
  家庭演员、桌子或纸笔。
- S3 在原任务 `2c74476f-d7d3-4515-8060-297a79f01e89` 形成当前 V7
  `d352bfc7-7d55-415b-bd6a-8fe3b529fa3e`，题为`安静时，先看清对方的节奏`。它只使用
  一名创作者、一部固定且全程不移动的手机、空墙、呼吸和停顿；从柯桥门店人物位置，以假设
  语气观察“对方尚未表示需要时的浏览节奏”，并明确这只是本人理解、不是本店既成做法。
  没有真实关店事件、顾客、商品、励志化或品牌宣言。
- 总部小红书新任务 `d8fbe92b-d43a-42ae-9dd8-5914bb87ab7b` 当前 V2
  `d8b01ad1-c236-4a3e-9c8a-c9ebe4c77b97`，题为
  `滑完四张图，看中心与侧边怎么改变颜色主次`；微信视频号新任务
  `a1221b91-3f14-4471-9c2f-3c946081061e` 当前 V2
  `91c47dcf-f20a-4098-81e5-84a0b78116dd`，题为
  `亮黄短袖站中间，白色连衣裙在旁边｜先看这一组`。两者的 `parent_version_id` 都精确指向
  H3 V6 `01afa226-2b8e-473d-914c-0d835a06be9a`；抖音源、四图滑读和结果先行连续视频共同
  保持“两件完整商品、同平面、同距离，以等权/居中/侧边形成主次”的 P5 核心，标题、开头、
  结构和媒体组织实质不同。所有旧平台任务和版本继续保留。

小红书跨平台任务的同目标后续自然修改曾稳定暴露一次 `factual_conflict`：任务已经是小红书
载体，但其后续修订没有继续携带原抖音源的改编语义，模型把旧稿误当成普通同目标父稿。最小
共享修复只让**确实跨渠道或跨媒体**的重编译任务在后续修订中继续携带原
`source_description`；同渠道同媒体的普通 parent 不会被误标成平台改编。父版本关系只读，
tenant + brand + account + created_by 失败关闭、冻结事实和失败原子性均不放宽；没有增加
模型、审核 Agent、重试层、关键词体系或第二编译器。修复后小红书和视频号正式 V2 均在一次
调用中成功；本轮小红书失败运行 `1` 个、关联版本 `0`。

一次性入口的最后日志边界同步关闭：正式与 maintenance 配置合计四个 `/activate/` location
均位置级 `access_log off`、`error_log /dev/null crit` 并返回
`Referrer-Policy: no-referrer`；正式 HTTP 激活入口固定 `303` 到 HTTPS 首页，不再携带
`$request_uri`。生产以明显测试夹具假 token 定向验证，HTTP/HTTPS 响应均为 `no-referrer`，
HTTP 跳转不含 marker；access/error/app 三类日志原文命中数分别为 `0/0/0`，没有读取、输出
或提交真实 token。历史日志不删除，既存未使用演示链接已失效的事实继续保留。

承重代码 SHA 为 `ca4f85c454217720ebc3328789a4c8d470ed9c44`，schema
`20260730_24`，镜像摘要
`sha256:64c0ec711e9daaa9d5d585578849ae64dc903f4afe0bb40de6034147d283b266`。
本轮唯一备份
`/var/backups/diyu-m5-4/20260726T081419Z-predeploy` 的 `SHA256SUMS` 全部通过，清单为成功
内容版本 `275`、完整链 `247`、对象 `1`；前一健康镜像 `e157978…` 保留且回退不做 schema
downgrade。定向测试通过，最终一次完整本地门为 `git diff --check`、Ruff、mypy、Golden、
后端 `155 passed`、前端 lint/typecheck/build/interaction 全绿。包含全部承重代码和收口证据
的唯一远程 CI 提交为 `7f920d491d198b2f9a4ef7d9565f9c7af686d300`，run
`30194854609`，`deterministic-quality-gate / quality` 用时 `1m17s` 且结论 `success`。
生产最终运行标签使用同一 CI SHA，复用
`sha256:64c0ec711e9daaa9d5d585578849ae64dc903f4afe0bb40de6034147d283b266`，没有重建镜像、
重跑迁移或生成第二份备份。
最终状态同步只改权威文档并以 `[skip ci]` 提交，不触发第二次 CI；最终状态 SHA 的不可变值
固化在生产 `/var/lib/diyu-m5-4/m7-2b-review-final.json`（`0600`）。

根状态据此重新进入 `REVIEW`；唯一下一动作是**主控重新进行产品级人工终验**。不启动 M7-3，
也不自行将 M7-2B 置为 `CLOSED`。

## 七、真实停止线

旧停止线中“缺真实身份/控制组织/画像”与“缺真实商品责任来源”已被等深模拟业务裁决取代，
不再成立。当前只在继续必须改变 P1—P5、账号权威、事实边界、租户隔离或里程碑范围，必须取得
密码/MFA/真实平台凭据或做未经授权的破坏性生产操作，或者同一稳定结构缺陷经一次最小共享修复
仍无法通过且继续只能降低硬事实、安全或隔离边界时停止。

模型首稿瑕疵、可自然形成 V2、可替代资源不足、搜索/趋势失败、无平台表现数据、普通工程问题、
中间只完成一部分或禁用审查 Skill 不构成停止理由。

## 八、未证明项

真实发布、平台流量和排名、爆款、GMV、销售、企业实际采用、多个真实租户之间的内容差异化、
企业 SLA 及 20/55/44 全部组合的稳定支持，均不属于本里程碑可宣称结果。
