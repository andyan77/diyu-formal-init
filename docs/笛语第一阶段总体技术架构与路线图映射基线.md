# 笛语第一阶段总体技术架构与路线图映射基线

- 状态：已确认
- 确认日期：2026-07-22
- 最新补充：2026-07-26（UI-02 已按有限范围 `CLOSED`；UI-03 正式 React 骨架进入 `ACTIVE`）
- 适用范围：第一阶段共享 SaaS 的设计、软件纵向闭环、部署、租户入驻与试点验收
- 取代事项：`M3-1-D1` 的“DIFY 或直接模型 API”候选比较

## 一、唯一架构结论

笛语第一阶段采用：

> **六个逻辑责任层，一套模块化单体，一套部署在 ECS 上的共享应用，以及独立于平台部署的新租户入驻生命周期。**

六层是责任边界，不是六套服务；路线图中的软件里程碑是贯穿必要层次的纵向闭环，不采用“先做完 KE、再做完 RAG、再做完编排、最后才做 UI”的横向瀑布。

第一阶段取消 DIFY。应用核心通过一个极薄模型适配器直连 `DeepSeek-V4-Flash`。DIFY 不再是候选运行路径、数据真源或部署依赖；首期也不建设多模型路由或自动模型降级平台。实际 API 的 `model` 参数由部署配置承接，不在架构文档中猜测供应商线上的字符串。

## 二、六个逻辑责任层

| 层 | 责任 | 第一阶段边界 |
|---|---|---|
| L1 交互 UI | 内容生产、陈列搭配、自然对话、成品阅读、修改、保存和入驻入口 | 不向普通用户暴露 ID、提示词、检索过程、内部评分或运行轨迹 |
| L2 笛语应用核心 | 可信身份、租户/组织/品牌/账号/角色解析，任务与价值路由，版本、系列、保存和事件 | 系统级、品牌级、人设级编排均在这里合成，不拆成三套 Agent |
| L3 作用域上下文编译器（RAG） | 先确定合法作用域，再检索 SQL/全文及获准素材，组装模型最小上下文 | RAG 不等于向量库；第一条闭环优先精确查询，质量缺口成立后才增加派生检索 |
| L4 KE 与权威数据 | 候选导入、专业复核、激活、版本和适用范围；SQL 保存权威对象、关系、权限、版本与事件 | 领域通用、品牌专属、组织和私人来源保持显式范围；候选不能冒充正式知识 |
| L5 模型生成适配器 | 接收应用核心已裁减的上下文，调用 `DeepSeek-V4-Flash` 并返回生成结果 | 不掌握身份、权限、知识真源、成品版本或事件；不预建多供应商路由 |
| L6 部署与运行 | ECS 应用、共享 PostgreSQL、必要对象存储、HTTPS、密钥、日志、模型调用观测、备份和回退 | 首期不建设微服务、Kubernetes、消息队列或独立工作流平台 |

## 三、一次生成的固定顺序

```text
登录身份
→ 服务端解析 tenant_id
→ 解析组织、品牌适用范围、发布账号/岗位身份和私人所有者范围
→ 判定内容生产或经营陈列任务
→ 在合法范围内检索并编译最小上下文
→ 通过小型适配器调用 DeepSeek-V4-Flash
→ 保存 GenerationRun、不可覆盖的成品版本和必要事件
→ 只向用户呈现自然说明与完整成品
```

`tenant_id` 必须来自服务端可信会话，客户端不得通过请求字段切换租户、品牌、组织或账号。所有租户拥有的数据、缓存键、后台任务、检索投影、对象路径、调用记录、导出和恢复都必须承接相同作用域；外部模型只接收过滤后的上下文。

租户根隔离采用双保险：应用层每次访问都显式携带服务端派生的合法作用域；共享 PostgreSQL 对所有租户拥有表启用并强制执行行级安全（RLS）。请求应用角色不得拥有 `BYPASSRLS`、不得作为表所有者绕过策略；迁移/管理角色与请求应用角色分离。缺少可信租户上下文时数据库访问必须失败关闭，插入和更新也必须通过同租户 `WITH CHECK`。RLS 负责兜住租户边界，租户内部的品牌适用、组织权限、发布账号和私人所有者范围仍由应用核心继续收窄，不为第一阶段建设通用策略引擎。

## 四、共享 ECS 运行形态

“一套共享应用”表示一套租户中立的代码和部署，不表示永远只能有一台虚拟机。第一阶段按预计 2—3 个、最多 5 个租户，全平台合计不超过 200 个已启用账号设计；这是账号容量冗余，不等于 200 个并发生成请求。首期从一个应用部署单元开始，真实负载超过当前边界后再横向扩容，不改变租户模型。

- ECS 承载模块化单体应用；权威关系存储统一采用当前项目独立的共享 PostgreSQL 数据库和共享 schema，租户拥有表以 `tenant_id NOT NULL`、应用显式作用域和强制 RLS 共同隔离，不采用每租户 schema。
- 每条租户拥有记录显式承接 `tenant_id`；租户内继续按品牌适用范围、组织权限、账号角色和私人所有者收窄。
- 领域通用知识可以是系统公共范围，但租户不能修改系统真源；品牌、组织和私人数据不得因共用基础设施而串用。
- M5-3 首次启用私人/组织媒体素材持久化时才接对象存储，应用本地磁盘不作为长期素材真源。
- 不为品牌、区域、门店或账号分别部署应用、数据库、代码分支或 CI/CD。
- 只有未来真实合同明确提出独立网络、数据地域、专属实例或 SLA 时，才形成专属部署方案。

### 正式域名与历史系统边界

第一阶段共享应用的正式产品入口冻结为 `https://diyuai.cc`。M5-4 已把当前项目的独立模块化单体、PostgreSQL/RLS 和对象存储边界接入该域名；生产应用只监听 ECS 回环地址，由 Nginx 提供公网 HTTPS，公开 readiness 返回 `200`。2026-07-22 的只读预检所见 `503` 只保留为接入前历史，不再是当前运行状态。

`dify.diyuai.cc` 当前仍代理历史 DIFY 环境，只是同一域名资产和 ECS 上的旧入口，不是当前项目的产品入口、编排层或业务真源。M5-4 只把 `diyuai.cc` 指向当前项目独立应用和独立数据边界，没有复用旧 DIFY 的工作流、数据集、用户、会话、提示词、知识、素材或运行状态。历史子域及旧容器继续独立处置，不阻塞当前项目，也不得被新系统静默继承。

## 五、KE、RAG 与检索演进

1. M0—M2 已冻结业务问题、25 类语义核、来源和使用边界。
2. M1-1 的 62 个文件和 243 条候选是用户提供的首批 KE 输入。M3-1-C1 已将它们完整登记为系统公共目录，但全量登记不是生产激活。
3. 当前已激活 41 条有直接消费者的系统资产：P1 的 4 条、DM01 的 13 条、M5-1 为 P2—P5 分别激活的 3/3/3/4 条，以及 M5-2 的 11 条媒体方法。每次内容生成先得到唯一主要产品，再只加载该产品和当前目标媒体实际适用的资产；H 类继续由可信会话、RLS、上下文编译和最小合同承接，不进入普通知识上下文。243 条全量登记不等于全量生产激活。
4. 运行激活唯一读取版本化的 `12-系统资产运行治理-v1.json`：它绑定当前候选聚合源的相对路径与 SHA-256、资产消费者和适用条件。导入按结构、唯一 ID、已知 schema、引用与快照完整性校验，而非固定数量；数据库生命周期只用 `review_candidate / active / deprecated`，`active` 还必须未过期且未被 `superseded_by` 取代。
5. `ContentBrief` 是运行时语义概念，首期物理上由 `business_tasks` 的内容目标/上下文以及 `content_items/content_versions` 共同承接；不新增 `content_briefs` 表或同义领域对象。
6. SQL 是权威真源；全文、摘要、向量和图查询都是可重建投影，不能替代租户隔离、对象 ID、版本、权限和审计。
7. 第一阶段不预选向量库。只有黄金任务或真实检索证明精确查询/全文稳定漏召回时，才裁决是否增加向量；GraphRAG 继续后置。
8. 未来如启用 pgvector 或其他向量引擎，向量投影必须承接权威来源作用域：租户专属向量使用 `tenant_id NOT NULL` 并受 RLS，系统通用向量进入独立公共投影或明确的 `system_public` 作用域，不把两者混成可空租户字段。查询先由服务端合成“获准公共范围 + 当前租户及必要品牌/组织/私人范围”，再执行相似度 Top-K；只在全库召回后做二次过滤不合格。外部向量引擎须使用不可由客户端覆盖的集合/命名空间和元数据过滤。

## 六、模型 API 与历史基础设施的复用边界

`M3-1-D1` 正式关闭：第一阶段不使用 DIFY，统一使用 `DeepSeek-V4-Flash`；离线确定性替身只是确定性测试模式，不是第二套架构。

用户已授权：真实模型调用需要既有凭据时，执行端可从 Windows `W:\home\diyu\笛语领域通用数据库` 对应的 WSL 只读路径 `/home/diyu/笛语领域通用数据库` 安全查找并复用与 `DeepSeek-V4-Flash` 兼容的凭据和连接参数，不再重复询问“复用还是新建”。执行端不得输出、提交、记录或复制明文密钥；若安全检查后凭据不存在、无效或无法调用已确认模型，只阻塞真实外部调用，并继续全部不依赖工作。

构建和测试阶段调用 `DeepSeek-V4-Flash` 不设人为成本预算或调用次数闸门，可按当前开发、黄金任务与人工语义审阅需要调用；不得以费用为由停工或重复请求预算。确定性 CI 仍使用测试替身，真实模型调用仍只服务当前里程碑，不扩张为无验收目的的批量生成或压力消耗。

同一历史仓库中的 ECS 与基础设施资料可以作为现场参数来源，但不得成为当前项目业务真源：

- Docker 运行方式和 PostgreSQL 引擎可以在当前里程碑确有消费者、现场兼容且隔离成立时复用；
- 复用 PostgreSQL 基础设施必须使用当前项目独立数据库或等效硬边界、独立数据库用户/凭据和独立迁移，不读取旧表、旧租户或旧业务数据；
- 现有向量设施的存在不构成启用理由；未来如评测触发，必须使用当前项目独立集合、作用域元数据和凭据；
- DIFY 即使仍在历史环境运行，当前项目也不得连接其工作流、数据集、记忆或运行状态；
- 禁止继承旧项目的品牌、组织、账号、知识、素材、向量、提示词、事件、用户记忆、Schema 含义和完成状态。

这属于“复用基础设施能力，不继承历史项目”。

## 七、技术架构与路线图映射

| 路线图节点 | 主要架构落点 | 证据性质 |
|---|---|---|
| M0-1 | L1/L2/L5：双应用、P1—P5、共享能力和路由合同 | 业务合同 |
| M0-2 | L1/L2/L5：自然交互、完整文字成品、版本事件和黄金基线 | 运行与验收合同 |
| M0-3 | L2/L3/L4：25 类语义核、承重关系和扩展边界 | 语义合同 |
| M1-1 | L4：领域候选、来源、方法卡、角色模板和评测规则 | 用户提供候选输入 |
| M1-2 | L3/L4：合成品牌真源、黄金上下文和渐进入驻参考 | 合成夹具 |
| M2-1 | L2/L3/L4/L6：租户、品牌、组织、私人素材与共享部署隔离合同 | 数据使用合同 |
| M2-2 | L2/L3/L4：前情复用、正式知识升级和禁止跨范围学习 | 连续性合同 |
| M3-1 | L1—L5：P1 自然输入、可信作用域、最小 SQL/KE、API 契约、模型边界、V1/V2 与保存 | 本地软件闭环 |
| M3-2 | L2—L6：诱饵范围、缓存/历史/故障反证，把黄金回归入口挂入远程 CI | 自动化持续闸门，不是上线 |
| M4-1 | L1—L5：商品清单到双层挂杆文字方案、反馈和局部新版；接入对应陈列知识 | 对内陈列软件闭环 |
| M4-2 | L1—L3：双应用自然入口、对外账号身份和对内组织岗位身份分流 | 双应用共同底座 |
| M5-1 | L2/L3/L5：P2—P5 与 X-01 价值路由；接入对应领域知识与生成方法 | 内容产品完整 |
| M5-2 | L1/L2/L5：三平台、视频/图文文字成品及媒体重编译；接入媒体与平台方法 | 媒体交付完整 |
| M5-3 | L1—L4：将前端 V1.1 纳入主仓并实现正式响应式 React 工作壳，复用既有内容/陈列 API；以合成可信会话交付相互隔离的租户用户/租户管理入口，以及渐进入驻、账号/表达身份、连续系列、私人/组织素材生命周期及对象存储边界 | 合成租户可通过正式产品界面完成核心工作与渐进入驻 |
| M5-K | L3/L4/L6：修复品牌资料错位与引用链，分离候选快照和运行激活清单，以结构/引用/快照完整性替代软件魔数，将少量确定性 H/夹具绑定既有 CI，并补齐最小有效期、被取代关系和文档对账 | 知识真源与运行治理在上线前收口；不扩建知识平台或激活候选池 |
| M5-4 | L1—L6：将同一租户中立版本部署到 ECS；完成三个生产入口、最小认证、共享 PostgreSQL/RLS、独立对象存储、密钥、日志、模型限流观测、备份/回退，以及模型文字成品的最小 AIGC 显式标识；事实边界只拒绝具体冲突与编造，不以普通词或固定句式误杀 | 共享 SaaS 在线且模型文字成品可被清楚标识，不证明真实企业采用、流量或销售 |
| M6-1 | L1—L4：已在既有平台创建首个授权真实租户；品牌方已确认品牌草案、一个实际使用的发布账号、当前主 `ContentRole` 和自然人操作资格，并以该身份完成非商品 P3。商品与真实挂杆按直接消费者渐进补齐，缺失只限制相应能力 | 主控终审通过并 `CLOSED`；不重新部署、不猜补局部资料 |
| M6-2 | L6：按最多 5 租户、合计 200 个已启用自然人账号的容量包络，验收模型限流、小规模突发、包含真实租户的新备份隔离恢复和版本回退；200 不是并发或业务硬上限 | 主控终审通过并 `CLOSED`；保持单实例和进程内限流，不建设分布式平台 |
| M6-3 | L2—L5：品牌调性、账号表达身份、专属知识缺口、范围串味与自然口语/事实不编造/制作可执行检查；用 8 个真实内容机会验证当前就绪与局部门槛 | 未通过；新候选 `3bcae817` 同 SHA 仅 `4/7`，Q2/Q4/Q7 失败、审查 A 失败、审查 B 通过，生产回退。终态 `SUPERSEDED`，successor 为 M7-2B；不表示成功 |
| M7-1 | L1/L2/L4/L6：笛语服饰一个门店、一组双层墙面挂杆和一次任务库存输入，经确定性 DM01 形成**参考执行方案** V1；用户自然提出差异时只改受影响部分形成 V2并保留 V1 | `CLOSED`；系统只交付参考建议，不要求确认人、授权、审批或现场回执；不消费 P1—P5、ContentRole 或 DeepSeek。真实现场采用与效果尚未证明，按裁决不构成软件验收阻断 |
| M7-2A | L1—L4：最小内容控制面——实际启用发布账号的可版本化五段人话画像及控制组织判权/运行追溯；原始风格/题材/体裁向内部五轴的机械对账；精简目录V1、可选创作方向、用户可见的透明品牌转译、体型主动启用、本人偏好控制、任务文字素材选择、账号冷启动/轻量计划、能力状态和未满足需求 | `CLOSED`（主控 2026-07-25 终审关闭；**CLOSED 只证明软件控制面成立，不证明真实 DeepSeek 内容质量、真实账号画像、发布、流量、企业采用或销售效果**，这些转入 M7-2B）；已实现并部署（首轮 `c0f2873`；2026-07-25 单一有界修复轮 `6710072`，schema `20260727_21`）。修复轮硬化：迁移推断出来的账号控制组织记为 `inferred` 且**不授予任何维护资格**，只有新建时明确指定或租户有权主体一次声明才成立；判权与版本写入同事务；当前画像指针由数据库约束封死到本账号；快照冻结实际使用的 ContentRole 名称与表达边界且修订只重放它；私人协作说明入模型不入租户记录并提供真正的临时无偏好会话；每轴区分沿用默认/本次选择/本次不使用且空选择不清空已保存默认；计划可「用这条开始」而不建任务；缺人工说明的原件不可勾选并可就地补写；笛语运维获得最小缺口分类与回告入口。自然语言仍为主入口，面板默认收起且每轴可不选。**对账诚实结果：风格 20/20、题材 55/55、体裁 41/44，缺 3 条登记为 `CAT-SOURCE-GAP-GENRE-001/002/003`，不得宣称 119 项全部登记或支持。** 2026-07-25 主控终审极小返工再关闭五处：同一轴优先级写死为 **本次明确选择 > 本次自然语言中未被否定的精确标签/别名 > 已保存默认**；为已声明标签/别名增加最小否定语义（`不要 / 不想 / 别 / 不用 / 取消`，仍不做模糊风格识别、关键词黑名单或第二模型），被拒绝的默认本次不生效且不修改已保存默认；修改接口读取临时无偏好会话请求头，同目标修改只重放快照、跨目标改编显式不读私人偏好；私人协作说明只控制协作与表达取舍，成品中不得引用、复述或解释其原文（提示级约束，未新增审查层）；修复前端交互验证在 Node 20 与 Node 22 的兼容。 未证明：有界 DeepSeek 真实内容冒烟未执行；真实账号的控制组织仍是推断值、五段画像仍未由有权主体保存。不建全国强制人格系统、设置中心、119项全量前端或自动学习 |
| M7-2B | L1—L6：一个总部岗位账号和一个门店人物账号使用已确认画像，各完成至少三篇连续系列；验证账号差异、系列前情、透明品牌转译、内容质量、Q2/Q4/Q7的R/O实际表现；合法商品/商品组分别验证P1/P2/P5；同一源成品分别形成抖音/小红书/微信视频号版本，按需使用带来源和时效的上下文、发布辅助和自愿反馈 | `CLOSED`（主控 2026-07-26 独立产品级终验通过）；M6-3 successor。已形成 H2 V13、H3 V6、S2 V4、S3 V7、单一服务端人话投影和安全的一次性入口；以 H3 V6 为唯一源形成总部小红书/视频号 V2，父版本精确且旧任务保留。正式/maintenance 四个激活入口关闭 access/error token 日志、返回 no-referrer，HTTP 丢弃原始 URI。schema `20260730_24`；本地门、备份、同 SHA 部署、生产复验与唯一 CI run `30194854609` 均通过。关闭不证明真实员工/商品采用、真实发布、平台市场效果、多真实租户市场差异或企业 SLA |
| M7-3 | 全层：内容与DM01两条正式路径、三入口、桌面/移动、账号画像版本追溯、权限与偏好隔离、同品牌总部/门店账号差异及三条护城河当前可证明部分的首期整体验收 | `CLOSED`（2026-07-26 主控产品级独立终审 `PASS`）。正式内容 bootstrap 返回权限解析后的 `current_target`，账号身份、默认目标和直接提交一致；人类页面按入口跳转或返回人话 HTML 403，API 保持 JSON 401/403。承重实现与部署 SHA `af63a62c9169ba94907bf8316059109e9191ed66`，唯一 CI `30200960337` 为 `success`，生产 readiness 正常。关闭只证明第一阶段软件闭环、生产部署、两条业务路径、入口恢复、隔离边界和有界产品验收成立，不承诺真实采用、发布、流量、排名、爆款、GMV、销售、真实跨租户市场差异、企业 SLA 或 `20/55/44` 全组合稳定支持 |
| UI-01 | L1：19 张静态页面覆盖盘点和首版首页动效 | `SUPERSEDED`，successor 为 UI-01R；旧 v0.1 只作页面覆盖盘点与历史反例，不是当前设计基线 |
| UI-01R | L1/L4 投影：产品叙事、资产能力只读总账、核心体验原型和独立评审壳 | `CLOSED`；只表示产品策略探索、资产盘点和方向裁决完成。首页采用 A“种子找到声音”，B/C 留作历史候选；六张代表页不是正式 React 复制模板，UI-01/UI-01R 历史文件完整保留 |
| UI-02 | L1/L4 投影：角色任务架构、能力映射与全链路高保真 HTML 原型 | `CLOSED`；只证明三套隔离空间、四条静态旅程、纯文字 DM01、25 项精简 V1 消费者映射和 UI-G/CAP-G 产品合同成立，不证明正式 React、正式 API 或生产 UI |
| UI-03 | L1/L2/L3/L4：正式 React 设计系统、公共首页、A 动效、三入口、统一创作端与内容主链 | `ACTIVE`；接正式路由、API、可信身份和任务快照，只消费内容主链直接需要的既有激活资产，不切换生产默认 UI |
| UI-04 | L1—L6 接缝：纯文字 DM01、系列、素材、租户管理、运维、状态页与生产替换 | 在 UI-03 以后接入剩余首期业务、最小 Chromium 冒烟、备份、生产切换与回退；不建设绘图、多模态、交易、自动发布或批量清偿 F10 |

### M7-3 整体验收结果（`CLOSED`）

- M7-2B 保持 `CLOSED`。总部/门店 `2` 个表达身份、`2` 个连续系列、`6` 篇源成品、`6` 份
  平台成品、自然修改历史和 H3 平台父链均只读复用并成立，没有重建六篇、删除旧版本或改写
  父子关系。资料仍明确属于 `synthetic_business_fixture / 等深模拟业务资料`；正式代码、
  部署、数据库、RLS 与 DeepSeek 路径真实，但不冒充真实员工、真实商品或真实经营采用。
- 内容与 DM01 两条正式路径共同成立。内容页面、复制和导出使用同一用户投影；DM01 继续只交付
  不调用 DeepSeek、也不误标 AIGC 的参考执行方案，关闭证据及生产对象无回退。正式内容页覆盖
  服务端 bootstrap 的集成缺陷已在同一共享入口修复；`1440×900` 与 `390×844` 的真实浏览器
  验收合计 `131` 条断言通过，三入口和移动双段切换成立。
- 正式 `/content` bootstrap 显式返回服务端已经完成权限解析的 `current_target`，使账号身份、
  默认目标和不手工切换的直接提交保持一致，且没有重排目标列表。人类页面无会话时按入口跳转，
  错误入口资格返回带恢复动作的人话 HTML `403`；`/api/v1/*` 保持 JSON `401/403`。
- 总部/门店账号画像、ContentRole、任务快照、系列前情、历史版本和私人偏好边界可追溯；
  RLS、错误租户/品牌/账号/自然人失败关闭及失败不产生半任务、半运行、半版本继续成立。
- 唯一生产反馈探针明确标记为 synthetic，经过正式提交、笛语运维分类回告与用户读取；本人可读、
  他人失败关闭，目录、知识、方法、画像、私人偏好、系列前情等受保护对象均无回写。生产预期只
  新增 `1` 条已回答的 synthetic 未满足需求候选；发布后自愿反馈仍由 F08 承接。
- 目录对账冻结为风格 `20/20`、题材 `55/55`、体裁 `41/44`；能力状态为
  `verified 10 / composable 64 / experimental 6 / unsupported 38 /
  explicitly_out_of_scope 1`。三个来源缺口及 9 条原误指向已关闭 M7-2B 的
  `unsupported` 后续去向均为 F10，状态未被提升、没有孤儿。
- 当前生产承重实现与部署 SHA 为 `af63a62c9169ba94907bf8316059109e9191ed66`，唯一远程 CI
  `30200960337` 为 `success`；运行镜像摘要为
  `sha256:3d9e2847f5c2ba482200be2a66658f859a60b4e58eefa1ac922ac8418837b7cb`，
  schema 为 `20260730_24`。部署前备份
  `/var/backups/diyu-m5-4/20260726T115643Z-predeploy` 校验通过；最终文档 SHA 为
  `1860861bc2180e60bc360a3ff932252b58cbe394`。公网/回环 readiness 正常；此前实现和镜像
  只作为历史与回退锚点保留，不再描述为当前最终生产锚点。
- 三条长期护城河的证据等级保持诚实：**已证明代表性范围**内的同品牌总部/门店差异、多题材、
  多机制、多风格、多形式与透明转译；**部分证明**租户/RLS 隔离和合成反串味，真实跨租户市场
  差异化未证明；**部分证明**事实准确、受众回报、完整可说可拍成品、系列连续、三平台实质
  重编译和受控反馈候选，不证明真实发布、平台流量/排名、爆款、GMV、销售、企业采用、持续
  市场进化、全 `20/55/44` 组合稳定或企业 SLA。

主控已完成产品级独立终审并裁决 `PASS`，M7-3 当前状态为 `CLOSED`。关闭只证明第一阶段软件
闭环、生产部署、两条业务路径、入口恢复、隔离边界和有界产品验收成立；不证明真实员工采用、
真实商品采用、真实门店现场执行、真实发布、平台流量、排名、爆款、GMV、销售、企业采用、
真实跨租户市场差异、企业 SLA 或 `20/55/44` 全组合稳定支持。该关闭时点的下一讨论是
“用户界面产品化重构”；随后 UI-01 已被 UI-01R 取代，当前承接见下节。

### 第一阶段关闭后的 UI 产品化路线

UI-01 的 19 张静态页面已经完成覆盖盘点，但创始人产品审查确认其首页没有产品叙事、页面按
功能机械铺开、产品与评审模式混用，不能直接成为 React 工程化输入，因此状态为
`SUPERSEDED`，由 UI-01R 承接。旧 v0.1 完整保留为历史反例。

UI-01R 已完成产品叙事、五个产品空间、四条任务旅程、六张代表性页面、三套首页动效、独立
评审壳，以及现有 JSON/JSONL 资产真源的人可读总账，现正式 `CLOSED`。关闭只表示产品策略
探索、资产盘点和方向裁决完成，不表示六张代表页可以逐页复制到 React。UI-01 与 UI-01R
历史文件均完整保留。创始人最终采用 A“种子找到声音”作为首页方向，B/C 只保留为历史候选；
A 的正式 Logo 定格保留约 `0.8—1s` 静止呼吸，正式 VI SVG 继续是唯一 Logo 真源，首页文字
仍未冻结为永久品牌口号。

UI-02 已按有限范围 `CLOSED`，将产品架构收敛为三套工程空间：租户管理端、总部/区域/代理商/门店共用的统一创作端、
笛语运维端。角色差异只能来自服务端可信组织、当前发布账号、ContentRole、操作者资格和当前
可用能力；没有资格的能力不显示，双入口资格不静默混用。UI-02 以离线高保真 HTML 走通公开
与认证、租户管理、内容 V1→V2→V1、纯文字 DM01 V1→V2→V1 四条旅程；DM01 只交付文字
参考方案，不提供或暗示陈列图、效果图、墙面预览或自动还原现场。

后继顺序保持为 UI-03 正式 React 产品骨架与内容主链、UI-04 全业务接入与生产替换。UI-03
现已启动，接入真实路由、API、可信身份和任务快照，只绑定内容主链直接消费的资产与规则，不切换生产
默认 UI；UI-04 才接入纯文字 DM01、系列、素材、租户管理、运维和状态页，并完成生产切换与
回退。UI-04 仍须等待 UI-03 关闭后才能启动。

平台上线与租户入驻严格解耦：M5-4 可以只承载“折线之间”；一个授权真实单品牌租户仍是最低试点验收锚点。第一阶段可按实际就绪情况渐进接入预计 2—3 个、最多 5 个租户，全部复用同一已上线平台，不复制代码、不重走 CI/CD、不默认新建独立数据库。

2026-07-24 的 M5-4 执行验收已证明合成演示租户在正式认证和真实 DeepSeek 下完成冻结 P2 连续 `2/2`、错误事实反证 `1/1` 与 AIGC 来源/阅读/转移 `3/3`；生产应用角色无 `BYPASSRLS`，无租户上下文和错误租户均失败关闭，失败生成不产生版本。根因修复使用语义判断与关系级事实反证，不再用普通词黑名单或固定句式充当事实合同，也没有放宽具体商品、现实事件、个人信息和内部信息边界。主控终审未发现需要退回的业务、安全或隔离缺口，M5-4 已正式 `CLOSED`。

同日的 M6-1 执行验收在同一共享 SaaS 创建“笛语服饰”真实租户、品牌和首位自然人管理员，由品牌方本人完成一次性激活、品牌表达 V1、首个发布账号、独立 `ContentRole` 和本人授权；真实 DeepSeek 非商品 P3 已按服务端可信租户/品牌/账号/自然人作用域生成，AIGC 披露成立且无合成租户串味。错误租户、品牌、账号和未授权自然人读取均失败。首次探针暴露无商品事实仍可能误路由 P2 后，执行端在任务创建前增加确定性商品事实门，未通过的 P2 不产生任务或版本；商品和真实挂杆继续保持未激活，只限制商品承重 P1/P2/P5 与 DM01。主控独立复核完成定义、提交、定向测试和唯一 CI 后未发现需退回缺口，M6-1 已正式 `CLOSED`。M6-2 随后以最多 5 租户、合计 200 个已启用自然人账号作为容量验收包络，只验证单实例模型限流、小规模突发、包含真实租户的新备份隔离恢复和版本回退；不把 200 解释为并发或业务硬上限，不建设分布式限流、队列、压测平台或企业级 SLA。

M6-2 执行验收以隔离测试库完成 5 租户、200 个已启用自然人及正式品牌/账号/角色/授权关系，跨租户、错误品牌、错误账号和未授权自然人均失败关闭；正式内容 API 的全局、单租户、单用户和分钟速率限流快速返回 `429`，拒绝不落任务/运行/版本，成功、失败和取消均释放槽位。生产保持单实例、单 worker 和 `4/2/12` 模型边界，20 路读取突发与 4 路无敏感 DeepSeek 探针通过；真实进程异常后自动恢复并可继续读取 M6-1 P3。新备份用随快照清单替代固定资产数量，在隔离库验证真实应用角色 RLS、M6-1 完整关系链、readiness 和无敏感对象恢复；候选回到 M6-1 再恢复候选时 schema 与真实关系无损。上述只是一组当前 ECS 观察值，不构成企业 SLA、RTO、RPO 或 200 并发证明；主控复核后已正式 `CLOSED`。

M6-3 曾以笛语服饰真实品牌官方账号运行八个自然内容机会；`58a52f1` 的运行、隔离、
Q8 原子性和 AIGC 证据成立，但其 Q4 使用未登记合照、Q7 把品牌观点写成现实服务做法，
故旧 `7/7`、审查 A `PASS` 和 `REVIEW` 已被主控推翻。随后新候选 `3bcae817` 只增加
R“资源能力闭世界”和 O“品牌观点不等于经营做法”，在同一 SHA 下冻结 Q1—Q8 各请求一次：
Q1/Q3/Q5/Q6 成功，Q2/Q4/Q7 在唯一一次单元修复后失败，Q1—Q7 仅 `4/7`；Q8 最小
追问且任务/运行/版本差分 `0/0/0`。审查 A `FAIL`、审查 B `PASS`；失败运行无半版本，
租户/品牌/账号/角色、知识、41 条激活资产、AIGC 和 DM01 完整性保持。因 Q4/Q7 正是
目标结构路径，已命中停止线，生产安全回退至 `58a52f1`。主控随后将 M6-3 置为
`SUPERSEDED`，successor 指向 M7-2B；只替代原验收方法和 M7-1 的串行依赖，不表示通过
或关闭。商品承重 P1/P2/P5、具体门店 P4、DM01、真实发布、平台表现、销售、企业采用和
多真实租户差异化均未得到证明。

D-033 已由 ADR-027 正式记录为已确认规划：内部使用题材、内容机制/讲法、表达风格、媒介
形式、连续与参与方式五轴；原始20条风格、55条题材、44条体裁必须形成完整能力对账，
精简目录V1和可选创作方向由M7-2A承接。M7-2A同时承接只面向实际启用发布账号的可版本化
五段人话画像、组织控制、私人偏好、合法素材选择、账号冷启动和能力缺口；总部与门店真实
系列、透明品牌转译、内容质量、平台/在地上下文和R/O实际表现由M7-2B承接。
长期核心目标是品牌一致性下的表达广度、不同租户实质差异化与反串味、自媒体内容市场
竞争力与持续进化。这些是已确认规划，不是已实现、已验证或流量/销售证明。

所有已经确认但没有进入M7-1、M7-2A、M7-2B或M7-3的事项，统一进入
[待落盘推进事项清单](待落盘推进事项清单.md)，按真实消费者触发首期后里程碑；不得只
留在对话、旧评审或“未来考虑”中。该清单不替代根 `MILESTONE.md`，也不形成平行状态机。

M7-1 已与内容生产解耦，完成数据驱动改造与产品语义纠偏后由主控终审置为 `CLOSED`。用户 2026-07-25
裁定：系统只根据租户、品牌、账号、用户输入和可用知识交付**参考建议**，不自动执行陈列，
也不承担监管责任；因此不设确认人、确认日期、系统代录人、复核人和“业务指定／系统建议”
二选一，账号与数据访问权限是一次性的系统资格而非每次任务的授权流程。门店可复用结构与
本次任务输入分开保存，本次库存不写成长期真实库存、本次焦点建议不写成长期品牌政策，也不
用旧快照阻断新任务。原“阿丹已现场确认”等无来源表述已被撤销。每次 V1 会把本次表达、商品
轻量属性和挂杆结构冻结到该任务；没有保留这些条件的历史任务不再生成新版本，只返回一句
“请按当前库存新建一份方案”的自然提示，作用域之外的任务仍直接失败关闭。原带确认语义的
验收成品已连同其 artifact / run / version 精确清理，证据 ID 与备份位置保留在文档中；一次性
验收账号资格也已退出生产。真实现场执行、采用与效果尚未证明，但不构成 M7-1 软件验收阻断。

### 前端交付映射

- `docs/前端UI架构/` 是前端设计与交互文档真源；V1.1 继续作为历史设计输入，不自动宣布
  继续有效，也不覆盖创始人对 UI-01R 的新裁决。
- V1.1 的高保真 HTML 和 16 个状态属于 `prototype_verified`；UI-01 v0.1 的 19 张页面属于
  覆盖盘点与历史反例。两者都不证明生产前端已经实现；UI-01/UI-01R 历史文件完整保留。
- UI-01R 已把评审工具与裸产品页面分离，并完成产品策略、资产盘点和方向裁决；六张代表页
  不是 React 复制模板。UI-02 将产品结构收成租户管理、统一创作、笛语运维三套隔离空间并
  扩展四条完整旅程；UI-03/04 再分阶段进入正式 React 和生产替换。
- M3-1—M5-2 的轻量服务端页面服务纵向业务合同验证，不追溯改写为正式前端完成证据。
- M5-3 使用一套 React + TypeScript 工作壳承接现有内容与陈列闭环，并实现当前直接消费的入驻、账号/表达身份、系列和素材能力；不把 16 个原型状态机械开发成 16 张独立页面。
- M7-2A 在现有工作壳中只增加一张人话账号画像卡和一个可折叠“创作方向（可选）”面板。
  画像在现有身份/上下文抽屉展示摘要；总部维护者从既有账号管理进入，区域/门店维护者从
  自己的账号身份卡进入，服务端按控制组织判权。透明转译保留用户原选择，用一句人话解释
  品牌边界和转译结果并允许继续调整。不新增独立设置页、五轴向导或复杂参数中心；移动端
  继续只有“对话/成品（方案）”两段。
- M5-3 内的租户用户入口和租户管理入口互不展示页面或导航，服务端按当前入口和资格失败关闭；同一自然人确需兼任时仍一人一号、分别取得入口资格。每个租户用户最多维护一个本人默认表达人设，题材/类型/风格不受限；企业发布账号人设独立，允许多个已登记操作者共同运营，但发布账号本身不得成为共享人员登录。
- M5-4 在同一工作壳上补三个正式生产登录入口、ECS/HTTPS、密钥、日志、限流、备份和回退，不重新建设第二套 UI。租户管理员创建人员后交付用户名和一次性激活链接，由用户自行设密；首期不建设短信发送。笛语运维使用独立平台操作身份和 MFA，不能借平台权限查看租户私人素材或内容正文。
- M6-1 已把租户管理作用域从发布账号作用域中分离：管理员可在零发布账号时确认人话版品牌草案，再以一个事务创建首个发布账号、独立 `ContentRole` 和本人授权；唯一操作者自动选中，缺项给出明确提示。激活后的管理员进入租户管理入口，不再误导到尚无业务资格的用户工作台。
- M6 用授权真实品牌验证入驻和内容质量；M7 用真实账号和门店证明实际业务结果。原型、合成软件、上线和真实采用四类证据不得互相冒充。

## 八、贯穿性质量与商业运行闸门

### 1. 黄金基线是持续闸门

- M3-1 形成一个本地统一黄金回归入口，不按固定文案或向量相似度判定。
- M3-2 将确定性的合同、作用域、版本、路由和失败检查接入远程 CI。
- M3—M5 每次新增产品、修改 API、上下文、知识激活、模型适配或核心提示策略，都必须重跑受影响黄金套件。
- 真实模型语义质量不进入每次提交的确定性 CI，原因是结果非确定性且依赖外部服务，不是成本预算；模型、提示、知识编译或主要生成策略实质变化时，按需运行黄金任务并进行一次人工语义审阅。

### 2. 品牌入驻验收

- 每个真实租户在 M6-3 检查品牌调性符合度、专属知识缺口及是否混入“折线之间”或其他租户表达。
- 一个真实单品牌租户是第一阶段最低试点验收锚点；预计接入 2—3 个、最多 5 个租户是容量和渐进入驻边界，不成为等待客户资料的施工阻塞。
- 从第二个真实租户起，同一 M6-3 合同增量启用跨租户诱饵和串味检查。若这些租户均为单品牌，仍不构成“同一租户多品牌”真实运行证明；只有真实多品牌租户出现时才验证 D-017 的该部分。
- “笛语服饰”作为首个真实品牌候选，与系统产品“笛语”使用不同内部品牌标识。M5-K 只把 Windows 权威目录中的品牌资料整理为待品牌方确认的入驻候选，不提前创建真实租户或生产激活。M6-1 先由品牌方确认品牌基线、一个实际使用的发布账号、当前主 `ContentRole` 和自然人操作资格，即可启用不依赖具体商品与门店事实的内容能力；少量真实商品事实和真实双层挂杆固定档案是能力局部门槛，分别在 M6-3 与 M7-1 的直接消费者出现时补齐，不再作为整个租户入驻的全局阻断。19 份重复的领域 G/H/合同/夹具文件不进入品牌库；《笛语品牌陈列表达基线 V1》只提取首期“一组双层墙面挂杆”会实际消费的品牌陈列约束，如该挂杆的焦点主次、视觉密度、色彩/轮廓关系和明确拒绝项，且必须由品牌方确认；《笛语实际门店挂杆条件档案 V1》只提取该组挂杆的上下结构、容量、正挂位、黄金视线、主要来客方向和不可改变条件；《笛语门店挂杆拍摄与测量采集清单 V1》只在上述固定条件无法自然说明时，提供一次性的最小采集提示。三份原稿中的橱窗、中岛、展台、模特、全店灯光、亲子/家庭主题、P1—P5 联动、视频拍摄、全套测量和组织治理等暂未采用内容不进入首期品牌知识、运行上下文或系统表单。
- 笛语服饰首批 26 条视频继续保存在品牌方云盘，首期不导入系统、不建设逐素材授权台账，只保留“品牌方提供、外部保存、当前未激活调用”的包级说明并遵守 D-020；未来直接读取云盘、复用原片或公开商业演示时另行立项。真实 DM01 的运行输入仍是已确认的门店固定档案和本次实际库存。

### 3. 模型调用用量、限流与失败恢复

- 主要容量约束在 `DeepSeek-V4-Flash` 调用链，而不是 5 个租户对 ECS/数据库形成的计算压力。M5-4 至少按租户、任务/会话记录请求、供应商用量、延迟、限流和失败，不建设完整计费系统。
- 共享环境对模型调用设置简单的全局/每租户并发与速率上限，遵循供应商 `Retry-After` 或有界退避；达到重试上限后清楚失败，不产生半写入成品版本。第一阶段不为削峰建设分布式消息队列或调度平台。
- `DeepSeek-V4-Flash` 价格低不取消用量和错误观测，但构建测试不设成本预算闸门。生产限制服务于供应商速率边界、稳定性和防滥用，不由模型供应商账单替代笛语自己的租户归集。

### 4. 非功能验收

- M6-2 已对共享发布候选按“最多 5 个租户、合计 200 个已启用账号”完成相称的启动、正常请求、小规模突发、模型限流、备份恢复和版本回退验证；200 个账号没有转写成 200 并发压测。
- 后续只根据真实试点合同和实测负载决定扩容与恢复目标；没有明确合同依据时不虚构企业级 SLA，不建设多地域、双活、分库分表、读写分离、分布式缓存/队列或大规模压测平台。

### 5. 素材与合规边界

M5-3 是媒体素材首次进入持久化边界，但不得把它升级成重型媒体审批或清理流水线：

- D-030 的文字规则从 M3 第一次生成起即生效：生成文字默认移除具体顾客个人标识并使用非识别性称谓；首期不建设独立 PII 扫描服务；
- 私人图片和视频保持原件、仅在原合法作用域作为创作参考，系统不编辑、打码或匿名化媒体；
- D-020 对已知或用户声明的可识别真人未成年人原件，在持久化或外部模型传输前拒绝；不建设年龄识别、监护人审批或自动打码；
- 平台红线约束生成文字和制作建议，不把素材入口变成监管审批系统。

## 九、技术选型与 API 契约冻结点

- M3-1：形成唯一、版本化、机器可读且可审阅的最小业务 API 契约；冻结客户端不能改变可信作用域的边界。
- M3-1-D2：已关闭为当前项目独立 PostgreSQL、共享 schema、应用显式作用域与强制 RLS；只建立当前纵向闭环直接消费的表和策略。
- 首次真实模型调用前：安全读取已授权既有凭据并确认 `DeepSeek-V4-Flash` 的实际连接参数；不重新讨论 DIFY 或模型供应商。
- 向量检索：仅在已有检索稳定无法通过黄金或真实任务时裁决。
- M5-4：以 `https://diyuai.cc` 为正式产品入口，完成当前项目独立应用的 HTTPS 接入；同时确定 ECS 账号、区域、共享数据库、对象存储、密钥托管和回退的其余实际参数，并在部署前复核磁盘余量。历史 `dify.diyuai.cc` 不作为当前项目依赖。
- M6-2：根据真实试点合同冻结规模、SLA、恢复目标和租户专属差异。

## 十、第一阶段边界与停止线

本路线图只覆盖第一阶段。ADR-009 公共素材、ADR-010 完整机器自评与数据飞轮、ERP/POS 连接器、多模态生成、真实多品牌运行及其他成长能力均在 M7 之后另行立项，不能成为当前阶段的隐含待办。

第一阶段以“折线之间”推动软件构建，以一个授权真实单品牌企业作为最低试点验收锚点；平台按预计 2—3 个、最多 5 个租户和合计 200 个已启用账号保持可用。合成夹具、自动检查、共享 ECS 上线和真实品牌分别证明不同层级，均不得互相冒充。M7 验收成立后立即停止扩展。
