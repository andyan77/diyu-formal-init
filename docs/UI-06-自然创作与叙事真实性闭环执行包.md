# UI-06 自然创作与叙事真实性闭环执行包

- 当前状态：`SUPERSEDED → UI-07`。此前 `BLOCKED` 事实、唯一候选、CI、部署、回退与清理
  证据完整保留；取代只表示旧的“模型 `ready` 直接授权创建任务”路径被前置
  `CreationIntentGate` 替代，不表示 UI-07 已成功。
- predecessor：`UI-05 SUPERSEDED`
- 启动提交：`4b27f2f17d5ff50621bf156831ca4dd8fc82e40f`
- 启动远端：`f0261e74421c5818afc337aefc42a5b2350dfdd9`
- 当前生产安全锚点：`845f63291ba5060e60f87d1afa5cfc1cdb057e3b`
- 唯一执行端：当前 WSL 执行端；同一时间只允许一个写入者。
- 完成状态：唯一生产候选已被真实 G1 否决并 forward revert；不得形成第二个生产语义候选。

## 1. 为什么存在

UI-05 第二个且最后一个结构候选在真实 DeepSeek 验收中命中停止线：G3 的自动检查放过了
“婆婆带孩子”等未提供家庭情境，G4 在发现越界后无法用单句修复重建整篇叙事，G7 因无合格
G4 V1 而不能成立。根因不是一句 Prompt，而是 Writer 同时自由创作、自报事实性质并参与给
自己作证；`non_event / hypothetical / user_presented_actual` 不能可靠区分一般观察、假设、
演绎和真人事实。

UI-06 只替代这条结构路径，不重做 UI-05 已关闭的租户管理、三入口、首页动效、安全认证、
发布身份、五轴、系列、素材、RLS 或部署能力。

## 2. 用户—系统责任

用户只需表达生产意图并给任意颗粒度的商品、生活片段、感悟、抱怨或题材；风格、形式和其他
创作控制均为可选。系统读取可信品牌、账号、平台和必要资料，自主选择主题、切口、观点、结构、
受众回报和平台组织，并负责事实边界、叙事模式、完整编译、自然修改与原子交付。

只有用户明确要求依赖一段真实经历、且缺少不可替代事实时，系统才问一个最小事实问题；不得
追问主题、观点、人物设定、情绪、结构或“想表达什么”。

## 3. 第 0 步：路径有界前向恢复

先把以下 17 条被否决责任合同路径以前向修改恢复为 `845f632…` 的运行内容：

1. `frontend/src/app/CreatorApp.tsx`
2. `frontend/src/app/types.ts`
3. `frontend/test/interaction.test.tsx`
4. `frontend/test/run.mjs`
5. `src/brain/content_control_service.py`
6. `src/brain/content_expression.py`
7. `src/brain/content_service.py`
8. `src/brain/natural_entry.py`
9. `src/gateway/api/app.py`
10. `src/shared/content_snapshot.py`
11. `src/shared/types.py`
12. `src/tool/llm_gateway/deepseek.py`
13. `src/tool/llm_gateway/stub.py`
14. `tests/test_deepseek_adapter.py`
15. `tests/test_m7_2a_content_control_surface.py`
16. `tests/test_p1_vertical_slice.py`
17. `tests/test_ui05_semantic_rework.py`

恢复不得扩大到第 18 个运行文件，不恢复权威文档，不触碰 Alembic、认证、配置和部署文件，
不 reset、rebase、squash 或区间 revert。恢复与 UI-05→UI-06 状态交接形成一个普通前向
提交；连同本地两条纯文档提交只做一次 push、一次恢复基线 CI，不部署。恢复后远端新 HEAD
与生产 `845f632…` 的运行树等价，但提交 SHA 不同。

## 4. 三个最小合同

### 4.1 NarrativeFrame

只写入既有任务快照 JSON，不新增表或迁移。V1 最小字段：

- `frame_version`
- `narrative_mode`
- 冻结用户事实原文及稳定来源 ID
- 允许使用的品牌／商品事实 ID

四种模式：

- `actuality_reflection`：用户给出生活流水账、抱怨、感悟或真人片段；
- `general_observation`：只给开放题材或没有事实承重的安全方向；
- `hypothesis`：明确条件推演；
- `dramatization`：显式选择故事／短剧形式或明确要求情境演绎。

模式依据只来自用户显式形式控制、intake 独立提取并保留的原文跨度和服务端保守默认；不得以
裸关键词表或句型黑名单路由。没有明确演绎要求时不得升级为剧情。

### 4.2 NarrativeBlock

继续复用现有 title、slot、正文、配文和媒体产品，只要求每个最终可见语义块带：

- `block_id`
- `block_type`
- `text`
- `source_refs`
- `scene_ids`

类型只保留 `actuality_source`、`general_observation`、`hypothesis`、`dramatization`。
`actuality_source` 由服务端从冻结原文插入，只做确定性空白、标点和格式整理，Writer 不得
改写、概括、扩展或润色。一般观察不得冒充亲历；假设必须保持可见条件语义；演绎只在显式
演绎模式出现，必须自然可见为演绎且不能绑定真实用户、员工、顾客、品牌案例或门店现场。
所有可见字段必须来自类型化块或可信事实编译，不允许未类型化正文旁路。

### 4.3 ReviewerObservation

Reviewer 可继续使用同一 DeepSeek，但必须是独立请求、独立输入，不复用 Writer 上下文，
也不读取或采信 Writer 自报的 `basis`、`actuality`、`source_refs`。它读取最终完整标题、
导读、正文、口播、字幕、配文、画面、动作、声音和制作提示，以及 NarrativeFrame、
block/scene ID、实际文本和允许事实／资源登记表。

每个 block 与 scene 都必须返回一份观察，至少包含真实存在的精确跨度、人物、关系、动作／
事件、对白、动机、前因结果、时间地点持有物、现实绑定、拍摄资源和不确定性。Reviewer 不
直接裁决 pass；服务端验证跨度、ID、完整覆盖、模式、事实和资源。未知 ID、缺跨度、稀疏
覆盖、格式漂移或语义不确定均失败关闭。

## 5. 内容与资源分离

用户现实原话只授权文字忠实承载，不登记其中的人物、家、店、厨房、物品、照片或声音为拍摄
资源。制作只可使用已登记创作者表达、已选素材、已确认商品资源或本次原创抽象构成。画面关联
NarrativeBlock，但不能反过来成为事实来源，也不得重演或伪装成现实现场。

## 6. 确定性对账与摘要

服务端而非 Reviewer 完成最终裁决：

1. 精确跨度必须存在于最终可见文本；
2. 每个 block／scene 恰好一份完整观察；
3. 真人事实块只能绑定冻结原文；
4. 一般观察不产生具体真人事件绑定；
5. 假设保持条件语义；
6. 演绎有可见提示且不绑定现实履历；
7. 拍摄资源全部在允许登记表；
8. 用户事实来源永不成为拍摄资源。

对被审完整可见对象计算稳定摘要，保存对象必须完全相同。最终审查后不得修改用户可见文本，
纯确定性序列化也必须在摘要之前完成。

## 7. 唯一一次块级修复

首次完整审查失败，只允许替换一个或多个完整失败 NarrativeBlock 及其全部关联画面、动作、
字幕和制作提示；`actuality_source` 不可修改，NarrativeFrame、模式、事实来源和核心用户要求
不可改变。随后重新编译整个成品、重新发起独立 Reviewer、重新执行完整对账与摘要校验。

仍失败则运行失败、版本增量 0、无永久 running；不得第二轮修复、随机重跑或添加
settlement／cascade／stabilize／bind／固定场景回填。

## 8. 自然修改

G7“别讲道理，荒诞一点。”只能改变 `general_observation` 或已有 `dramatization`。
NarrativeFrame、模式、`actuality_source` 原文字节与来源、允许事实集合、账号／品牌／平台和
ContentRole 保持不变。V2 必须实质改变相应表达块；V1 完整可读，前端异步版本读取不得清空
用户已经输入的下一条修改。

## 9. 开发验证与反证

定向测试覆盖快照版本与旧任务兼容、四模式边界、服务端真人事实插入、允许块类型、独立
Reviewer 输入、精确跨度、完整覆盖、人物／关系／动作／对白／动机／结果／资源对账、块级一次
修复、审后不可变、失败原子性、G7 不变量、RLS／历史版本／平台目标，以及 41/243/25/119
不变。

只做五项最小 mutation proof：

1. 去掉演绎提示必须失败；
2. 一般观察改成用户真人经历必须失败；
3. 用户现实来源加入拍摄资源必须失败；
4. G7 修改真人事实块或 NarrativeFrame 必须失败；
5. Reviewer 漏 block 或返回不存在跨度必须失败。

不建设通用 mutation 或评测平台。

## 10. 冻结验收

同一最终实现 SHA、同一生产部署不可拼接执行 G1—G7、H1、D1：

- G1 普通交流，任务／运行／版本差分 0/0/0；
- G2 ZX-C218 零追问完整 V1，只用已确认商品事实；
- G3 婆媳题材为 `general_observation`，无具体未提供生活情境；
- G4 流水账为 `actuality_reflection`，原句原样保留，不扩写、不重演；
- G5 无选题时系统自主选择安全主题；
- G6 真实创业经历缺事实时只问一个问题且 0/0/0；
- G7 从 G4 V1 形成 V2，Frame、模式和真人事实块不变，V1/V2/V1 可读且前端无竞态；
- H1 条件推演为 `hypothesis`；
- D1 明确演绎为 `dramatization`，可见为演绎且资源闭世界。

另做一次失败反证：一次块级修复仍失败时版本 0、无永久 running，输入和交流保留。真实模型
每卡只运行一次；只有网络、429 或超时可按既有供应商传输重试，语义失败不得重复调用求绿色。

## 11. 本地门、CI 与生产

稳定本地候选串行执行一次：

```text
git diff --check
make lint
make typecheck
make golden
make frontend-lint
make frontend-typecheck
make frontend-test
make frontend-build
```

完成产品与内容审查、工程与安全审查各一份后，只形成一个 UI-06 生产语义候选并推送。CI
绿色后核对同 SHA，创建并校验新鲜备份，执行隔离恢复，使用既有 deploy/compose 和受保护
DeepSeek 配置部署，完成人工逐字段全文审阅、失败原子性、临时材料清理、不 downgrade 数据库
的健康回退验证，再切回最终合格 SHA。实现 SHA、CI、镜像与运行 SHA 必须一致。

## 12. 结构停止线

若同 SHA 真实验收再次出现 G3 新增具体生活情境、G4 扩写真人事实、G4 把事实来源变成拍摄
资源、Reviewer 人工可见假绿，或 G7 改写 Frame／真人事实块，立即置 UI-06 `BLOCKED`：
不重跑、不形成第二个语义候选、不补 Prompt／词表／修复层；生产回安全版本，远端运行代码
普通 forward revert 回安全基线，并只提交一个更换审查模型／供应商或引入人工审核边界的裁决。

普通 lint、类型、构建、环境和 CI 机械问题不属于语义停止线。

## 13. 禁止范围与规模

不新增第二模型／供应商、DIFY、微服务、数据库表／迁移、持久聊天系统、队列、WebSocket、
监控平台、主张／证据图谱、大型 DSL、第二生成器、第二修复轮、随机重跑、关键词黑名单、
固定成稿／场景、P1/P2/P5 重构、租户管理重做、知识激活、20/55/44 目录或 39 项缺口清偿。

运行资产仍为 41，候选 243，目录 25/119，激活增量 0。UI-06 是运行结构增强，不是知识或
能力目录扩容。

## 14. 完成门

全部通过后 UI-05 保持 `SUPERSEDED → UI-06`，UI-06 进入 `REVIEW`。仍不证明真实员工采用、
真实品牌资料完整、真实发布、平台流量／排名／爆款、GMV／销售、企业采用、真实跨租户市场
差异、企业 SLA 或 20/55/44 全组合稳定支持。

唯一下一动作：主控独立终审 UI-06；若命中停止线，则只提交一个模型／人工审核边界裁决。

## 15. 实际执行结果（`BLOCKED`）

### 15.1 安全恢复、实现与确定性证据

- 17 条冻结路径的安全恢复与 UI-05→UI-06 原子状态交接提交为
  `dc86b9520001a91e6b3d37591baa068f8f8b05ea`。恢复守卫确认相对 `845f632…`
  没有第 18 条运行差异；它同 `31cec35…`、`4b27f2f…` 一次推送，恢复基线 CI
  `30325920857 success`，未部署。
- 唯一 UI-06 功能候选为
  `e77099f4c8b8d1c135cb4b78708663dcb8b4e403`。实现使用
  `narrative-frame-v1`（模式、逐字用户事实及来源 ID、允许品牌／商品事实 ID）、
  `actuality_source/general_observation/hypothesis/dramatization` 四类块、独立
  `ReviewerObservation` 和最多一次完整块级修复。Writer 不生成真人事实块；Reviewer
  不读取 Writer 的事实自报，服务端校验精确跨度、block/scene 完整覆盖、人物关系与事件、
  现实绑定、资源闭世界和审后摘要。
- `deepseek.py` 从安全基线约 1,905 行收为 1,565 行，适配器测试从约 1,314 行收为 788 行，
  独立叙事合同 589 行。五项 mutation proof（去演绎提示、一般观察冒充亲历、真人来源变
  拍摄资源、G7 改 Frame／事实块、Reviewer 漏 block／虚构跨度）全部能使测试失败。
- 唯一一次完整本地门全部绿色：`git diff --check`、ruff、mypy（84 个源文件）、
  Golden `155 passed` 与 OpenAPI、前端 lint/typecheck/interaction/build。开发期产品与
  内容审查、工程与安全审查各一份，无本地阻断；这些确定性／stub 证据没有被冒充为真实
  DeepSeek 成品证据。
- 候选只推送一次，承重 CI `30328125186 success`（quality job `90177639167`）。

### 15.2 备份、部署与真实 G1

- 部署前确认生产为 `845f632…`、schema `20260801_28`、readiness 正常。新鲜备份
  `/var/backups/diyu-m5-4/20260728T041650Z-ui06-e77099f4` 通过 checksum、清单、
  隔离数据库恢复、RLS、应用 readiness 与对象恢复；正式 `deploy.sh` 又生成
  `/var/backups/diyu-m5-4/20260728T041731Z-predeploy`。
- 同 SHA 候选镜像为
  `sha256:a70591063d0ead5971260e5fe70ee9e3afb4d1bef079508fbc9ffb52bf22066b`；
  schema 未变化，公网与回环均 `ready`。真实轮次使用一个 root-only 短会话、正式 API、
  PostgreSQL/FORCE RLS 和受保护 `deepseek-v4-flash`，没有输出凭据。
- 第一张 G1“今天有点不知道从哪儿开始。”即失败。系统没有自然回复，而是把它冻结为
  `general_observation`，创建任务
  `fa45a92e-d3e8-46f4-9b4b-e16b24df74bf` 与运行
  `c2924165-360b-4f65-b1ec-214b902a88af`；Writer 随后因“模型返回的类型化成品不完整”
  失败关闭。任务／运行／版本实际差分 `+1/+1/+0`，永久 running `0`，直接违反 G1
  `0/0/0`。
- 失败约 13 秒返回，生产模型超时为 45 秒；API 不是 429，运行模型记录为
  `deepseek-v4-flash`，所以不是可按传输规则重试的网络、429 或超时。G1 未重跑；G2—G7、
  H1、D1 没有开始，也没有真实成品可供人工逐篇审阅。不能把未执行写成通过，不能用新会话
  拼接一轮。
- 预设结构停止线列举的是 G3/G4/G7 真实性失败，但阻断性 G1 的“普通聊天不建任务”已经
  实际回退；本轮又只允许一个生产语义候选，因此该未列名失败同样不能成为第二候选的漏洞。
  产品与内容终审结论为 `BLOCK`；工程与安全终审确认失败原子关闭和零版本成立，但不能覆盖
  产品阻断。

### 15.3 清理、回退与远端安全树

- G1 失败任务、运行、对应活动事件与已撤销短会话
  `b7e91953-1d52-4df4-a2d7-7391d6477cbb` 已按精确 ID 清理。租户计数从
  任务／运行／版本 `285/358/275` 回到 `284/357/275`，短会话 `1→0`，永久 running
  保持 0；用户可见输入无 marker。
- 生产未 downgrade 数据库，已用 `rollback.sh` 回到 `845f632…`。当前 845 重建镜像为
  `sha256:1171b153cbc709a760caf4a5db1fb14fe00e0bca3ef9c7b79c85f737a3a6bdb9`，
  schema `20260801_28`，公网与回环均 `ready`。回退脚本重建标签后，回退前记录的旧
  `sha256:19ca263…` 不再留存在本机；这是实际偏差。被拒候选镜像仍保留。
- 两份本轮备份最初继承既有脚本的 `0644` 转储权限；发现后仅将这两条精确快照递归收紧为
  root-only `0700/0600` 并重新通过 checksum。没有在本里程碑修改部署脚本或历史快照。
- 被拒运行代码已由普通 forward revert 提交 `93f824f` 回到 `dc86b95…` 的安全运行树；
  实现、CI、失败与回退历史全部保留，没有 reset、rebase、squash 或删除。
- 资产激活增量为 0；运行资产 41、候选 243、目录 25/119 与既有缺口不变。真实员工采用、
  真实品牌资料完整、真实发布、流量／排名／爆款、GMV／销售、企业采用、真实跨租户市场
  差异、企业 SLA、20/55/44 全组合，以及 G2—G7/H1/D1 均未证明。

### 15.4 历史外部裁决（已被 UI-07 取代）

`UI06-D1`：**已被 UI-07 的前置 `CreationIntentGate` 裁决取代；历史原文保留。**
原文为：是否授权以一个经过有界结构化输出实测的新模型／供应商，**整体替换**当前
DeepSeek 在 UI-06 intake、Writer、Reviewer 的单一运行角色后再重开里程碑。不得形成并行
fallback、随机重跑或同模型 Prompt 候选；只增加人工成品审核无法修复 G1 在审核前已经误建
任务，因此不推荐作为本次单独补丁。

UI-06 当前终态为 `SUPERSEDED → UI-07`，不是 `REVIEW` 或成功关闭。生产在 UI-07 形成唯一
合格候选前继续保持 `845f632…`；不得继续 UI-06 代码施工，也不得把 UI-07 写成 UI-06 第二
候选。
