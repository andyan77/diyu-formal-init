# M7-3 第一阶段整体验收与停止执行包

- 状态真源：根目录 `MILESTONE.md`
- 当前里程碑：M7-3
- 当前状态：`ACTIVE`
- 启动日期：2026-07-26（UTC）
- 唯一执行端：当前 WSL 执行端
- 起始 Git：`1e4423519ec74e69d062f3eb6809c06901921590`
- 前置：M7-1、M7-2A、M7-2B 均已 `CLOSED`；M6-3 保持 `SUPERSEDED`

## 一、状态承接与可信起点

主控已独立完成 M7-2B 产品级终验并裁决通过。M7-2B 由 `REVIEW` 正式同步为 `CLOSED`；
关闭范围是正式生产路径下的连续内容、自然修改、历史版本、系列前情、多平台重编译及安全入口。
它不证明真实员工采用、真实商品采用、真实发布、平台流量、排名、爆款、GMV、销售、多个真实
租户的市场差异或企业 SLA。

2026-07-26 启动时完成只读核验：

- 本地、`origin/main` 与生产部署仓库均为
  `1e4423519ec74e69d062f3eb6809c06901921590`，两处工作树均干净；
- 生产运行/唯一 CI 标签为 `7f920d491d198b2f9a4ef7d9565f9c7af686d300`，镜像摘要为
  `sha256:64c0ec711e9daaa9d5d585578849ae64dc903f4afe0bb40de6034147d283b266`；
- schema 为 `20260730_24`；回环 readiness 返回 `ready`，公网 readiness 返回 `200`；
- GitHub Actions run `30194854609` 的工作流为 `deterministic-quality-gate`，对应
  `7f920d491d198b2f9a4ef7d9565f9c7af686d300`，结论 `success`；
- M7-2B 最终证据 `/var/lib/diyu-m5-4/m7-2b-review-final.json` 权限为 `0600`，记录的
  `final_state_sha` 为起始 Git，且 `m7_3_started=false`；
- 备份 `/var/backups/diyu-m5-4/20260726T081419Z-predeploy` 的 `SHA256SUMS` 全部通过，
  清单为 schema `20260730_24`、成功内容版本 `275`、完整链 `247`、对象 `1`。

这些锚点没有漂移。M7-3 的状态检查点只更新文档并使用 `[skip ci]`，不部署、不迁移、不创建
备份、不触发新 CI。

## 二、唯一业务结果

复用已经关闭里程碑的不可变证据，并只新鲜验证跨里程碑接缝，证明第一阶段同时具备：

1. 内容生产与 DM01 陈列参考建议两条正式业务路径；
2. 租户用户、租户管理员、笛语运维三个相互隔离的入口；
3. 桌面与移动端关键路径；
4. 账号五段画像、ContentRole、任务快照、系列前情和历史版本追溯；
5. 企业账号身份、本人默认表达人设、私人协作偏好与知识作用域隔离；
6. 同品牌总部与门店账号的实质差异，以及非机械的三平台重编译；
7. 未满足需求只形成候选且不自动回写正式对象；ADR-011 定义的发布后自愿反馈仍是 F08
   后续事项，不冒充本阶段已经交付；
8. 三条长期护城河的当前证据等级与未证明边界；
9. 风格 `20/20`、题材 `55/55`、体裁 `41/44` 及三个来源缺口的诚实对账；
10. 第一阶段以后事项全部具有明确触发条件和去向。

达标后只进入 `REVIEW`，等待主控独立终审；执行侧不自行关闭 M7-3，也不启动第一阶段以后的
任何施工。

## 三、复用证据与新鲜接缝

| 验收面 | 复用的关闭证据 | M7-3 只补的接缝 |
|---|---|---|
| 内容生产 | M7-2B 两个系列、六篇源成品、历史版本、六份平台成品、人话投影、AIGC | 页面、复制、导出读取同一当前投影 |
| DM01 | M7-1 正式 V1、确定性编译器、任务快照、参考建议语义、无 DeepSeek | 从正式租户作用域读取当前参考方案并核对与内容路径共存 |
| 三入口 | M5-4、M7-2A、M7-2B 的认证、资格与自然错误页 | 三个入口及跨入口会话的最小新鲜反证 |
| 桌面/移动 | M5-3/M7-2A 前端交互和生产工作壳 | `1440×900` 与 `390×844` 的关键路径复验 |
| 画像/版本/偏好 | M7-2A/M7-2B 的画像、快照、重放、RLS 与失败原子性 | 两个演示账号、旧任务、临时无偏好和错误作用域的接缝 |
| 平台与目录 | M7-2A 目录对账、M7-2B 三平台父链与人工审查 | 平台方向来源字段、稳定 ID、状态和去向无孤儿；历史版本只做诚实关联 |
| 反馈 | M7-2A 未满足需求分类与回告、无自动回写反证 | 正式受控入口到用户可读回告的最小闭环；发布后自愿反馈仍转 F08 |

不得重新生成或润色 M7-2B 六篇，不调用 DeepSeek，不重跑 M6-3 Q1—Q8，不调用已禁用的
系统级审查测试验证 Skill，不建设新的审查、反馈、评分或状态平台。

## 四、验收矩阵

### 4.1 两条业务路径

- 内容：读取既有演示索引、源/历史/平台版本；核对自然概要、完整正文或台词、制作说明、
  发布辅助和 AIGC 提醒在页面、复制与导出中的一致性。
- DM01：读取 M7-1 当前参考方案；核对无确认人、授权、审批、责任声明或现场回执要求，
  不自动执行陈列、不误标 AIGC、不调用 DeepSeek。

### 4.2 三入口与响应式

- 租户用户：`/login`；
- 租户管理员：`/tenant-admin/login`；
- 笛语运维：`/ops/login`。

匿名、错误入口资格与跨入口会话必须失败关闭且不显示裸技术信息。桌面使用 `1440×900`，
移动使用 `390×844`；移动只保留既有“对话 / 成品或方案”两段，创作方向默认收起且每轴可
不选，不新增设置页。

### 4.3 画像、版本、偏好与隔离

核对两个账号各自的五段画像 V1、ContentRole、系列与冻结前情；旧任务仍使用冻结画像和快照，
V1/V2 等历史可读，同目标修改只重放快照。临时无偏好会话不读写私人偏好；管理员、其他自然人、
其他账号、错误 tenant/brand/account/operator 均失败关闭；失败不产生半任务、半运行或半版本。

### 4.4 同品牌差异、表达广度与三平台

建立最小证据矩阵，覆盖总部/门店的观察位置、事实资格、受众关系、内容领地和系列方向；核对
至少三种题材或机制、三种表达风格、三种可执行形式与一次透明品牌转译。H3 抖音源、小红书 V2、
视频号 V2 的父链必须一致，三平台的标题、开头、结构和媒体组织必须实质不同。平台规则快照按
实际消费范围核对来源、适用范围、版本或无版本、生效/观察时间、最后核验时间、新鲜度、
替代关系与维护责任。

M7-3 只为后续新运行补齐平台方向来源合同，绝不倒写历史运行回执。H3 及其既有平台子版本的
证据等级固定为 `association_kind=legacy_version_reference`、
`full_provenance_frozen_at_generation=false`：只可由资源版本
`M5-2-platform-directions-v1`、精确目标与方向摘要，以及该资源首次引入的 Git 提交
`0ad19c2e9feadda5cfea13b3f17383c0bfba6a2e` 建立不可变关联。当前三个相关目标及摘要为：

| H3 载体 | 平台方向目标 | 方向摘要 |
|---|---|---|
| 抖音源 | `douyin_video` | `9e5b718ff31ef56ada3b2fc3219f62fa94cc8794996e3a1aadd8c56c04c92468` |
| 小红书 V2 | `xiaohongshu_graphic` | `16c0672a525f53fda167fe127fcbcf46af65420990abd312ead7f3348ff5ee81` |
| 视频号 V2 | `wechat_channels_video` | `37e99fef02034d3d8aaed00920af6323adda1774b86af517498bb69ad6a936ac` |

这组关联只证明历史成品使用的版本、目标和方向正文可追溯，不得改写成历史运行已经冻结
来源、适用范围、核验时间、新鲜度、替代关系或维护责任人。新运行才在既有任务输入回执中冻结
完整 provenance；不新增表，不迁移或重写旧任务。

### 4.5 能力目录与反馈

冻结风格 `20/20`、题材 `55/55`、体裁 `41/44`；三个来源缺口继续为
`CAT-SOURCE-GAP-GENRE-001/002/003` 并指向 F10。能力状态只按仓库真源机械计数：
`verified 10`、`composable 64`、`experimental 6`、`unsupported 38`、
`explicitly_out_of_scope 1`。

对账发现 9 条 `unsupported` 仍把已经关闭的 M7-2B 当作未来去向；本轮只修正去向，不改变
稳定 ID、状态、可见性或能力声明：

- `CAT-TOPIC-TEACH-07`、`CAT-TOPIC-NEWITEM-01`、`CAT-TOPIC-NEWITEM-06`；
- `CAT-TOPIC-TREND-01`、`CAT-TOPIC-STORE-02`、`CAT-TOPIC-STORE-05`；
- `CAT-GENRE-VLOG-01`、`CAT-GENRE-INTERACT-01`、`CAT-GENRE-INTERACT-06`。

它们按各自真实依赖改绑 F10，只有来源、时效、真实商品事实或直接消费者等触发条件成立后才
另立能力增强。三个体裁来源缺口仍为 F10，既不猜补标签，也不改变 `41/44`。

当前反馈证据只覆盖“未满足需求”：它经既有受控入口分类与回告，用户可以读自然语言结果，
目录、知识、方法、账号画像、私人偏好和系列前情均不得自动变化。ADR-011 描述的发布后自愿
采用、修改原因与表现反馈尚未实现，明确转 F08；M7-3 不以未满足需求入口替代它，也不把二者
合并成已经闭环的“自愿反馈”能力。

## 五、三条护城河的证据口径

1. **品牌一致性下的表达广度**
   - 当前已证明：同一品牌边界内，总部与门店可以完成多题材、多机制、多风格、多形式和一次
     透明品牌转译；
   - 部分证明：代表性组合表明目录可以参与真实生产，但覆盖面仍是有界样本；
   - 尚未证明：`20/55/44` 全组合稳定、三个缺失体裁标签及 119 项逐项生产验证。
2. **不同租户的实质差异化与反串味**
   - 当前已证明：同品牌总部/门店的观察位置、权威边界、受众关系和内容领地实质不同；RLS、
     tenant/brand/account/operator 错误作用域与合成反证失败关闭；
   - 部分证明：技术隔离与同品牌多身份差异表明系统具备反串味基础；
   - 尚未证明：没有第二个真实租户，因而真实跨租户的市场表达差异化未证明。
3. **自媒体内容市场竞争力与持续进化**
   - 当前已证明：事实边界、受众回报、完整成品、真人可说可拍、平台原生重编译、连续系列，
     以及未满足需求候选入口成立；
   - 部分证明：上述结果只构成内容竞争基础；候选回告不会自动学习或污染正式对象；
   - 尚未证明：ADR-011 的发布后自愿反馈仍待 F08；真实发布、流量、排名、爆款、GMV、销售
     与持续市场进化均未证明。

## 六、工程、生产与停止纪律

M7-3 默认不改代码。没有真实集成缺陷时，只运行风险相称的定向检查；复用当前 CI、镜像、
schema、备份与回退锚点，不重新部署、不创建备份、不触发 CI。若出现可复现集成缺陷，只修复
最小共享根因并按受影响范围验证，不能降低事实、隐私、RLS 或失败原子性。

普通探针、测试、网络、路径或配置失败不构成停工。只有继续必须改变产品语义、削弱安全/隔离，
需要未经授权的破坏性生产操作，或不能安全保留用户修改时，才按 `AGENTS.md` 停止并请求裁决。

## 七、当前执行记录

- 状态检查点：`8f82864` 已提交并推送 `[skip ci]`，没有部署或触发 CI；
- 跨里程碑接缝验收：定向后端 `92 passed`，正式路由 bootstrap、平台来源字段缺失/显式空值、
  目录精确计数与无孤儿反证通过；前端两套交互检查通过；
- 有界验收发现两个真实集成缺陷，均按共享根因处理，不重做六篇：
  1. 正式 React 内容与陈列路由虽然收到服务端作用域 bootstrap，挂载后却又用通用
     `/api/v1/session/context` 覆盖它，导致发布账号、ContentRole、平台载体和
     `synthetic_business_fixture` 标识可能丢失。修复以正式路由 bootstrap 为权威，仅当
     bootstrap 在开发/测试壳中未定义时才读取通用 fallback，并为正式 `/content`、`/display`
     明确 `formal_runtime`；
  2. `platform_directions.json` 只有版本、目标和方向正文，缺少来源、适用范围、观察/生效与
     最后核验时间、新鲜度、替代关系和维护责任。修复保持既有方向正文和
     `M5-2-platform-directions-v1` 不变，为新运行增加完整 provenance 并冻结到既有
     `input_receipt`，历史运行只采用 §4.4 的 legacy 关联。
- 目录对账同步把 9 条错误指向已关闭 M7-2B 的 `unsupported` 去向改绑 F10；能力状态和目录
  数量不变。反馈验收只证明未满足需求闭环，ADR-011 发布后自愿反馈保留 F08 去向。
- 当前代码/数据边界：需要代码与配置的最小共享修复；无迁移，尚未部署，未改生产数据；
- 本地最终完整门已通过：`git diff --check`、Ruff、mypy、Golden、后端 `159 passed`、
  前端 lint/typecheck/build/interaction 全绿；失败的首轮只在 mypy 阶段暴露新验收测试的
  嵌套 JSON 类型标注不足，补成显式 `TypedDict` 后从头完成了唯一成功候选门；
- 最终候选 SHA、唯一 CI、运行镜像与新鲜备份：待唯一候选完成后落定，不预填、不拼接旧证据；
- 当前剩余验收：唯一 CI、必要备份与同 SHA 部署、桌面/移动生产复验、生产不回退探针及
  权威文档无孤儿收口；
- 唯一下一动作：完成上述有界修复与验证，全部成立后将 M7-3 置为 `REVIEW`。

## 八、未证明项

真实员工采用、真实商品采用、真实门店现场执行、真实发布、平台流量与排名、爆款、GMV、销售、
企业采用、多个真实租户之间的市场差异、真实多品牌运行、企业 SLA、`20/55/44` 全组合稳定支持，
均不属于 M7-3 可宣称结果。
