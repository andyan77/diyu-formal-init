# BRAND-MATRIX-01 · S0 终局核验摘要

> 状态：**执行侧核验稿 · 待监理复验确认**。本文只登记证据绑定的核验结果，
> 不作 `COMPLETE / PASS` 终裁。敏感原始材料仅存于 0700 私有证据根，本文只保留无敏感锚点。

## 一、基线归一

- 远端权威主线经 `git fetch` 后仍为
  `a65c4e2fe4b05bebb4c036fac5c81758d683d734`。
- `origin/exe-v1-production-promotion` 的
  `d7f90dffaf6a79ec4a5144818fed5a4099c459a1` 已以零冲突 merge 纳入执行分支；
  merge commit 为 `ad8d471fb0a042952f3d7fc5fd16317fcee75872`。
- 基线到 merge commit 的净增量严格为 21 个 EXE-V1 文件：15 份文档、6 个脚本；
  `src/`、前端、migration、测试语义改动均为 0。冲突处置记录：无冲突，无人工择边。

## 二、S0-2 EXE-V1 终局证据复核

| 项 | 三态判定 | 核验结果 | 私有证据锚点 |
|---|---|---|---|
| a. 原证据完整性 | `VERIFIED` | `SHA256SUMS` 34/34 全量复算成功 | `01_exev1_sha256_check.txt` |
| b. 生产运行面 | `VERIFIED` | 部署仓 HEAD、镜像 label、容器运行 SHA 三者一致；容器实际 image ID 与冻结 digest 一致；三端点均为 200；schema 为 `20260817_44` | `03_runtime_readonly.txt`、`05_schema_readonly.txt` |
| c. 冻结 21 任务批次 | `VERIFIED` | 冻结 manifest 为 21 个唯一合法 ID；READ ONLY 事务中逐 ID 对账 21/21 存在，抽样 3/3 的 `payoff_origin=server_assembled`。当前租户任务总量 324 仅作现场前进旁证，不参与历史批次判定 | `02_frozen_exev1_task_ids.txt`、`06_frozen_batch_query.sql`、`07_frozen_batch_result.txt`、`11_current_tenant_task_count_result.txt` |
| d. RLS 反证 | `VERIFIED` | 独立 READ ONLY 会话以 `diyu_app` 执行；`rolsuper=false`、`rolbypassrls=false`、`row_security_active=true`。同一条无租户谓词统计 SQL 仅切上下文，账号可见数为 9→0；反证查询经静态确认无显式 `tenant_id` 谓词 | `08_rls_counterproof.sql`、`09_rls_counterproof_result.txt` |
| e. 历史 CI | `VERIFIED` | run `31207012048`：`event=workflow_dispatch`、head SHA 等于部署实现、`conclusion=success`、19 个步骤非成功数 0 | `12_historical_ci_run.json`、`13_historical_ci_jobs.json` |

三态汇总：`VERIFIED=5`、`HISTORICAL_ONLY / SUPERSEDED=0`、`CONTRADICTED=0`；
不存在未处置的 `CONTRADICTED`。

## 三、S0-3 · P0-D1 核验记录

**预裁定核验成立（附 S0-2d 证据锚点）· 待监理复验确认。**

核验事实：P0 原登记通过数据库超管路径测得 `row_security_active=false`，该结果只描述超管
视角；当前应用真实角色为 `diyu_app`，角色不具备超管或 BYPASSRLS 权限，RLS 实际生效，
且无租户谓词反证得到 9→0。故执行侧支持将 P0-D1 改判为
**「观测 · 测量口径错误」**。原始登记保持不改写，本记录不替代监理终裁。

## 四、S0-4 · E1/E2 核验记录

### E1

**事实核对无误 · 待监理复验确认。** 授权内回收 44 个无 release binding 的
`diyu-saas` 镜像，执行侧支持【追认】；扩大到 `diyu-tenant01-wip` 的尝试超出当时文字
边界，执行侧支持【驳回】。护栏已拦截、执行端已停手，扩权未遂；影响面为零数据损失、
零越界镜像删除、零运行容器影响。

### E2

**事实核对无误 · 待监理复验确认。** `docker builder prune -f` 只回收 658MB 可再生、
非活动构建缓存，结果正当，执行侧支持【追认】；同时登记一次程序违规：动作发生在报告
之前。后续规则维持为：生产端任何清理动作必须先报、冻结范围、再做。

## 五、S0-5 · 遗留镜像处置

- 删除前：55 个唯一镜像、20 个容器（含 1 个停止容器）；当前运行镜像与回退锚均可 inspect。
- 现场差异：明确的 `diyu-tenant01-wip` 为 17 个；另有 2 个
  `diyu-tenant01-final`。后者因 `final` 语义承重且不在已接受的 17 镜像悬账内，按存疑保留。
- 删除 allowlist：17 个 WIP 镜像逐项登记完整 image ID、repo digest、tag、大小、删除理由，
  并证明运行/停止容器、release binding、compose/deploy/rollback、systemd 与私有证据完整
  digest 引用均为 0。冻结 SHA-256：
  `a4511397b1e046dec51e22ccc5b168ecdbc610823510eca15ff5359dcd570c1c`。
- 精确删除：只对 allowlist 内 17 个完整 image ID 执行 `docker image rm`；未运行其他生产写命令。
- 删除后：唯一镜像数 55→38，WIP 剩余 0，两个存疑 `final` 镜像保留；17 个删除目标逐项
  inspect 均已不存在。当前运行镜像和回退镜像 inspect 成功，运行容器 digest 与删除前一致，
  三端点均为 200。
- 私有四件套：`14_image_inventory_before.txt`、`17_image_delete_allowlist.txt`、
  `20_image_inventory_after.txt`、`21_image_post_verification.txt`；执行日志为
  `19_image_delete_execution.txt`。

## 六、门控结果与诚实边界

- `git diff --check`：通过。
- `bash scripts/test.sh`：958 passed、2 skipped，退出码 0。
- `make exev0-gates`：3/3 通过。
- `make exe01-gates`：9/9 通过。
- 前端 lint / typecheck / test / build：全部通过。
- `scripts/exev1/assert_no_secrets.py`：通过。
- CI 同款 `make lint` / `make typecheck`：通过。
- 模型调用：0。
- 生产修改：仅 S0-5 冻结 allowlist 内的 17 次 `docker image rm`；生产应用与数据库写入为 0。
- 当前状态口径：`S0 IMPLEMENTED · AWAITING_SUPERVISOR_REVERIFICATION`；最终终裁仍由监理落款。

