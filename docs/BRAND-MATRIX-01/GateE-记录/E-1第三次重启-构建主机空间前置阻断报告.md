# BRAND-MATRIX-01 · Gate E E-1 第三次重启构建前置阻断报告

日期：2026-08-09

状态：**`GATE-E PRECONDITION_BLOCKED · BUILD_HOST_DISK_CAPACITY_INSUFFICIENT`**

监理状态：待监理复验确认

## 1. 基线与分支

- `EXPECTED_BASE_SHA`：`65995f3ba6ba99f79941395ab5042f671d2bc2e7`。
- `origin/claude/brand-knowledge-pilot-review-8a3105` 开工时精确等于该 SHA。
- 旧 `exe/brand-matrix-e` 已删除，本地与远端同名分支均从该 SHA 重建。
- 用户主工作树既有 `docs/项目记忆.md` 修改和 `var/media-staging/` 未跟踪素材保持未动。

## 2. 冻结前确定性证据

- Ruff：PASS。
- mypy：`178` 个文件，PASS。
- Golden：`1057 passed / 2 skipped`，PASS；另一次独立全量 `scripts/test.sh` 同为
  `1057 passed / 2 skipped`。
- EXE-V0：`3/3` PASS。
- EXE-01：`9/9` PASS。
- 前端 lint / typecheck / test / build：全部 PASS。
- Chrome 正式纵向：PASS，V2 preview/save/confirm/readback 请求计数 `2/1/1`。
- 两套秘密扫描：PASS；`git diff --check`：PASS。
- CI run：`31316900146`。
  - `event=workflow_dispatch`
  - `headSha=65995f3ba6ba99f79941395ab5042f671d2bc2e7`
  - `conclusion=success`
  - 非成功步骤=`0`

## 3. 构建主机前置硬门

正式 runbook 要求生产候选镜像开建前根分区可用空间不少于 `4,000,000,000` 字节。

- 构建前可用：`2,751,897,600` 字节。
- 候选镜像：不存在；候选 release binding：不存在；没有发生重复构建。
- 生产 checkout：干净；正式 build-once 脚本：存在。
- 只按 runbook 第一优先级清理了超过 24 小时、无活动构建依赖的 Docker builder cache，
  实际释放约 `428 MB`；未删除镜像、容器、卷、release binding 或业务数据。
- 清理后可用：约 `3,179,950,080` 字节，仍低于硬门。
- 逐一核对现存 `diyu-saas` 镜像共 `10` 个：当前镜像正在运行，其余镜像全部有
  `/opt/diyu-saas/releases/<sha>/image-binding.json` 回滚绑定；符合 runbook 删除条件的
  “未运行且无 binding”镜像为 `0`。
- 其余可回收项属于其他镜像仓库或 Docker 卷，超出 Gate E 已授权写面，未触碰。

因此没有降低空间门槛、没有冒险构建，也没有破坏任何回滚锚。

## 4. 状态边界

- 六对象冻结回执：**未生成**；候选：**未冻结**；生产镜像 digest：**不存在**。
- SEALED-SET-01：只核对密文 SHA256
  `332ac8f0bac7ecb5b7c3793cd8aea9b8ce5e5d8cf4a7c998b94984d138297a8a`；未开封、未解密、
  未猜题，继续有效。
- provider request：`0`；模型累计仍为 `59/300`。
- 未部署、未迁移、未导入、未上传母版、未切账号、未读取 `/etc/diyu` 配置值或本地 `.env`。
- 本轮生产侧变化仅为可再生 builder cache 清理；运行容器、数据库、镜像与业务数据均未改变。

## 5. 解除条件与唯一下一动作

需由主控选择并明确授权一种安全解除方式，使受控构建主机可用空间达到 runbook 硬门：

1. 扩容根分区；或
2. 明确批准一份经核对、未运行且无 release binding 的跨仓库历史镜像/无主卷精确删除清单；或
3. 指定另一台满足同等 build-once、digest 与镜像托管合同的受控构建主机。

解除后从生产镜像构建前置检查继续；在镜像 digest、六对象冻结回执和对应远端证据完整前，
不得开封盲测集。

## 6. 追加式解除记录

2026-08-09，用户明确授权删除精确匹配的 56 个未挂载匿名 Docker 卷。删除集 digest 为
`ebe0a7b7f58d0967654fe7fc4adc2d72560c92e7d329ff1c79984c262bb90cbe`；`56/56` 删除成功，
容器与 6 个在用卷前后不变，健康端点均为 200，可用空间增至约 `6.62GB`。

因此本报告登记的构建容量前置阻断已解除；Gate E 仍未冻结，后续状态以
《E-1构建空间清理记录-56匿名卷.md》及其后的追加记录为准。
