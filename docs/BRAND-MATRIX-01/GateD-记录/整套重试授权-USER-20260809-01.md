# BRAND-MATRIX-01 · Gate D 整套重试授权

- 授权 ID：`USER-20260809-01`
- 用户原话：`再次尝试`
- 执行解释：保留历史 42 次 provider 请求账本；不补跑失败单卡，不拼接旧成品；在新的唯一
  候选上重建隔离栈，从第 1 张卡完整运行八剧本与八异常。
- 冻结边界：产品代码、Writer Prompt、模型、temperature、max_retries、Gate A 数据合同、
  媒体合同均不因本授权改变；只升级验收器的累计账本基线和完整重跑登记。
- 模型参数：`deepseek-v4-flash`、temperature `0`、max_retries `0`。
- 安全边界：生产/SSH/ECS 0；密钥仅由既有精确三键解析器注入且不回显、不落盘。
