# 授权修订单·AMD-AUTH-20260809-01

- 裁决日期：2026-08-09
- 裁决来源：笛语品牌 founder
- 治理记录：主线 commit `b107ee02ac39acd93dfb259b9f3e73d3f7318792`
- 状态：`APPROVED_FOR_GATE_D_IMPORT`

## 裁决

> 笛语演示运营中任务素材授权取消单次限制，改为可重复授权。

## 应用范围

- `PS-S02-05`
- `PS-S04-03`
- 授权用途仍为 `organization_people`，账号、组织、来源文本摘要、生效期与安全边界不变。
- 新版授权为 `authorization_version=v2` 且 `single_use=false`。

## 版本与摘要纪律

- 原 `v1` 单次授权行和摘要原样保留，不更新、不删除（`append-only`）。
- 新 `v2` 授权使用新稳定 ID 和新摘要；正式 qualification 只指向 `v2`。
- 导入事件记录 `v1` 与 `v2` 的 ID、版本和 digest，作为追加式 supersede 链。
- Gate A 合同、素材草案及原句正文保持冻结不变。

## 单次授权能力保留

平台的单次授权机制不删除。Gate D 异常样本 7 改由两条 `DEMO-TEST`
合成授权承重；其测试引语不属于 `PS-S02-*` / `PS-S04-*` 人物原句库，不得进入正式 Writer 上下文。
