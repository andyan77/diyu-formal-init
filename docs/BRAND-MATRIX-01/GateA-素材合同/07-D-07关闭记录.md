# A-7 · D-07 正式关闭记录

- 状态：`CLOSED`
- 对象：柯桥店快照 `DIYU-STORE-KQ-WALL-01` / DM01。
- 裁决：柯桥店不属于 BRAND-MATRIX-01 十账号矩阵；柯桥资料不向杭州、湖州或成都门店复用；
  DM01 不进入本次演示导入。

因此：

1. `DIYU-STORE-KQ-WALL-01` 的 `consumption_items` 项为
   `internal_template_not_publishable`，P1—P5 消费方为空，Writer eligibility 为 false。
2. 三家演示门店挂杆档案继续为空；`SK-HZ-05`、`SK-HuZ-05`、`SK-CD-05` 只登记缺口，
   不把柯桥数值搬入。
3. 后续如要纳入柯桥，必须改变账号/组织范围并另行裁决；本记录不预授权该扩张。

旧 `素材草案-v0` 的开放记录保持原文，以本追加式关闭记录和 manifest `D-07-KQ` exclusion
作为后继裁决。
