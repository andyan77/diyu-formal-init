# M7-2A 执行包：内容表达目录、账号表达画像与用户控制面

- 状态：`REVIEW`（真源仍是根目录 `MILESTONE.md`）
- 上位裁决：[ADR-027](架构决策/ADR-027-内容表达空间目录与用户控制边界.md)、
  [ADR-005](架构决策/ADR-005-总部组织性岗位人设与操作人解耦.md)、
  [ADR-014](架构决策/ADR-014-内容生成运行语义与评测分层.md)、
  [ADR-015](架构决策/ADR-015-首期平台媒体成品与跨平台重编译.md)、
  [ADR-020](架构决策/ADR-020-多品牌共享隔离与经营关系.md)、
  [ADR-026](架构决策/ADR-026-内容前情复用与正式知识升级分离.md)
- 待落盘去向：`L02—L07` 由本里程碑消费；`F10`（119 项对账缺口）与 `F11`（磁盘清理）在本轮产出证据。

## 一、本里程碑交付什么

让用户在保持自然语言主入口的前提下，获得一个**最小内容控制面**：

1. 可选地控制本次的题材、讲法、风格、形式和连续方式；
2. 看见自己的原始选择在品牌边界下如何被透明转译；
3. 使用可版本化、可追溯的发布账号五段表达画像；
4. 自己控制私人创作偏好（查看／修改／关闭／恢复／删除／本次完全不读不写）；
5. 显式选择本次允许使用的文字素材或原媒体的人工说明；
6. 获得账号冷启动的内容机会与一份可修改的轻量计划；
7. 提交当前系统未满足的创作需求。

**不是**内容市场质量试点、不是 M6-3 七卡复验、不是真实连续系列、不是平台流量/SEO/趋势验证，
也不建设第二套生成器、人格系统、评测平台或审批流程。

## 二、五轴与目录真源

系统内部固定五轴：`topic` 题材、`mechanism` 讲法、`style` 风格、`form` 形式、`continuity` 连续。

目录真源是**版本化仓库数据**，不建数据库目录后台：

| 文件 | 职责 |
|---|---|
| `config/content_expression/catalog-v1.json` | 运行时精简 V1：只含有当前运行消费者的 `verified` / `composable` 项 |
| `config/content_expression/capability-inventory-v1.jsonl` | 研究全景、五轴映射、能力证据、依赖、缺口与去向 |
| `src/brain/content_expression.py` | 轻量 schema / loader；非法版本、重复 `stable_id`、未知状态一律失败关闭 |

能力状态只允许 `verified` / `composable` / `experimental` / `unsupported` / `explicitly_out_of_scope`。
全局目录语义、版本和状态只能由笛语运维通过 Git 版本更新；**不给租户管理员建设目录编辑接口或后台**。

### 目录来源对账（机械计数，见 §九-A）

ADR-027 声明目标为风格 `20`、题材 `55`、体裁 `44`，合计 `119` 个目标位置。本轮**只登记本执行
Prompt 给出的原始研究枚举**，不上网、不由模型补写、不用个人常识补足。逐项机械计数结果与缺口
处置见本文件 §九-A 与《待落盘推进事项清单》F10。

## 三、八个闭环与实现位置

| 闭环 | 实现位置 |
|---|---|
| A 版本化五轴目录 | `config/content_expression/*`、`src/brain/content_expression.py`、`GET /api/v1/content/expression-catalog` |
| B 发布账号五段人话画像 | `account_expression_profile_versions` 表、`content_accounts.control_organization_id` / `current_expression_profile_id`、`auth_grants.can_maintain_expression_profile` |
| C 私人创作偏好 | `user_creation_preferences`（租户＋本人双重 RLS）、`/api/v1/user/creation-preferences` |
| D 可选创作方向面板 | 前端 `创作方向（可选）`＋`CreateContentRequest.creative_direction` |
| E 透明品牌转译 | 目录声明的 `restrained_variant` ＋ 品牌克制标记；成品导读之前一句人话 |
| F 本次合法素材选择 | `material_assets.reference_note`、`CreateContentRequest.material_ids` |
| G 冷启动机会与轻量计划 | `POST /api/v1/content/opportunities`、`content_plans` 表、`GET/PUT /api/v1/content/plan` |
| H 未满足需求候选 | `unmet_capability_requests` 表与其两个端点 |

## 四、任务冻结与修订一致性

`business_tasks.content_context_snapshot`（expand-only、可空）冻结：目录版本、原始五轴选择、
应用后的方向、透明转译、`ContentRole`、账号画像版本、私人偏好模式与实际采用的偏好版本、
明确选择的素材引用与版本。**不复制私人偏好正文**。

V2 修订只读本任务冻结快照；账号画像、目录、私人偏好或素材后来变化不得改写旧任务。
历史任务由迁移从既有 `generation_runs.input_receipt` 无损回填 legacy 快照
（`profile_version=null`、`legacy_content_role=true`）；无法可靠回填的任务不读取当前设置，
只给一句自然提示，旧成品继续可读。

## 五、权限与隔离硬边界

- 账号使用资格 ≠ 画像维护资格；
- 租户管理员只能维护**其自身组织控制**的发布账号画像；
- 区域/门店维护人必须同时满足：属账号控制组织、有该账号有效使用资格、
  `auth_grants.can_maintain_expression_profile = true`；
- 客户端传入的 account / profile / organization ID 一律不可信；
- 私人偏好在数据库层用 `app.tenant_id` ＋ `app.user_id` 双重限制，同租户管理员也读不到；
- 现存账号的控制组织只能在**唯一证据**成立时回填，存在歧义时保持 `NULL`，不按账号名、
  角色名或操作者姓名猜测。

## 六、验证纪律

只使用仓库自身可信入口：`make lint`、`make typecheck`、`make golden`、`make test`、
前端 lint / typecheck / build、OpenAPI Golden、定向 PostgreSQL RLS 反证、必要的桌面/移动冒烟。
最多两份有界人工审查（产品语义/UI；权限/迁移/隔离）。禁止调用已确认存在假绿与真源错配的
系统级审查测试验证 Skill。

真实模型只做**一次**有界 DeepSeek 内容冒烟；不重跑 M6-3 Q1—Q8、不要求随机首稿 7/7、
不因软风格措辞不理想拒绝整份成品、不反复形成生产 SHA、不换模型、不加重试层。

## 七、明确不做

不建设 `119 × P1—P5 × 平台` 组合矩阵、不把 119 项铺到前端、不建独立设置页、不建人格 Agent、
不建第二模型/第二编译器/主张图谱/AI 味检测器、不做自动发布、不做图片或视频理解与生成、
不批量激活 243 条候选资产、不接 ERP/POS/核销、不新增审批或逐任务授权流程。

---

## 八、施工与部署记录

- 唯一实现提交：`c0f28732fe85e35ff28e35911d09144fb97b94a5`；唯一远程 CI run `30159843299` 绿色。
- 部署前 F11 有界清理（保护清单＝当前运行 SHA＋指定回退目标＋`MILESTONE.md` 明确保留的
  `3828073` / `58a52f1` / `3bcae817`）：删除 102 个历史 `diyu-saas` 镜像并回收未使用构建缓存，
  镜像标签 `107 → 5`，根分区 `89%`（剩 `4.2G`）→ `76%`（剩 `9.2G`）；19 个容器全部照常运行，
  历史 DIFY 与其他应用未触碰，备份目录仍为 `4.1M`。
- 部署：`deploy.sh c0f2873…` 自动完成部署前备份 → expand-only 迁移 `20260725_19 → 20260726_20`
  （生产 migrator 既非 superuser 也无 `BYPASSRLS`，两处回填均逐租户 `set_config('app.tenant_id')`）
  → 构建并启动同一 SHA 镜像。运行镜像 `diyu-saas:c0f2873…`，schema `20260726_20`。
- 有界回退检查：`rollback.sh 1241cbd…` 后本机/公网 readiness 均 `200`、公网首页 `303`、
  匿名 API `401`，**schema 保持 `20260726_20`，未执行任何 downgrade、未重跑旧迁移**；
  用回退版本自身代码路径读取生产已有内容成功（`version=1`、`1459` 字符），该版本不认识新增
  字段但不受影响；最终版本读同一条内容结果完全一致并额外带出透明转译。随后 `deploy.sh` 切回
  `c0f2873…`，readiness `200/200`、首页 `303`，内容 `271/271/185`、陈列 `1/1/1` 计数不变。

### 两份有界审查与处置

| 审查 | 结论 | 已修复的实质问题 |
|---|---|---|
| 产品语义 / UI | `FAIL` → 已修复后复验绿色 | ①修订收到自然提示时前端按版本渲染会崩溃 → 加 `kind` 守卫；②「以后优先这样帮我」会清空本人已写的偏好说明 → 改为只写方向默认、保留原说明并如实提示；③已保存的偏好默认值静默生效而摘要显示「不指定」 → 折叠摘要标出「你保存的默认」、逐轴提示、成品显示「这一版实际按…」，并改掉与实际不符的文案；④重开历史版本丢失转译提示 → 版本读取路径回传 `translation_notice`；⑤离线 stub 把画像版本号写进正文 → 移除，改由运行回执承载；⑥「换一批」实际不换 → 改为「重新读一遍」；⑦连续轴问句与唯一选项不符 → 改写问句；⑧移动端侧栏整体隐藏导致四个次级工作面不可达 → 改为可横向滚动的胶囊导航，仍只有两个底部标签；⑨⑩体型开关与「不写入」文案 → 如实改写；另补 `aria-current` 与非颜色选中标记 |
| 权限 / 迁移 / 隔离 | `PASS_WITH_NOTES` | ①`control_organization_id` 此前没有任何生产写入面，新账号将永远为 `NULL`、画像功能不可达 → 建账号时由创建管理员显式指定（默认其自身组织）并校验组织属本租户；②`can_maintain_expression_profile` 无授予面 → 新增 `grants_expression_profile_maintenance`；③私人偏好派生的 `body_related_opt_in` 会落进租户作用域快照 → 快照只保留任务条件，不再写该字段；④冻结素材版本从未被强制 → 重放时逐条比对，版本变了即失败关闭；⑤冻结画像读不到时静默降级 → 改为与素材一致的失败关闭；⑥`account_expression_by_id` 缺 `account_id` 谓词 → 补上；⑦未满足需求快照可记录未启用的体型项 → 过滤 |

审查同时确认：账号使用资格不授予画像维护资格、总部管理员不能维护其他控制组织的账号、
私人偏好在无 `app.user_id` 时对同租户管理员完全不可见、素材越权引用整批失败关闭、
修订只重放本任务冻结条件、四张新表全部 `ENABLE + FORCE` RLS 并逐租户回填、迁移 expand-only、
机会/计划/未满足需求不创建任何业务任务。

## 九、验收结果

### A 目录

- 稳定 ID 唯一、能力状态合法、版本可追溯（`content-expression-catalog-v1`）；
- **诚实对账**：风格 `20/20`、题材 `55/55`、体裁 `41/44`，合计有定义 `116`，目标 `119`，
  缺 `3`；缺口登记为 `CAT-SOURCE-GAP-GENRE-001/002/003`，`gap_type=source_gap`，去向 F10，
  不进入前端。**不得**宣称 119 项全部登记或全部支持；
- 精简 V1 共 `25` 条，全部为 `verified/composable`；默认可见 `21` 条，`4` 条体型相关项
  只有本人主动启用后才出现；
- React 不硬编码任何目录条目（源码中出现的 `stable_id` 数为 `0`）；
- OpenAPI 中与目录相关的路径只有 `GET /api/v1/content/expression-catalog`，租户管理员没有
  任何全局目录写入面。

### B 画像

五段完整校验、保存形成 V1、再次保存形成 V2、V1 不可变且仍可读、当前指针指向 V2、
旧任务仍引用其冻结版本；普通操作者只读；同组织有维护资格者可写；持有维护标志但组织不匹配的
外部代运营仍失败关闭；总部管理员不能维护门店控制组织的账号；跨账号/不存在账号 `422`；
新建发布账号会记录控制组织并可立即维护。

### C 偏好

本人可查看/修改/关闭/恢复/删除；同租户管理员与其他自然人在数据库层读不到（`count=0`）；
未设置 `app.user_id` 的事务同样读不到；「这次不读取」真实不读（快照记 `temporarily_bypassed`）；
关闭记 `disabled`；删除后系列、素材、账号画像计数不变；普通生成前后偏好仍为 `exists=false`。

### D/E 方向与转译

零选择时既有自然输入路径无回归；选择项进入实际 `GenerationInput` 与成品；原始选择、应用结果、
目录版本同时进入任务快照与运行回执；软冲突时同时保留 `幽默玩梗` 与 `克制的冷幽默` 并给出一句
自然转译（正文中不出现原选择、不进入正文）；重开历史版本仍带同一句转译；目录版本过期 `422`；
体型项未启用 `422`、启用后可用。

### F 素材

未勾选素材不进入上下文；本人文字素材可进入；缺人工说明的图片原件 `422`；他人素材 `422`；
运行回执与任务快照记录 `asset_id + reference_version`；重放时版本不一致即失败关闭。

### G 机会、计划与缺口

前端不再有 `ZX-C218` 硬编码；机会来自服务端真实上下文且确定性可重复；浏览机会、读写计划、
提交未满足需求前后 `业务任务/运行/版本` 计数完全不变，目录两个文件字节不变，
系统激活资产恒 `41`，账号画像版本数不变；未满足需求只创建候选（`gap_type=unclassified`、
`status=received`），且只能读到本人提交的记录。

### H 快照与前端

V2 重放冻结画像、方向、偏好模式与素材版本；后来的新画像版本不污染旧任务；
空快照任务修订只返回一句自然提示且任务/运行/版本计数不变、V1 仍可读；
历史任务全部由迁移无损回填 legacy 快照（生产 `271/271`，`0` 条为空、`0` 条冒充画像版本）；
可见成品中不出现 `CAT-*`、`OPP-*`、目录版本串、任务或版本 ID。

### 本地与生产验证

- 本地：`make lint`、`make typecheck`、`make golden`、`make test` 全绿（`119 passed`），
  前端 lint / typecheck / build 全绿，OpenAPI 由实际路由重新生成并纳入 Golden；
- 生产：运行镜像 `diyu-saas:c0f2873…`、schema `20260726_20`、本机与公网 readiness `200`、
  公网首页 `303`；六个新端点匿名访问全部 `401`；四张新表 `ENABLE + FORCE` RLS 均为 `true`；
  五个新列全部存在；控制组织回填命中真实品牌账号（`笛语服饰品牌官方账号 → 笛语服饰管理组织`）；
  M7-1 陈列侧计数 `1/1/1` 且无空快照；系统激活资产 `41`。

### 未证明项（如实记录）

1. **有界 DeepSeek 真实内容冒烟未执行**。它需要一个正式登录会话；真实品牌管理员口令属
   founder 本人，按协作基线不由执行侧代持，而为合成演示租户临时开通一次性登录的动作被执行
   环境的权限闸拦下。执行侧未绕过该拦截，也未创建任何生产登录身份：`ops-run` 目录为空、
   `m7-2a*` 用户名的凭据数为 `0`、有效激活令牌为 `0`。因此**真实模型路径在生产上未验证**，
   控制面本身只由本地确定性测试与生产只读探针证明。
2. 真实笛语服饰账号的五段画像**仍未由有权主体保存**（按 ADR-027 属 M7-2B 启动前条件）；
   执行侧未代用户激活，本轮完全未写入该租户。
3. 现存且无创建事件的发布账号其 `control_organization_id` 仍为 `NULL`，本轮不提供修复接口
   （不按名称猜测）；生产实际不受影响，真实品牌账号已由唯一证据回填。
4. 内容质量、平台流量、真实发布、企业采用与销售结果均未证明，本里程碑也不声称证明。
