# M7-2A 执行包：内容表达目录、账号表达画像与用户控制面

- 状态：`ACTIVE`（真源仍是根目录 `MILESTONE.md`）
- 上位裁决：[ADR-027](架构决策/ADR-027-内容表达空间目录与用户控制边界.md)、
  [ADR-005](架构决策/ADR-005-总部组织性岗位人设与操作人解耦.md)、
  [ADR-014](架构决策/ADR-014-内容生成运行语义与评测分层.md)、
  [ADR-015](架构决策/ADR-015-首期平台媒体成品与跨平台重编译.md)、
  [ADR-020](架构决策/ADR-020-多品牌共享隔离与经营关系.md)、
  [ADR-026](架构决策/ADR-026-内容前情复用与正式知识升级分离.md)
- 待落盘去向：`L02—L07` 由本里程碑消费；`F10`（119 项对账缺口）与 `F11`（磁盘清理）在本轮产出证据。

## 一、本里程碑交付什么

让用户在保持自然语言主入口的前提下，获得一个**最小内容控制面**：

1. 可选地控制本次的题材、讲法、风格、形式和连续方式；
2. 看见自己的原始选择在品牌边界下如何被透明转译；
3. 使用可版本化、可追溯的发布账号五段表达画像；
4. 自己控制私人创作偏好（查看／修改／关闭／恢复／删除／本次完全不读不写）；
5. 显式选择本次允许使用的文字素材或原媒体的人工说明；
6. 获得账号冷启动的内容机会与一份可修改的轻量计划；
7. 提交当前系统未满足的创作需求。

**不是**内容市场质量试点、不是 M6-3 七卡复验、不是真实连续系列、不是平台流量/SEO/趋势验证，
也不建设第二套生成器、人格系统、评测平台或审批流程。

## 二、五轴与目录真源

系统内部固定五轴：`topic` 题材、`mechanism` 讲法、`style` 风格、`form` 形式、`continuity` 连续。

目录真源是**版本化仓库数据**，不建数据库目录后台：

| 文件 | 职责 |
|---|---|
| `config/content_expression/catalog-v1.json` | 运行时精简 V1：只含有当前运行消费者的 `verified` / `composable` 项 |
| `config/content_expression/capability-inventory-v1.jsonl` | 研究全景、五轴映射、能力证据、依赖、缺口与去向 |
| `src/brain/content_expression.py` | 轻量 schema / loader；非法版本、重复 `stable_id`、未知状态一律失败关闭 |

能力状态只允许 `verified` / `composable` / `experimental` / `unsupported` / `explicitly_out_of_scope`。
全局目录语义、版本和状态只能由笛语运维通过 Git 版本更新；**不给租户管理员建设目录编辑接口或后台**。

### 目录来源对账（机械计数，见 §九-A）

ADR-027 声明目标为风格 `20`、题材 `55`、体裁 `44`，合计 `119` 个目标位置。本轮**只登记本执行
Prompt 给出的原始研究枚举**，不上网、不由模型补写、不用个人常识补足。逐项机械计数结果与缺口
处置见本文件 §九-A 与《待落盘推进事项清单》F10。

## 三、八个闭环与实现位置

| 闭环 | 实现位置 |
|---|---|
| A 版本化五轴目录 | `config/content_expression/*`、`src/brain/content_expression.py`、`GET /api/v1/content/expression-catalog` |
| B 发布账号五段人话画像 | `account_expression_profile_versions` 表、`content_accounts.control_organization_id` / `current_expression_profile_id`、`auth_grants.can_maintain_expression_profile` |
| C 私人创作偏好 | `user_creation_preferences`（租户＋本人双重 RLS）、`/api/v1/user/creation-preferences` |
| D 可选创作方向面板 | 前端 `创作方向（可选）`＋`CreateContentRequest.creative_direction` |
| E 透明品牌转译 | 目录声明的 `restrained_variant` ＋ 品牌克制标记；成品导读之前一句人话 |
| F 本次合法素材选择 | `material_assets.reference_note`、`CreateContentRequest.material_ids` |
| G 冷启动机会与轻量计划 | `POST /api/v1/content/opportunities`、`content_plans` 表、`GET/PUT /api/v1/content/plan` |
| H 未满足需求候选 | `unmet_capability_requests` 表与其两个端点 |

## 四、任务冻结与修订一致性

`business_tasks.content_context_snapshot`（expand-only、可空）冻结：目录版本、原始五轴选择、
应用后的方向、透明转译、`ContentRole`、账号画像版本、私人偏好模式与实际采用的偏好版本、
明确选择的素材引用与版本。**不复制私人偏好正文**。

V2 修订只读本任务冻结快照；账号画像、目录、私人偏好或素材后来变化不得改写旧任务。
历史任务由迁移从既有 `generation_runs.input_receipt` 无损回填 legacy 快照
（`profile_version=null`、`legacy_content_role=true`）；无法可靠回填的任务不读取当前设置，
只给一句自然提示，旧成品继续可读。

## 五、权限与隔离硬边界

- 账号使用资格 ≠ 画像维护资格；
- 租户管理员只能维护**其自身组织控制**的发布账号画像；
- 区域/门店维护人必须同时满足：属账号控制组织、有该账号有效使用资格、
  `auth_grants.can_maintain_expression_profile = true`；
- 客户端传入的 account / profile / organization ID 一律不可信；
- 私人偏好在数据库层用 `app.tenant_id` ＋ `app.user_id` 双重限制，同租户管理员也读不到；
- 现存账号的控制组织只能在**唯一证据**成立时回填，存在歧义时保持 `NULL`，不按账号名、
  角色名或操作者姓名猜测。

## 六、验证纪律

只使用仓库自身可信入口：`make lint`、`make typecheck`、`make golden`、`make test`、
前端 lint / typecheck / build、OpenAPI Golden、定向 PostgreSQL RLS 反证、必要的桌面/移动冒烟。
最多两份有界人工审查（产品语义/UI；权限/迁移/隔离）。禁止调用已确认存在假绿与真源错配的
系统级审查测试验证 Skill。

真实模型只做**一次**有界 DeepSeek 内容冒烟；不重跑 M6-3 Q1—Q8、不要求随机首稿 7/7、
不因软风格措辞不理想拒绝整份成品、不反复形成生产 SHA、不换模型、不加重试层。

## 七、明确不做

不建设 `119 × P1—P5 × 平台` 组合矩阵、不把 119 项铺到前端、不建独立设置页、不建人格 Agent、
不建第二模型/第二编译器/主张图谱/AI 味检测器、不做自动发布、不做图片或视频理解与生成、
不批量激活 243 条候选资产、不接 ERP/POS/核销、不新增审批或逐任务授权流程。

---

## 八、施工与部署记录

（施工过程中持续更新。）

## 九、验收结果

（施工过程中持续更新。）
