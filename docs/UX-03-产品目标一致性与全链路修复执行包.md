# UX-03 · 产品目标一致性与全链路修复执行包

## 当前状态

- 里程碑：`UX-03`
- 总状态：`ACTIVE`
- Gate A：`COMPLETE`，主控最终终审 `PASS`
- Gate B：`COMPLETE`，主控最终终审 `PASS`
- Gate C：`COMPLETE`，主控最终终审 `PASS`；FT-039、FT-040、FT-041、FT-045 均已通过
- Gate D：`ACTIVE`；本轮关闭 FT-050—FT-052 与 FT-058
- 唯一写入执行端：当前 WSL Codex
- 启动基线：`86f5df16b9e42fe4eee322694156361a007bdb88`
- 远程与生产边界：Gate A、Gate B、Gate C 只做本地实现、测试和普通前向提交；不 push、不触发远程 CI、
  不部署、不连接生产。Gate A、Gate B 未调用模型；Gate C 只按明确授权在本地 synthetic
  PostgreSQL、正式 API／React 消费者和受保护 DeepSeek 配置上执行必要的最终套件。

UX-02 保持 `CLOSED`。本执行包是一个结果型总里程碑，不为内部 Gate 创建新的 UX 编号、
successor、平行台账或子状态机。

当前 Gate C 运行实现为 `ec3dbe45d485816c7a49f9c801cd2baa2fe58c04`；其后仅有测试
oracle 提交 `6e3cb46b6c18d82f245a92f36c972ab65fa62bde`，没有运行代码变化。此前
`faa4792538cbbcb54ff7b329dee7d03dd9488a24`、`ee1e2a4e56b2bd9303e44d83a3b001ab084230a5`
及更早正式商品媒体消费者候选、审计退回和失败证据均完整保留为前向历史。

## Gate A 的结果

Gate A 一次关闭正式入驻、身份、逻辑发布账号与多平台关系：

```text
登录身份（真实自然人）
  → 独立工作空间与能力资格
  → 一个或多个逻辑发布账号
  → 每个账号的一套 ContentRole、控制组织与五段画像
  → 账号下显式登记的合法平台与媒体形式
```

切换平台或形式不得切换自然人、逻辑发布账号、控制组织或五段画像。租户管理员资格、账号
使用资格、五段画像维护资格和账号控制组织必须相互独立。

## Gate A 直接范围

1. 修复发布账号和平台创建的正式 React/API 严格合同，不接受未知 `target` 字段，不通过
   中文标签替换推导平台。
2. 为全新租户提供品牌表达草案的读取、修订、确认和当前版本回读。
3. 为新逻辑发布账号提供确定性、可编辑、尚未生效的五段画像候选，确认保存后形成 V1。
4. 管理员显式选择账号使用者，并独立决定其五段画像维护资格。
5. 补齐成员、逻辑发布账号和平台载体的最小启用／停用／恢复与必要修改能力，历史引用不删改。
6. 修正 `/user`、`/content` 对自然人、所属组织、账号控制组织、ContentRole、画像版本和
   平台目标的正式投影。
7. 以一个全新 synthetic tenant 完成正式 React/API/PostgreSQL 纵向，随后精确清理。

## Gate A 冻结边界

- 不修改 Writer、DeliveryCompiler、P1—P5、DM01、内容生成、品牌知识检索、资产目录、
  团队用量或 `/status` 模型状态。
- 不调用 DeepSeek、千问或 OpenAI。
- 不连接或修改生产，不创建真实自然人、品牌资料或生产会话。
- 继续严格拒绝未知 API 字段；只有现有结构无法承载时才允许 expand-only 迁移。
- 普通 DTO、前端、浏览器、PostgreSQL 和测试错误在 Gate A 内前向修复，不升级为
  `BLOCKED` 或 successor。

## Gate A 完成门

- 全新租户可完成正式基线确认、两级组织、两名内容用户、两个逻辑发布账号、五段画像 V1、
  多平台目标、激活登录、身份投影和最小生命周期。
- 微信视频号映射正确；小红书图文／视频共用同一逻辑账号画像。
- 使用资格与画像维护资格独立，修改一类资格不覆盖另一类资格。
- 正式 API 契约、React 请求体、PostgreSQL 纵向和三个视口的浏览器旅程均能捕获旧错误。
- Golden、Ruff、mypy、OpenAPI、前端 lint/typecheck/interaction/build 全绿。
- 两份有界审查无阻断；功能真值按正式消费者重新计算。
- 本地形成冻结 Gate A 实现提交，工作树干净；不 push、不 CI、不部署。

Gate A 首次完成时只更新为：

- UX-03：`ACTIVE`
- Gate A：`COMPLETE`
- Gate B：`NOT_STARTED`

Gate A 已经主控最终终审 `PASS`；Gate B 首次候选经主控终审退回后已完成一次有界返工，
并完成 FT-031 最终极小返工，随后由主控最终终审 `PASS`。Gate C 已按用户独立裁决进入
`ACTIVE`，只承接 FT-039、FT-040、FT-041 和 FT-045。

Gate C 首次候选实现 `4d797d35614216c92a7e62ae7a62d7456165d70b` 的工程证据和同 SHA
七卡 raw 均作为历史保留。主控独立终审确认 FT-039、FT-040、FT-045 `PASS`，但 FT-041
因 v3 媒体单元仍由固定 Compiler 模板生成、P2 扩写未冻结商品语义、P3 缺少自然可识别的
账号关联，以及人工全文审阅摘要无法按文件 SHA／正式 visible digest 复算而 `FAIL`。
Gate C 因此原地恢复 `ACTIVE / REWORK`，临时功能真值为 `53/5/0/6/0`；不启动后续 Gate。

本次返工以普通前向实现先后完成 v3 媒体单元 Writer 所有、P2 冻结事实选择、P3 去标识账号
关联输入和 file SHA／正式 `visible_digest` 证据工具。实现
`715838f0a093f12e1be77b4d76581f5db8814e6c` 首次隔离套件在 P3 检出 Writer 结构包装；
共享结构说明修复 `c18fcf5d0393c7bb7a4bffbd1a8a92f335703834` 仍在 P3 检出相同责任违例；
将输出样板改为空内容槽并明确 `content_only`／Compiler wrapper 所有的最终重试实现
`ad398e60ea4b7544211190b99788c19ce7e293dc` 已通过完整本地门，但唯一同 SHA 七卡套件在
P1 后于 P2 `unit:media-opening` 再次失败关闭。三次 raw 与失败历史均保留，不随机重跑、
不拼接不同 SHA；最终重试 root-only 证据为
`/var/lib/diyu-ux03-evidence/ad398e60ea4b7544211190b99788c19ce7e293dc/gate-c-final-suite/`。

主控随后批准一个且仅一个协议规范化：图文 `media_opening` Writer 单元可在原始文本起始
位置精确去除一次规范字面 `首图：`，随后对完整剩余文字重新执行既有保留 heading、范围、
bidi 和 Default Ignorable 安全检查。实现
`1f0dffcea106bff814925051ac3671bcb654f409` 以服务端既有
`unit purpose + media shape → Compiler heading` 映射为唯一真源；不做 NFKC 后匹配、
零宽清洗、正则别名、循环去除或其他 unit 的宽松兼容。首次生成和受影响单元修复复用同一
解析函数。

保存的 P2 raw 无模型回放确认失败原文确为图文 `unit:media-opening`、以 U+9996 U+56FE
U+FF1A 的规范字面开头且去除后非空；raw text SHA-256 为
`18a19a2481eff0a6da3eaf274f4415b55e722e69d0f037c56a7da58fdf5ac73c`，规范化后 text
SHA-256 为 `0bb064cac13be8cab11959d3a13c018a59371b83201d46486a7564152a76d89b`，合同版本为
`writer-wrapper-normalization-v1`。精确／模糊匹配、安全复扫和单次去除三类 mutation
均能捕获错误实现。

该实现通过 Ruff、mypy、Golden/OpenAPI `467 passed, 2 skipped`、前端四门和显式 Gate C
Chrome；新的同 SHA P1—P5、series2、series3 各调用一次且均生成成品。人工阅读全文没有
通过：仅 P5 同时满足主要价值、账号关联、平台完整性、事实／资源边界和制作可执行性；P1、
P3、P4、series2、series3 的媒体单元引入未登记衣物、杯子、场地或道具，P2 把冻结的
“两面完整外观”改写为正反面并增加未冻结的衣领／袖口／织物细节，多张图文还重复低饱和
静物与居中排字结构。工程协议缺口已经关闭，但它不能证明自由媒体文案没有新增现实资源或
商品语义。继续需要改变“无登记媒体资源时的表达方式”或资源／媒体责任合同，命中本 Gate
停止线；不得继续随机重跑或补失败句 Prompt。FT-041 保持 `有缺陷`，Gate C 保持
`ACTIVE / REWORK`。root-only 证据目录
`/var/lib/diyu-ux03-evidence/1f0dffcea106bff814925051ac3671bcb654f409/gate-c-final-suite/`
包含 7 份 raw、7 份 artifact、套件配置、无模型 normalization replay、逐卡人工失败审阅、
两份有界审查、失败摘要和 `SHA256SUMS`；20 个文件已完成 checksum 复算，未写成功
manifest。

## Gate C FT-041 媒体责任最终收敛

前述 `1f0dffc…` 资源责任停止线是主控媒体责任裁决前的失败历史，继续保留。主控随后正式
采用 `MediaCapabilityEnvelopeV1 + 封闭多程序 MediaProgram + DeliveryCompiler 确定性
编排`，并冻结三层媒体责任：

1. 只有资源正式登记、当前启用、租户／品牌／账号／组织作用域合法、本次明确选择且版本
   冻结时，Compiler 才能生成拍摄、摆放、出镜或使用等确定指令。
2. 没有登记实物时，Compiler 可以生成独立的 `optional_capture_suggestion`，但必须使用
   “如果仍在手边／如果方便补拍／如果你愿意”等条件语态，不进入 required resource refs，
   不成为主成品成立条件，并与不依赖该实物的抽象编排同时存在；可见建议进入 provenance
   和 artifact digest。
3. 没有登记资源且无需补拍建议时，只使用文字、排版、色块、线条、符号、留白、字幕和切换
   节奏，不出现未登记人物、商品、场地、家具、道具、照片或环境声。

实现 `fc45c11a5beb021d8c4ff69aee61a19f035eba77` 新增版本化
`media-capability-envelope-v1`、`media-program-v1`、`creative-kernel-v4` 和
`delivery-compiler-v4`。Writer 对 v4 只填写 `title`、`natural_guide`、`body` 和
`release_caption`；返回任一媒体单元即失败关闭。服务端在 Writer 前冻结 capability、
resource、program、unit binding 和可见顺序；Compiler 只消费封闭 primitive 与 Envelope
内资源，确定性生成首图／首帧、观看链、字幕、声音、制作提示、provenance 和 digest。
production condition、用户提及、ProductFact 或未选择素材均不能自动授予媒体资源。修订
重放原 Envelope／program／resource；legacy v1/v2 和旧 v3 快照继续按原版本读取。

首轮套件暴露 P2／P5 商品 Writer 仍从原始题材和账号领地推导商品属性或用途。最终实现
`0c4f0d48f2d89daf0082c063c22c4f14aa6ea7e5` 进一步为 product_truth／
visual_styling 使用 `deidentified-product-writer-brief-v1`：Writer 只获得与对象无关的
判断顺序、语气和本次明确目录选择，不获得可反推商品身份、属性、用途、体验的原始题材；
冻结商品事实仍由服务端逐字插入。

完成证据：

- Gate C 定向测试 `52 passed, 1 skipped`；`git diff --check`、Ruff、mypy、
  Golden/OpenAPI `475 passed, 2 skipped`、前端 lint／typecheck／interaction／build 和
  显式 Gate C Chrome 全部通过。
- Writer 媒体自由文本、production condition 授予资源、ProductFact 授予媒体资源、修订
  改变 Envelope／program、所有无资源成品退化为单一程序，以及 product Writer 恢复原始
  题材的 mutation 均能让对应承重测试真实变红，恢复正确实现后转绿。
- 产品／内容审查逐卡检查观看价值、账号关联、平台完整性、自然语言、非模板化、事实／资源
  边界、制作可执行性与系列连续性；工程／安全／兼容审查复核 Writer／Compiler 责任、
  Envelope／program／resource 冻结、legacy、UI-12、digest、RLS、AIGC 和失败原子性。
  两份有界审查均无阻断。
- 同一 `0c4f0d4…`、`deepseek-v4-flash`、temperature 0、max_retries 0 下，P1—P5、
  series2、series3 各调用一次；无数据库、Redis 或业务持久化，不择优、不跨 SHA 拼接。
  七卡逐篇阅读全文均通过，并形成七种不同 MediaProgram。
- P2 三条冻结商品事实各逐字一次，正文不新增正反面、内外层、通勤／约会、材质、版型、
  保暖、舒适或体验；P3 咖啡原句逐字一次，账号“把熟悉事物重新看见”的观察路径自然可读；
  P5 只使用两个本次冻结的登记商品资源，required resource refs 与 Envelope 精确一致；
  series2／3 按冻结前情连续推进且媒体职责不机械重复。
- 隔离执行主机 root-only 证据路径为
  `/var/lib/diyu-ux03-evidence/0c4f0d48f2d89daf0082c063c22c4f14aa6ea7e5/gate-c-final-suite/`，
  包含 raw、artifact、配置、`manifest.json`、`human-review.json` 和 `SHA256SUMS`。
  七卡 file SHA、raw SHA、正式 `visible_digest` 与人工审阅引用全部可复算一致；临时容器、
  源码投影和凭据环境已清理。

以上是当时的执行侧完成记录，现作为历史保留。主控随后审计确认 P5 的两个
`registered_product_display` 由七卡 Runner 手工构造，正式 ContentService 只解析本次
选择的普通素材；租户管理员也没有正式商品—组织官方素材绑定消费者。因此该记录不能证明
真实用户可经正式 React/API/PostgreSQL 获得 P5，FT-041 退回 `有缺陷`，当前真值为
`53/5/0/6/0`，Gate C 为 `ACTIVE / REWORK`。本次返工只补正式绑定、可信 resolver、
版本冻结、P5 纵向及证据防假绿；后续 Gate `NOT_STARTED`。

## Gate C FT-041 正式商品媒体消费者最终候选

主控正式消费者审计后的返工没有放宽资源闭世界。schema `20260809_36` 只增加最小、
expand-only 的 `product_media_bindings`：管理员在现有组织官方素材管理中明确关联同
tenant／brand 的已确认商品，并可停用或恢复绑定；绑定主体不可原地换商品或素材，表启用
并强制 RLS。正式 resolver 只从用户本次明确选择的组织官方素材出发，按逻辑发布账号控制
组织核验商品范围与素材范围交集、商品／素材／绑定状态、当前版本及 checksum；两个不同
product／asset／binding 才生成版本化 `registered_product_display`。ProductFact、
production condition、文件名、说明、普通或个人素材、未选择资料及客户端伪造 ID 均不能
授予该能力。

正式 React 管理页可查看、建立、停用和恢复商品—素材关联；创作端在素材项上以产品语言
展示关联商品，并只提交用户本次勾选的素材。P5 资格不足时在 task/run/version 产生前返回
一次自然提示；正式纵向覆盖管理员建两件确认商品、两份组织官方素材、两条绑定，内容用户
选择两份素材后经正式 React/API/PostgreSQL 生成并回读 V1。快照冻结
`MediaCapabilityEnvelopeV2`、program、两个 binding、商品版本、素材版本、checksum 和
来源；V1→V2→V1、legacy 读取与平台改编继续按冻结资源合同工作。

前向实现 `35f813cd23f7faafaa999c047307c8dc82cbcd9d` 建立正式绑定与 resolver，
`4da65d86a3cec695ce566a5f8b98b07512da21ae` 只以确定性正式选择信号修复 P5 被概率 intake
错路由，不授予任何资源；最终实现 `ee1e2a4e56b2bd9303e44d83a3b001ab084230a5`
补齐 artifact 对正式 AIGC 标签和发布提醒的证据绑定。Runner 已不能手工制造登记商品
资源，finalizer 只有在七卡逐条件人工 PASS 文件齐全且 file SHA、raw SHA 与正式
`visible_digest` 一致时才生成 manifest。

最终门为 `git diff --check`、Ruff、mypy、Golden/OpenAPI
`481 passed, 2 skipped`、前端 lint／typecheck／interaction／build，以及正式管理绑定和
Creator 两条显式 Chrome。正式绑定／作用域／冻结／Runner／finalizer 等承重 mutation
均能使直接测试真实变红，恢复后转绿；产品、成品与用户体验审查及工程、安全、RLS、兼容与
防假绿审查均无阻断。

在同一 `ee1e2a4…`、`deepseek-v4-flash`、temperature 0、max_retries 0 下，
P1—P5、series2、series3 各执行一次；全部使用正式 API/PostgreSQL，P5 由正式浏览器路径
创建。最终 task/run/version 为 `7/7/7`、永久 running 为 0、传输重试为 0；P5 使用
`graphic_registered_product_relation_v1`，Envelope V2 精确冻结两个不同商品、素材、
绑定、版本和 checksum。七卡逐篇、逐 8 项人工全文审阅通过，正式 AIGC 标签与发布提醒
均绑定到对应 artifact。可复算、无凭据的本地主机证据投影位于
[`var/evidence/ux03-gate-c-ee1e2a4e/`](../var/evidence/ux03-gate-c-ee1e2a4e/)；
private raw 保留在权限受限本地目录。synthetic 租户、身份、凭据、会话、绑定、商品、
素材、任务、运行、版本和运维对象已对 25 类表核对为零残留。

FT-041 据此恢复为 `真实可用`，当前功能真值为 `54/4/0/6/0`；资产仍为
`41/243/25/119`，激活增量 0。UX-03 继续 `ACTIVE`，Gate C 为
`COMPLETE / 等待主控独立终审`，Gate D `NOT_STARTED`。没有 push、远程 CI、部署或生产
变更；唯一下一动作是主控独立终审 Gate C。

## Gate C FT-041 主价值、headquarters 作用域与证据最终收敛

主控对 `ee1e2a4…` 正式消费者候选的进一步审计确认：商品媒体绑定、Envelope V2、resolver、
RLS 和正式 P5 API/PostgreSQL 主体成立，但租户用户素材列表会把 headquarters 素材按公司级
组织放宽到区域／门店账号，P2 和 P5 仍可能以通用判断替代商品专属价值，证据还未严格绑定
真实 task/run/version 及结构化价值审阅。该裁决没有推翻既有架构，Gate C 原地前向收敛。

- 素材列表与正式运行消费者现在使用同一控制组织语义：品牌全员保持品牌内可见；指定区域
  使用登记祖先／后代；headquarters 必须由素材归属组织与当前逻辑根账号控制组织精确相等。
  不可见素材整行不返回，标题、文件名、checksum、说明和 product-media 元数据均零泄漏。
- `product-value-contract-v1` 在 task 创建前从冻结 ProductFactPacket、正式绑定媒体和
  Envelope 构造。P2 必须给出一项商品专属新增理解、一项相伴限制／取舍和至少一个成立／
  不成立条件；每项绑定真实 fact ID 与 packet digest，并由服务端确定性插入可见成品。
  P5 只有两件不同商品各有正式素材、且冻结颜色或轮廓足以形成不可交换的主次关系时才进入；
  否则在 task/run/version 前返回一次自然提示。ProductFact、production condition、个人／
  未选素材继续不授予媒体能力。
- 最终实现 `faa4792538cbbcb54ff7b329dee7d03dd9488a24` 在同一 SHA 上通过 Ruff、mypy、
  Golden/OpenAPI `491 passed, 2 skipped`、前端 lint/typecheck/interaction/build 和显式
  Gate C Chrome。headquarters 元数据泄漏、通用 P2、P5 资格放宽、证据 all-PASS、P3 账号
  关联与 P2 形式观察等承重 mutation 均能真实变红，恢复后转绿。
- P1—P5、series2、series3 在同一实现、同一模型配置下各调用一次。正式数据库中 7 个 task、
  7 个 succeeded run、7 个 V1 均由 artifact、manifest 与数据库交叉核对；永久 running 与
  transport retry 均为 0。P2 人工记录精确包含专属理解、取舍和条件；P5 记录具体视觉命题
  及两个冻结 resource refs。七卡逐篇八项人工全文审阅均通过，不以哈希或 PASS 表单代替语义。
- 完整私有证据在
  `/home/faye/.local/share/diyu-ux03-evidence/faa4792538cbbcb54ff7b329dee7d03dd9488a24/gate-c-final-suite/`；
  `SHA256SUMS` 的 19 项、正式 `visible_digest`、raw SHA 和人工审阅引用复算一致。仓库内忽略的
  `var/tmp/ux03-gate-c-final-evidence-faa4792/` 是诚实标记为 index 的三文件自校验投影，明确
  指向私有证据，不冒充自包含原文副本。synthetic 数据与临时敏感材料已经精确清理。

据此 FT-041 恢复为 `真实可用`，功能真值为 `54/4/0/6/0`；Gate C 为
`COMPLETE / 等待主控独立终审`，Gate D 保持 `NOT_STARTED`。本轮没有 push、远程 CI、部署、
生产连接或资产激活。

## Gate B 目标

Gate B 在同一 UX-03 内一次关闭品牌文字资料、组织三级范围、商品事实、组织官方素材、团队
7/30 日使用口径和六类工作就绪诊断。正式运行作用域只取逻辑发布账号控制组织；资料版本、
来源和实际消费必须冻结到任务，不由登录自然人组织或客户端范围参数授权。Gate B 继续只在
本地施工，不 push、不触发远程 CI、不部署、不连接生产、不调用模型；Gate C 不自动启动。

## Gate A 完成记录

Gate A 首次冻结实现为 `734dcee900bc67e23d058d4fd6f8ef15db8d9847`，首次完成记录为
`11444a4d7920e5aa55979241691908120f25aca2`；二者作为主控有界返工前的历史证据保留。
最终有界返工实现为 `4899a068b7b2310cb45cecac4def4ff17e537f05`。这些提交只存在于
本地 `main`，没有 push、远程 CI、部署、生产连接或模型调用。

### 正式合同与生命周期

- `CreatePublishingAccountRequest` 和 `CreatePlatformCarrierRequest` 继续使用
  `extra="forbid"`；正式 React 不再发送 `target`。抖音、小红书、微信视频号由一个
  类型安全穷举映射产生，`微信视频号` 不再经过中文字符串删词。
- 一个逻辑发布账号只有一套 `ContentRole`、控制组织和当前五段画像；小红书图文／视频等
  内容目标只作为该账号的平台形式投影，不复制账号或画像。
- 全新租户先读取确定性品牌表达草案，经管理员修订和确认后才能创建发布账号。账号创建页
  同时给出基于租户、品牌边界和通用企业账号冷启动规则的五段画像候选；候选可编辑，保存后
  才形成 V1，不冒充品牌事实。
- 账号使用资格与五段画像维护资格按账号分别保存。创建账号不再静默授予维护权；成员工作
  资格调整在未显式提交维护清单时保持旧维护资格，专用维护接口只改变一个账号的维护资格。
  租户管理员入口、账号控制组织和两类资格互不推导。
- 成员支持名称／所属组织修改、停用、恢复、工作资格与重设链接；逻辑账号支持名称／控制
  组织、画像历史和新版本、停用／恢复；平台目标支持添加、停用和恢复。停用只阻止新选择，
  不删除历史任务、系列或版本引用。迁移 `20260807_34` 只增添
  `content_accounts.platform_enabled`，为 expand-first。
- `/user` 展示自然人和所属组织；`/content` 从当前发布账号读取控制组织、
  `ContentRole` 和画像版本。切换平台或形式不会改变自然人、逻辑账号、控制组织或画像。

### 正式纵向与防假绿

全新 synthetic tenant 通过正式运维 API、激活页面、租户管理 React/API 和本地 PostgreSQL
完成：首位管理员激活，品牌基线修订确认，总部／门店组织，两名内容用户，两个独立逻辑账号，
各一份五段画像 V1，总部四个内容目标、门店至少两个目标，两名用户隔离激活和登录，
`/user`／`/content` 身份与控制组织投影，平台切换画像不变，成员停用／恢复，平台停用／恢复
及历史引用保持。纵向结束后按精确 tenant/operator ID 清理，Gate A synthetic 租户、账号和
运维身份残留均为 0；全程没有直接 SQL 辅助业务步骤，SQL 只用于测试 fixture 建立、断言和
受控清理。

正式 Chrome 纵向覆盖 `1440×900`、`768px` 和 `390×844`，并检查 200% 等效缩放、键盘
焦点、无横向溢出、无控制台错误和无外部网络请求。四项 mutation proof 均真实红转绿：

1. 把 `target` 加回正式前端账号／平台请求，严格交互合同测试变红；
2. 恢复 `微信视频号 → 微信号` 的字符串替换，平台映射合同测试变红；
3. 恢复创建账号时静默授予画像维护权，交互测试变红；
4. 删除品牌基线确认门，前端纵向合同测试变红。

恢复正确实现后，完整本地门为：`git diff --check`、Ruff、mypy、OpenAPI、
Golden `421 passed, 1 skipped`、前端 lint/typecheck/interaction/build 全绿。

### 两份有界审查

1. 权限、身份及生命周期复核：无阻断。创建账号时自然人、控制组织和维护权在同一事务中
   校验；跨组织只可获得使用权，不能获得画像维护权；停用账号既有授权可保留或显式移除，
   不能向新成员或已移除成员重新新增；跨租户、错误账号、错误 carrier 和错误组织均失败关闭。
2. 正式 React、浏览器证据、清理和真值口径复核：无阻断。管理端会在维护权与负责团队不
   一致时阻止提交并在选择变化后清除失效勾选；内容端按可信授权只读或保存画像新版本；
   正式 Chrome 完成 V1→V2 与跨形式同版本回读；每轮 synthetic fixture 清理后逐表为零。

### Gate A 最终有界返工

- **画像维护权组织边界**：`operator_can_maintain_expression_profile=true` 时，首位使用者
  必须是当前租户内已启用的租户用户，且所属组织等于明确声明的账号控制组织；没有控制组织、
  不存在或跨租户使用者、同租户不同组织均在写入账号、ContentRole、画像或授权前以 422
  失败关闭。同租户跨组织且仅授予使用权继续按合同成立。
- **停用账号成员接缝**：成员资格替换区分“已有停用账号授权原样保留”“显式移除”和“非法
  新增”。正式成员抽屉展示“已停用，不能用于新工作”，管理员可保留或移除；修改姓名、组织或
  其他资格不会因历史授权导致 422，也不会静默删除它。
- **内容用户画像维护消费者**：`/content` 身份抽屉显示五段完整字段。可信授权返回
  `can_maintain`；有权者基于当前版本保存 Vn+1，无权者只读且没有伪编辑动作。服务端仍在
  版本写入事务内复核授权；旧版本、品牌基线、私人偏好和其他账号画像不改写，物理平台 carrier
  不能冒充逻辑账号。
- **证据与清理**：正式 API/PostgreSQL 反证覆盖跨组织维护 422 且零账号／授权、同组织维护
  成功、跨组织仅使用成功、停用授权保留／移除／禁止新增、无权写入和错误账号／carrier。
  显式正式 Chrome 命令
  `DIYU_RUN_UX03_BROWSER=1 .venv/bin/pytest -q tests/test_ux03_gate_a.py`
  在最终实现上退出码 0（3 passed）；三视口旅程确认管理端约束、停用账号接缝、内容用户
  V1→V2 以及同一小红书逻辑账号图文／视频共享同一画像 V2。清理后 tenant、registry、
  brand、organization、user、credential、token、session、grant、content account、
  ContentRole、profile version、activity event、platform operator/session 和 ops audit
  均为 0。

### 功能真值与后继边界

64 项真值已逐项重算，不机械沿用 UX-02 的 `58/0/0/6/0`。Gate A 当前为：

- 真实可用：`44`
- 有缺陷：`14`
- 纯占位：`0`
- 重复或应删除：`6`
- 尚无法证明：`0`

被降级的 14 项不是 Gate A 回归：品牌资料／商品／组织素材的完整生命周期与运行检索
（FT-027—FT-030）、团队用量口径（FT-031）、创作方向分类、正文生命周期、P1—P5 成品质量
和系列消费（FT-039—FT-041、FT-045）、DM01 商品作用域／规则消费／修订历史
（FT-050—FT-052）、`/status` 模型状态（FT-058），以及能力诊断（FT-032）的资料来源
可信度、版本时效、冲突与当前工作完整关联，均仍有正式消费者，但尚未达到 UX-03 后续 Gate
的产品目标，因此诚实记为“有缺陷”。该历史时点 Gate B 尚未启动，资产仍为
`41/243/25/119`，激活增量 0。

## Gate B 首次完成记录（历史保留）

Gate B 冻结实现为 `a67f44bdc4e73a2a8386cdfa311659220aefc0af`。迁移
`20260808_35` 只增加组织父级、三个不可变资料版本表和当前版本指针；新表启用并强制 RLS，
应用角色只有 `SELECT/INSERT`，版本行拒绝更新和删除。旧组织没有父级时保持原兼容范围，
不按名称回填；组织父级同租户、防自引用和循环。

### 资料、范围与运行消费

- 品牌文字资料必须先预览再明确确认，保存形成不可变 V1；修改形成 V2，停用不进入新任务，
  恢复继续使用原当前版本。商品事实支持手工、粘贴表格和 CSV 字段预览，空字段不猜测，
  明确确认后才形成 V1/V2。组织官方素材的原件、人工说明、来源、版本和范围保持绑定，
  只有本次明确选择才进入生成输入。
- 资料范围只有品牌全员、总部专用、指定区域。总部专用只接受明确登记的公司级组织；指定
  区域覆盖该组织及登记的后代，不覆盖兄弟区域。运行判断只读取逻辑发布账号控制组织，
  不读取登录自然人的组织，也不接受客户端范围授权；品牌与租户边界同时失败关闭。
- 新任务只读取当前、启用、范围匹配的文字资料，以及弱种子明确命中的已确认 SKU；未选
  官方素材和无商品输入不会引入整个库。任务快照记录稳定 ID、版本、来源和所选素材内容；
  后续 V3、停用或范围变化不改变旧任务及其修订。

### 团队使用与能力诊断

- 团队使用固定支持近 7 日和近 30 日，分别统计启用成员、登录成员、产品活跃成员、最近
  登录／产品动作、普通交流、首次生成、内容修改、系列续写、DM01、成功／失败／429 和已记录
  token。失败运行不算成品，token 不冒充账单，响应不含正文、Prompt、私人偏好或私人素材。
- 能力诊断固定为日常非商品内容、商品承重内容、连续系列、指定平台与形式、DM01、新成员
  首次创作六项；每项具有状态、依据、来源、版本、范围、更新时间、缺口、结构化冲突、影响、
  不影响项、补充入口、判断时间和合同版本。没有显式有效期时不猜过期，商品或门店资料的
  局部缺口不阻止非商品创作。

### 正式纵向、防假绿和完成门

一个全新 synthetic tenant 建立公司、华东／华南区域及门店、三个逻辑发布账号、另一品牌
和跨租户诱饵，完整经过正式 React/API/PostgreSQL：资料预览确认、V1→V2、历史、停用恢复、
组织范围、商品导入、素材明确选择、任务快照与修订、7/30 日使用和六类诊断。三个视口
`1440×900`、`768px`、`390×844` 的正式 Chrome 检查键盘、焦点、44px 触控、200% 等效缩放、
横向溢出、控制台和外部请求；结束后 tenant/operator 及其业务、认证、授权、版本、事件和
运维对象逐表为 0。

真实反向证明把兄弟区域素材放行、无商品任务注入整库、修订重读已停用素材三种错误实现
分别恢复后，同一纵向均稳定变红；恢复正确实现后再次转绿。另有直接断言覆盖登录人组织替代
账号控制组织、停用／旧版本进入新任务、7/30 日混算和局部缺口升级为全局不可用。

完整本地门为：`git diff --check`、Ruff、mypy、Golden/OpenAPI
`422 passed, 1 skipped`、前端 lint/typecheck/interaction/build 和显式 Gate B Chrome
纵向全绿。两份有界审查结论：

1. 产品、管理职责与真实使用体验：无阻断，资料生命周期、三级范围、团队口径和诊断使用
   产品语言，三视口主旅程可达。
2. 数据作用域、RLS、版本、运行消费与防假绿：无阻断，版本追加式、控制组织裁定、显式素材
   选择、任务冻结和跨租户／品牌／区域诱饵成立。

Gate B 后 64 项真值为 `50/8/0/6/0`。仅 FT-039—FT-041、FT-045、FT-050—FT-052、
FT-058 继续属于后续 Gate 的真实缺口。资产保持 `41/243/25/119`、激活增量 0；本轮未
push、未触发远程 CI、未部署、未连接生产、未调用模型。UX-03 仍为 `ACTIVE`，Gate C 为
`NOT_STARTED`。

## Gate B 主控终审退回与有界返工完成

主控没有重开 FT-027—FT-030，而是判定首次候选的 FT-031、FT-032 未成立：团队使用以
`series_id IS NOT NULL` 把系列首篇计为续写；能力诊断以品牌级独立数量拼接不同账号、组织
和区域的条件，可能产生 `available` 假绿；相邻的“指定区域”保存 API 还接受门店级组织。
该终审 `FAIL`、首次实现 `a67f44b…` 和首次完成记录 `a961709…` 均保留。当时功能真值诚实
退回 `48/10/0/6/0`。

本次有界返工实现为 `4a49e0d912e17a10965e840e89c1ef7f03bca3f6`：

- 团队使用只将 `business_tasks.series_position > 1` 计为系列续写；首篇、未入系列任务、
  普通 V2、失败运行和平台改编均不增加该项，7/30 日窗口分别作用于续篇任务时间。
- 一个共享的确定性可执行路径投影把品牌表达确认、启用逻辑账号、ContentRole、当前五段
  画像、启用 tenant-user 授权、账号控制组织、平台形式目标、作用域内当前商品／资料和
  DM01 同组织门店／成员／商品闭合在同一 tenant／brand 关系中。六类诊断的 `available`
  只来自至少一条完整路径；部分真实条件只形成 `conditional`，基础条件缺失为
  `unavailable`。
- evidence 不再以“当前资料 N 项”或 `confirmed` 冒充版本，而返回构成所选路径的真实对象
  ID、真实版本号／版本 ID或明确“未版本化”、适用范围和更新时间／未记录说明。普通 UI 只
  展示人话名称、范围和版本。
- 新保存的“指定区域”资料、商品和组织官方素材只能选择明确登记的 `region`；`company`、
  `operating_unit` 或其他层级均以自然语言 422 失败。已保存历史数据不改写，旧读取范围
  保持兼容。

同一 synthetic 纵向证明：华东完整账号＋华南商品、完整单平台账号＋另一不完整双平台账号、
以及分散在不同组织的 DM01 三条件均不是 `available`；将可见商品、第二平台或同组织 DM01
条件补到同一完整路径后才转为 `available`。兄弟区域、其他品牌和其他租户对象不进入
diagnosis evidence。系列只有首篇为 0，近 7 日首篇＋第二篇为 1，40 日前第二篇在 7/30 日
均不计，普通 V2 不增加续写。

三项真实 mutation proof 分别恢复“品牌级商品全量计数”“跨账号平台条件拼接”和
`series_id IS NOT NULL`，同一纵向均按预期变红；恢复正确实现后转绿。完整门结果为
`git diff --check`、Ruff、mypy、Golden/OpenAPI `422 passed, 1 skipped`、前端
lint/typecheck/interaction/build，以及
`DIYU_RUN_UX03_GATE_B_BROWSER=1 .venv/bin/pytest -q tests/test_ux03_gate_b.py`
退出码 0。产品诊断／团队口径复核与 SQL 作用域／证据闭合复核均无阻断；synthetic tenant、
系列、DM01 资格、资料、版本、身份和运维对象清理后为 0。

主控随后发现 FT-031 的相邻极小缺口：仅判断 `series_position > 1` 仍会把已经持久化 task、
但生成失败且没有 V1 的系列第二篇计为续写。最终前向实现
`0774ae685da3156e529a1b6fed5d502a920706a1` 将口径冻结为“统计窗口内成功提交完整 V1 的
系列后续篇”：

- 查询对 tenant 内 task 使用 `count(DISTINCT task.id)`，要求 `series_position > 1`、
  `parent_version_id IS NULL`，并存在同 tenant／task、`status='succeeded'` 的 run 所对应
  的 `version_number=1`；7/30 日窗口按该 V1 的 `created_at` 判断。
- 正式 ContentService 受控失败纵向证明系列第二篇会形成 task `+1`、run `+1`、version
  `+0`，run 为 failed、permanent running 为 0，但 `series_continuations` 保持不变。
  成功续篇增加 1；首篇、普通 V2、具有 `parent_version_id` 的平台改编和 40 日前提交的
  V1 均不增加。
- 唯一 mutation 临时恢复仅按 `series_position > 1`，失败续篇反证从 0 错增为 1 并真实
  变红；恢复正确查询后转绿。定向测试、`git diff --check`、Ruff、mypy 和唯一一次
  Golden/OpenAPI `422 passed, 1 skipped` 通过；正式前端零差异，未重复前端门。

功能真值据此保持 `50/8/0/6/0`；剩余 8 项仍是 FT-039—FT-041、FT-045、
FT-050—FT-052、FT-058。资产保持 `41/243/25/119`、激活增量 0；未 push、未触发远程
CI、未部署、未连接生产、未调用模型。UX-03 保持 `ACTIVE`，Gate B 为 `COMPLETE` 并等待
主控最终独立终审，Gate C 为 `NOT_STARTED`。

## Gate C 首次候选完成记录（历史，已被主控 FT-041 FAIL 取代）

执行侧当时以实现 `4d797d35614216c92a7e62ae7a62d7456165d70b` 自报在同一 UX-03 内关闭
FT-039、FT-040、FT-041 和 FT-045。开发期暴露的 P2 商品事实复述、P3/P4 现实扩写及 P5
商品类型猜测均保留为失败证据；每次后续模型调用只发生在共享合同或实现发生可说明变化之后，
没有随机重跑、fallback、第二模型或 Reviewer。

### FT-039 与 FT-040

- 题材、讲法、风格、形式、系列与互动仍是默认收起的可选控制；本次选择、开放自定义、
  cleared axes、origin、catalog version、body-related opt-in 和透明转译均进入任务快照。
  不打开面板不会伪造选择；修订重放原任务冻结方向，不重读当前目录或个人默认。
- `received → compiling_context → generating → validating → finalizing → completed` 由
  ContentService 实际到达节点发出。`completed` 只在版本事务提交、artifact digest 校验和
  正式回读成功后出现；模型草稿、修复中间稿和 token 不进入浏览器。
- commit／digest／正式回读失败时不发 `completed`，版本增量为 0、运行最终 failed、
  permanent running 为 0；普通“发送”不产生生成阶段或 task/run/version；同 request ID
  重试不形成重复版本。

### FT-041 媒体原生成品

- 服务端在 Writer 前预分配标题、核心正文、图序／镜头和发布配文等
  `creative_expression` 单元；Writer 只填写既定 unit ID，不能创建事实、轨道、来源、
  资源或结构。商品事实和真人事实继续由服务端原样插入。
- `delivery-compiler-v3` 只确定性组装事实、一次自然范围、媒体结构和独立 AIGC 提醒，不再
  创作“从你提供的片段出发”“沿着正文主线”或固定互动句，也不把正文机械复制为字幕和图序。
- recommendation、hypothesis 和 dramatization 的范围由服务端按整个单元添加。普通商品
  表达只向 Writer 暴露去标识化资源角色；P5 只获得“第一件／第二件已登记商品”的不透明资源
  别名，不能从 SKU、品类或属性猜测商品现实。

### FT-045 系列前情

- 第一篇无前情；第二篇只读取第一篇；第三篇按 position 稳定读取前两篇。每条前情的
  task/version/position/outline/body 经过同租户、品牌、逻辑账号、系列和用户边界校验，
  同时进入 Writer 输入和任务快照。
- 草稿、失败运行、普通 V2 和平台派生版本不能冒充系列新篇；跨租户、品牌、账号或系列诱饵
  失败关闭。修订重放冻结前情，后续资料、画像或目录变化不污染旧任务。

### 最终同 SHA 套件与人工全文审阅

最终源归档 SHA256 为
`ad717e17e41cb2294d45e3f9f3c1305c1302becb77cf8c126e37387a07feac65`。隔离 runner 使用
`deepseek-v4-flash`、temperature 0、max_retries 0，共 7 次必要调用，未连接数据库、
Redis 或业务存储。P1—P5 与系列第二／三篇均由执行端阅读全文并在主要价值、账号关联、平台
完整性、自然语言、非重复、事实／资源边界、制作可执行性和系列连续性八项上判定 `PASS`：

- P1：总部短视频给出有边界的两场景穿衣选择；
- P2：总部小红书图文只插入原样商品事实，并提供“一件两用”的选择解释；
- P3：咖啡真实片段逐字保留，其余为显著假设，作品不再重复工程范围或鸡汤编译文案；
- P4：门店观察逐字保留，未执行建议明确保持建议身份；
- P5：门店短视频使用两件已登记商品形成可拍视觉关系，不猜品类或属性；
- 三篇系列从“不打扰”承接到“回应”再到“留出选择”，可读出连续主线且不复述前文。

隔离执行主机的 root-only 证据位于
`/var/lib/diyu-ux03-evidence/4d797d35614216c92a7e62ae7a62d7456165d70b/gate-c-final-suite/`，
包含逐卡原始响应、配置、SHA256、人工全文审阅及产品／工程两份有界审查，目录 `0700`、
文件 `0600`。其中 P1—P5、series2、series3 artifact SHA256 分别为：

```text

```

### 防假绿、完整门与当前边界

方向运行映射、系列前情 Writer 输入、commit 后 digest 回读、v3 compiler 路由和商品媒体资源
归属五项错误实现均真实使直接测试变红，恢复后转绿。显式 Gate C Chrome 覆盖
`1440×900`、`768px`、`390×844`、等效 200% 缩放、键盘／焦点、44px 触控、无横向溢出、
无控制台错误和无非模型外部请求。

最终本地门为 `git diff --check`、Ruff、mypy、Golden/OpenAPI
`440 passed, 2 skipped`、前端 lint/typecheck/interaction/build 和显式 Gate C Chrome
全绿。产品语义／全文质量审查与工程／事实边界／原子性审查均无阻断；UI-12 的事实轨、
创作轨、digest、RLS、AIGC 和失败原子性不回退。

该历史候选当时自报功能真值 `54/4/0/6/0`，剩余缺陷仅为 FT-050—FT-052 和 FT-058。资产保持
`41/243/25/119`、激活增量 0。本 Gate 未 push、未触发远程 CI、未部署或连接生产；原始
证据中的凭据投影、临时源码与容器均已清理。UX-03 保持 `ACTIVE`，Gate A、Gate B 均
`COMPLETE / PASS`，Gate C 为 `COMPLETE / 等待主控独立终审`，后续 Gate
`NOT_STARTED`。本候选不证明真实员工／品牌采用、真实发布、平台流量、排名、爆款、
GMV／销售、多真实租户市场差异、企业 SLA 或 `20/55/44` 全组合稳定支持。其后主控已将
FT-041 退回并形成正式消费者返工；该退回历史保留，最新状态与结论以本执行包顶部及
“Gate C FT-041 正式商品媒体消费者最终候选”一节为准。

## Gate C FT-041 最终产品语义候选（当前）

主控保留已经成立的组织作用域、RLS、商品—组织官方素材绑定、Envelope V2、正式 P5
API/PostgreSQL 路径、证据 UUID／digest/checksum、FT-039、FT-040 和 FT-045；本次只关闭
P2 自然商品价值、P5 冻结主辅角色及人工证据独立引用三个缺口。运行实现为
`ec3dbe45d485816c7a49f9c801cd2baa2fe58c04`：

- P2 的服务端 `ProductValueContract` 仍只引用冻结商品事实，但用户可见正文不展示验收
  标签。双面短外套的价值落到“两面是两种完整可见选择”，取舍落到“突出一面会暂时放下
  另一面视觉重点”，并明确只有用户确实需要切换两面时才成立。Writer 的通用商品正文不再
  进入 P2／P5 可见事实段，也不能带出合同未选择的重量、属性、用途或体验。
- P5 的主视觉／辅助视觉由本次明确选择的两份正式绑定素材顺序冻结，绝不按 product、asset、
  binding UUID 或数据库返回顺序推导；UUID 重建和插入顺序交换反证均保持同一结论。Compiler
  在首图、图序和制作提示中共同执行主视觉居中／较大／先出现、辅助视觉侧置／较小／随后回应，
  正文与媒体结构不再互相矛盾。
- `gate_c_evidence` 的人审合同要求 P2 分别引用最终 artifact 中的商品专属理解、取舍和成立
  条件；P5 分别引用具体视觉命题、两个 resource refs、主辅角色、图序和制作提示。只复述
  task snapshot、只填 PASS 或篡改 artifact／visible digest 均不能生成 manifest。

同一运行 SHA、同一 `deepseek-v4-flash`、temperature 0、max_retries 0 的 P1—P5、
series2、series3 各执行一次；正式 API/PostgreSQL 形成 7 task／7 succeeded run／7 version，
永久 running 和传输重试均为 0。七张成品逐篇八项阅读全文均通过：P1 给出早凉午热条件下
可调整的穿衣选择；P2 逐字保留三项冻结事实并形成自然取舍；P3 保留咖啡原句且由总部穿衣
编辑的“重新看见熟悉选择”视角承接；P4 保留门店观察并让建议保持未执行身份；P5 只使用
两份登记商品图片和确认颜色形成色彩主辅；series2 从“不打扰”推进到“回应”，series3 再
推进到“留出选择”。AIGC 提醒独立保留，事实／假设／资源范围和系列前情均未回退。

私有完整证据为
`/home/faye/.local/share/diyu-ux03-evidence/ec3dbe45d485816c7a49f9c801cd2baa2fe58c04/gate-c-final-suite/`；
本地主机索引为 `var/tmp/ux03-gate-c-final-evidence-ec3dbe4/`。两处 `SHA256SUMS` 均通过
`sha256sum -c`，manifest 绑定真实 task/run/version、raw/artifact SHA、正式
`visible_digest` 和 artifact 原文引用。synthetic tenant 及其 38 task／38 run／33 version、
会话、商品、素材、绑定已清零，journey token、隧道和临时 runner 已销毁；不可变证据保留。

最终工程门为 `git diff --check`、Ruff、mypy、Golden/OpenAPI `495 passed, 2 skipped`、
前端 lint/typecheck/interaction/build 和显式 Gate C Chrome。Chrome 覆盖 `1440×900`、
`768×900`、`390×844`、等效 200% 缩放、键盘与焦点、44px 触控、无横向溢出、无控制台
错误和无非模型外部请求。产品／内容审查与工程／安全／兼容审查均无阻断。首次 Golden
发现旧 M5 oracle 仍要求 Writer 自由带出未选重量事实；测试提交
`6e3cb46b6c18d82f245a92f36c972ab65fa62bde` 改为验证服务端实际冻结的双面完整外观，运行
代码没有变化，定向与全量回归均转绿。

当前功能真值为 `54/4/0/6/0`，剩余缺陷精确为 FT-050—FT-052 与 FT-058；资产保持
`41/243/25/119`、激活增量 0。UX-03 保持 `ACTIVE`，Gate C 为
`COMPLETE` 且主控最终终审 `PASS`，Gate D 已转为 `ACTIVE`。Gate C 本轮未 push、未触发
远程 CI、未部署或修改生产。唯一下一动作：在本地完成 Gate D 的正式 DM01 与诚实状态页
纵向闭环，全部完成门通过后将 UX-03 置为 `REVIEW`。
