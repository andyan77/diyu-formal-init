# UI-05 租户管理与创作体验语义重构执行包

- 当前状态：`REVIEW`
- 启动基线：`a95c4e54bc36bcd80a0a016bbfbb3a29cde7aaa7`
- 前置：UI-04 `CLOSED`；本里程碑是新真实使用证据触发的 successor，不重开或否定 UI-04。
- 关口结果：Gate A 首次候选 `58520b965baa1c3d67eb04aa571e83647d2b912e` 未通过主控
  产品审查，历史证据保留；原位返工 `438e90404e63da421f720422988edd64d06548d5` 获主控
  `PASS`。Gate B 正式工程化与 Gate C 生产替换已连续完成。
- 唯一下一动作：主控独立终审 UI-05；执行侧不得自行 `CLOSED`，不得启动新里程碑。

## 1. 真实结果

租户管理员应清楚知道“我管理谁、谁在使用、缺什么、影响什么、如何补”；创作者应在一个
发布账号下直接选择平台和形式，不把平台载体误认成另一套账号或人设，并在生成期间看到可信
过程状态。产品页面不暴露数据库对象、内部权限词、工程证据或验收过程。

## 2. 冻结语义

1. 登录账号代表自然人与入口资格，不是企业发布账号。首期租户管理员与租户用户入口职责
   互斥：管理员只进入品牌管理；租户用户按资格进入内容创作和／或陈列搭配。
2. 用户只看到逻辑“发布账号”：名称、品牌与负责团队、唯一当前 ContentRole 内部分类、
   唯一版本化五段账号画像、可操作成员和一个或多个平台目标。
3. carrier rows 继续作为兼容物理实现，普通 UI 不展示其名称、父子 ID 或物理行数。
4. 平台是本次内容目标。页面显式选择优先于账号默认和服务端稳定默认；自然语言不静默覆盖
   页面选择。
5. ContentRole 只作为内部稳定类型或画像短标签；原表达边界并入画像第二段。画像只归逻辑
   发布账号，跨平台新建读取当前画像，重编译与旧任务继续使用冻结画像。
6. 内容和陈列继续是参考建议，不增加确认、审批、授权或责任流程。
7. 普通交流不自动创建成品；想法不完整时只问一个最有价值的问题，要求足够时直接进入生成。
8. 创作方向是可选快捷控制，采用题材、讲法、风格、形式、系列与互动五轴；人物关系和本次
   边界由自然语言承接，不进入封闭风格枚举。
9. 品牌资料可用范围只使用“品牌全员”“总部专用”“指定区域”；账号画像不进入品牌资料库。

## 3. Gate A：本次交付

静态原型位于 `docs/前端UI架构/产品化HTML原型_UI-05/`。独立评审壳先选择两条互不穿透的
身份旅程：

- 租户管理员：管理概览 → 团队使用 → 成员与互斥入口资格 → 发布账号与五段画像 →
  品牌资料库三级范围 → 当前可用与待补 → 管理员访问创作端被拒绝；
- 租户用户：选择发布账号、平台和形式 → 普通交流 → 内容想法与一次澄清 → 五轴与人物关系
  开放输入 → 生成 → V1→V2→V1 → 失败恢复 → 租户用户访问管理端被拒绝。

原型必须直接使用正式 VI，离线可浏览，支持 `1440×900` 与 `390×844`，没有外部请求，
产品页不含评审控件。所有按钮都落到页面、抽屉、弹窗或稳定状态；不放平台连接、自动发布、
账单、实时监控、OCR、知识图谱、自动激活、取消生成或其他 Gate B 不准备实现的动作。

Gate A 的有界自然交流分支只冻结产品合同，不证明任意自然语言理解，也不证明正式 API、
React、数据库聚合、流式响应或生产替换已经完成。

## 4. Gate B 前承重预检

仓库只读核对确认 root account 与 carrier rows 原则上可以无损聚合，但正式实现前必须在生产
做一次零写入预检：

- 每个启用根账号恰有一个当前 ContentRole；
- carrier 指向同租户、同品牌的根账号，组织与角色关系没有漂移；
- carrier 没有需要保留或迁移的独立五段画像；
- 根账号与 carrier 的成员授权差异可以无损解释；
- 多根账号自然人的显式账号选择不依赖客户端猜测；
- 历史系列、任务、版本及平台父子关系可继续读取。

任何冲突都不得用 `LIMIT 1`、名称裁剪、画像复制或删除历史行掩盖。

## 5. Gate B / Gate C 合同（已执行）

Gate A 获主控通过后，Gate B 只补最小聚合 API、团队用量、确定性能力诊断、品牌资料导入、
正式 React 与真实过程状态流；Gate C 只执行唯一 CI、备份、expand-first 迁移、生产替换和
有界验证。实际实施没有扩张到平台连接、自动发布、知识扩库、第二模型、消息队列或新工作流。

## 6. 不变边界

- 不删除或合并历史 carrier rows，不复制三份画像；
- 不新增平台连接、OAuth、发布、审批、监控平台、知识图谱或第二套状态机；
- 不批量激活候选资产，不清偿 39 项缺口；
- Gate A 资产激活增量为 `0`：41 条运行激活、243 条候选、25/119 目录及既有缺口不变；
- UI-04 的生产 SHA、镜像、schema、备份、readiness、RLS 与回退证据保持有效；
- 不证明真实员工采用、真实资料完整、真实发布、市场表现、企业采用、跨租户市场差异、
  企业 SLA 或 20/55/44 全组合稳定支持。

## 7. Gate A 首次候选（历史证据）

首次候选已在提交 `58520b965baa1c3d67eb04aa571e83647d2b912e` 落盘并推送。其当时的
有界验证与两份审查记录继续作为历史过程保留；主控随后确认六项产品语义缺口，因此该候选
没有获得进入 Gate B 的裁决：

1. 管理员和租户用户被串成可穿透的双资格旅程；
2. 一个逻辑发布账号、多平台的演示不完整；
3. 任意输入直接落到固定成品，没有普通交流与一次自然澄清；
4. 缺少保留对话和输入的用户可见失败状态；
5. 五轴没有完整呈现，人物关系被错误压入封闭风格分类；
6. 品牌资料库范围语义冲突，账号画像错误混入资料库。

以下首次候选检查记录只说明当时检查过的技术表面，不再代表当前产品语义已经通过：

- [x] 八张代表页面与连续评审入口成立；
- [x] 一个发布账号、三个平台、四个内容目标和一份账号画像表达一致；
- [x] 概览、团队使用、资料缺口、资料导入和生成过程均有稳定落点；
- [x] 桌面、移动、键盘、44px 触控、无横向溢出成立；
- [x] 产品页没有内部工程与验收语言，没有外部网络请求或控制台错误；
- [x] `git diff --check` 通过；
- [x] 以 `[skip ci]` 提交并推送，不触发 CI、不部署。

### 首次候选有界验证（历史）

- HTML/JavaScript 基础解析通过，9 份 HTML 标签配平，本地链接缺失为 0；
- 无头 Chrome 在 `1440×900` 与 `390×844` 共检查 18 个页面/视口组合：文档横向溢出、
  小于 44px 的可见交互控件、控制台错误和外部请求均为 0，每个产品状态至多一个实心主动作；
- 团队 7/30 日切换、资料导入第 3 步、平台/形式冲突、最终成品、添加成员稳定结果、画像
  V3、素材与系列选择、小红书图文目标冻结及 V2→历史 V1 回读均通过交互反证；
- 产品审查修复了跨页数字、导入数量、死按钮、多个主动作与旧产品语言；语义审查修复了生成
  后目标仍可切换、形式冲突、平台载体计数、陈列依据和减少动效误跳业务过程。两审均无剩余
  Gate A 阻断；
- 本关口未修改正式 React、API、OpenAPI、数据库、认证、权限、生产或资产真源，未触发 CI、
  部署或模型调用。

## 8. Gate A 产品语义极小返工

本次返工原位关闭上述六项缺口，不重做 UI-05，也不进入 Gate B：

- 管理员与租户用户入口类型互斥；双方错入口均落到人话拒绝与正确恢复动作；
- 管理端演示两个逻辑发布账号；总部账号含三个平台、四个内容目标，画像始终归逻辑账号；
- 普通交流、一次自然澄清、要求足够后直接生成、V1→V2→V1 形成连续代表旅程；
- 失败状态保留对话和原始输入，不生成伪 V1；
- 五轴选择、自定义原话与生成前摘要联动；“婆媳”及“不把任何一方写成反派”作为开放情境
  保留，不进入封闭风格枚举；
- 品牌资料范围统一为品牌全员、总部专用、指定区域；指定区域必须选择具体区域，账号画像只在
  发布账号页维护。

### 可重放验证

`review/verify-ui05-gate-a.mjs` 使用仓库既有 Node 与本机 Chrome，结果写入
`review/verification.json`：

- 13 份 HTML 的本地链接与禁用语言检查通过；
- 两条身份旅程、入口互斥、两个逻辑发布账号、三个平台／四个内容目标、普通交流、一次澄清、
  五轴、开放人物关系、失败恢复和三级资料范围均通过；
- 完全不打开或不选择方向面板仍可生成，摘要诚实显示“本次方向：未选择（按自然交流整理）”，
  不静默补造五轴选择；
- `1440×900` 与 `390×844` 共 24 个页面／视口组合，无页面级横向溢出、低于 44px 的可见
  触控目标、控制台错误或外部网络请求；
- 五项内存单点变异均能令相应断言失败：管理员创作链接、普通交流直出成品、删除讲法轴、
  丢失／替换“婆媳”、删除总部专用范围；
- 资产激活增量仍为 `0`。

达到以上结果后 UI-05 当时仍保持 `ACTIVE`，以 `[skip ci]` 提交
`438e90404e63da421f720422988edd64d06548d5` 并计划性暂停。主控随后裁决 Gate A
`PASS`，因此本段只作为进入 Gate B 前的历史交付记录，不再描述当前关口。

## 9. Gate B 正式工程化结果

### 9.1 入口资格与逻辑发布账号

- `tenant_admin`、`tenant_user` 与笛语运维三类入口在正式认证中互斥。既有双资格身份保留
  租户管理资格，内容／陈列授权关闭，相关会话撤销；自然人、历史任务、版本、系列、审计和
  外键均未删除。
- 正式管理与创作 API 只把根账号投影为逻辑发布账号，`platform_targets[]` 聚合现有 carrier
  rows。平台载体继续兼容历史，不复制画像，不拥有密码；同一自然人可显式操作多个获准根
  账号，只有显式根授权才开放该根下的合法平台。
- 新任务和系列同时保存物理 `account_id` 与规范化 `logical_account_id`；平台、形式、
  ContentRole 和账号画像版本进入冻结快照。旧任务、旧系列、旧版本和平台父子 provenance
  继续可读，兼容触发器允许上一健康镜像在新 schema 上保持旧写形。
- 账号画像仍只归逻辑根账号。生产盘点和迁移前置检查确认 carrier 当前画像和历史画像均为
  `0`，不存在需要业务方裁决的冲突画像。

### 9.2 自然协作、进度和正式 React

- `/content` 以 `chat / question / ready` 三种自然结果承接生成前交流：普通交流零任务；
  内容意图只缺一个承重信息时只问一个问题；完整要求直接形成紧凑 brief 并进入正式生成。
- `/api/v1/content/stream` 使用有界 NDJSON 传递真实生命周期：
  `received → compiling_context → generating → validating → finalizing → completed/failed`。
  未校验正文不逐 token 暴露，完整版本只在验证通过后一次交付；失败或断线没有永久
  `running` 或伪版本。
- 正式 React 实现管理员任务型信息架构、团队 7/30 日使用、发布账号与画像、品牌资料库、
  六项当前可用与待补、统一创作端多账号／多平台、自然交流、五轴、V1→V2→V1 和失败恢复。
  DM01 继续只输出文字参考方案，不显示图形预览或 AIGC。
- 创作目录升级为 `content-expression-catalog-v2`，仍为 25/119（21 默认＋4 体型主动启用）。
  历史 `闺蜜聊天` ID 和旧任务原样保留，但不再进入默认面板；使用台账中已有、状态与消费者
  合法的 `口语短句` 补位，没有创建新能力或激活新资产。

### 9.3 品牌资料、团队使用和能力诊断

- 新增同一品牌资料空间的三种确定性范围：`brand_all`（品牌全员）、
  `headquarters`（必须绑定明确 company 组织）与 `organizations`（必须绑定明确组织）。
  过滤先于上下文编译，不能用名称推断组织；私人素材不会进入管理员资料库。
- 团队使用只聚合本租户注册／激活／启停、7/30 日活跃、最近使用、内容／修改／系列／陈列
  尝试、成功／失败／限流、逻辑账号与平台分布及“已记录模型用量”。响应不含正文、种子、
  Prompt、私人素材或偏好，也不冒充账单或完整成本。
- “当前可用与待补”只诊断六个已有直接消费者：品牌与账号表达、日常非商品内容、商品选择与
  解释、连续系列、跨平台版本、门店墙面挂杆参考方案。每项均返回状态、依据、缺口、影响、
  唯一动作、来源、版本和判断时间，不生成知识完整度百分比。

### 9.4 数据与安全

- expand-first migration `20260801_28` 在第一笔 schema/data 写入前，以真实非 superuser
  migrator 和逐租户 `set_config` 检查 carrier/root、授权、角色、画像与历史关系；发现歧义
  即整事务失败，不用 `LIMIT 1` 或名称猜测。
- `display_access_grants`、`brand_library_entries`、
  `brand_library_entry_organizations`、`brand_product_scope_organizations` 和
  `material_asset_scope_organizations` 均 `ENABLE RLS + FORCE RLS`。跨租户、品牌和组织
  反证、私人作用域、失败原子性及历史任务／系列回填均通过。
- 本轮没有改变领域资产真源。运行激活增量 `0`，继续为 41（20 知识、21 方法）；候选 243、
  目录 25/119、39 项缺口、6 项实验、1 项明确不做和 3 个来源缺口保持原去向。

## 10. Gate B 本地门与两份有界审查

- 定向测试覆盖 UI05-G01—G10 的承重正向和相邻反证；对普通聊天、入口互斥、多逻辑账号、
  资料范围、团队用量、能力诊断和真实生命周期均存在会因错误实现而变红的反向断言。
- 候选稳定后只运行一次串行完整门：`git diff --check`、lint、typecheck、前端
  lint/typecheck/test/build 与 `make golden` 全部通过；Golden 最终为 `172 passed`，
  OpenAPI 与真实路由一致。
- 产品/UI 审查 `PASS`：三空间任务优先、管理员无创作入口、创作端自然语言优先、DM01 纯
  文字、桌面移动无内部工程语言或竞争主动作。
- 工程/安全审查 `PASS`：可信会话、逻辑／物理账号冻结、FORCE RLS、私人作用域、迁移前置
  失败关闭、旧镜像兼容和资产 0 激活无阻断。
- 承重实现提交为 `7cf470f7f01e4c3f2e422853bf51779f5136b00f`；唯一远程 CI
  `30275795461` 对应同一 SHA，结论 `success`。

## 11. Gate C 生产替换与证据

- 部署前只读盘点确认旧锚点为 UI-04 实现 `da5391b…`、schema `20260731_27`、公网／回环
  readiness 正常，根账号 5、carrier 6、carrier 画像 0、历史任务 319、版本 280、系列 3、
  运行资产 41。
- 新鲜有界备份
  `/var/backups/diyu-m5-4/20260727T143917Z-ui05-predeploy` 完成校验和、数据库隔离恢复、
  RLS、应用 readiness 与对象恢复验证；部署脚本另生成
  `/var/backups/diyu-m5-4/20260727T143946Z-predeploy`。
- 同一绿色实现完成 expand migration 和生产替换。运行标签为
  `7cf470f7f01e4c3f2e422853bf51779f5136b00f`，镜像摘要
  `sha256:7337c492c489900926caf96c41804ea5dce04ffb7f677364866fcc979b08f298`，
  schema `20260801_28`，公网／回环 readiness 均为 `ready`。
- 有界生产探针使用现有 synthetic fixture 和短期内存会话，正式 API 证明：三入口互斥；
  一个逻辑账号四个平台及同一用户两个逻辑账号；三级资料范围互斥；7/30 日团队用量；六项
  确定性诊断；普通聊天零任务；模糊意图零任务且只追问一次；完整假设场景通过真实 DeepSeek
  形成 V1、自然修改 V2 并回读 V1；六段真实进度顺序成立；未授权账号 422 零任务／运行／
  版本；DM01 纯文字 V1→V2→V1 且库存守恒。
- 第一条承重生成尝试因现实主张边界未通过而终态 `failed`、版本数 `0`；执行侧没有原样随机
  重跑，而是基于直接失败把正向任务改为明确假设场景，随后成功。最终生产 `running=0`。
- 生产证据位于 `/var/lib/diyu-m5-4/ui05-review-final.json`，权限 `0600 root:root`，
  `status=pass` 且不含正文、Token、Cookie、Prompt、密钥或私人资料。临时授权、三级资料、
  会话和远端脚本均清零；没有新建长期验收账号或密码。
- 真实 Chromium 经只读 SSH 隧道直接访问 ECS Nginx，覆盖 5 个公共路由 ×
  `1440×900`/`390×844` 共 10 个组合；横向溢出、小触控目标、控制台错误、外部请求均为
  0，减少动效直接显示正式 Logo，跳过动效成立。
- 不做数据库 downgrade 的回退往返通过：上一健康镜像 `da5391b…` 在 schema
  `20260801_28` 上启动、readiness 正常并读取历史任务／版本／系列，随后恢复最终镜像并再次
  复核公网／回环 readiness 与 schema head。

## 12. REVIEW 边界

UI-05 当前为 `REVIEW`。本里程碑证明正式产品语义、React/API 消费者、迁移、生产路径、
DeepSeek 有界生成、RLS、失败关闭、备份和回退成立；仍不证明任意自然语言都无需澄清，也不
证明真实员工采用、真实品牌资料完整、真实发布、平台流量、排名、爆款、GMV、销售、企业采用、
真实跨租户市场差异、企业 SLA 或 `20/55/44` 全组合稳定支持。

唯一下一动作：**主控独立终审 UI-05**。执行侧不得自行 `CLOSED`，不得启动新里程碑。
