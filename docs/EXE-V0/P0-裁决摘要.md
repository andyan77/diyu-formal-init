# EXE-V0 · P0 生产画像只读核查 · 裁决摘要（2026-08-07）

> 来源：P0 一次性只读核查会话（connection_path=B，零仓库写入；完整证据在 founder
> 本地私有目录 `~/diyu-p0-evidence-2026-08-07/`，本文件只保留无敏感摘要与裁决）。
> 查询包编写与守护审查同源（监理），founder 知情；编写与执行分离。

## 一、核查摘要（P0_SUMMARY 要点）

| 项 | 值 |
|---|---|
| checked_at | 2026-08-07T13:58:28Z – 13:59:20Z |
| schema_revision | 20260817_44 |
| runtime_sha | `b12b3cbeb17c0af1b4a5452e54c4a5685adb0461`（部署仓 HEAD 与运行容器镜像 label 双向一致，dirty=0） |
| 只读自证 | read_only=on / isolation=repeatable read / tenant_ctx=258aa400-152a-4c36-96b5-324cd18b88b6 |
| 逻辑发布账号 | 3（同一品牌，channel=抖音；另 6 个平台 carrier 均无 current profile，符合约束——证明查的是逻辑账号） |
| 画像 | v1 / v1 / v2，confirmed_not_draft 全 true，content_territories 非空（78/64/72 字） |
| 发布投影 | version_number=2，status=confirmed，digest=`102e0cdf…b0faf5`（confirmed_at=2026-08-05） |
| overall_classification | **mixed_or_ambiguous** |

## 二、监理裁决

1. **P0 任务判定 COMPLETE**：只读纪律、证据纪律、诚实边界全部达标；会话对 RLS 失效
   所做的收窄型最小适配（三段查询显式加 `tenant_id` 谓词）正确且必要。
2. **mixed_or_ambiguous 成立、原因已查明**：A/B 两组整词零命中，但构成子串广泛存在
   （穿/家庭/商品 3/3 账号命中，生活 2/3，工作 1/3）——生产文案用词与预设关键词表
   不对齐，且大概率同时混有「家庭/商品」与「生活/工作」两种取向成分。会话按
   「不做语义发挥」规则拒绝语义判读，处置正确。
3. **分叉走向（第三态预案）**：EXE-V0 代码不受影响照常推进
   （`production_profile_calibration` 维持 `UNVERIFIED` 直至画像裁决落定）；
   **EXE-V1 启动前置新增一项：founder 画像评审**——founder 从私有证据目录取三账号
   `content_territories` 原文交监理，监理出改进草案，founder 定稿后经**单独授权**的
   生产画像更新动作落库，再做 EXE-V0 固定样本生产校准。分类判据自此以 founder
   画像评审结论为准，不再依赖关键词表（见 P0-D3）。

## 三、附带缺陷登记

| # | 发现 | 定级 | 去向 |
|---|---|---|---|
| P0-D1 | 生产容器连接角色 `diyu` 为 superuser 且 `rolbypassrls=t`，`row_security_active=f`——FORCE RLS 对其失效，租户隔离仅剩应用层单防线（全库 13 个 content_accounts 中 4 个属演示租户，原查询原样跑会串租户） | **中危·生产加固缺口** | EXE-V1 部署 runbook 加固项：改用非超管应用角色 / RLS 生效验证；生产变更届时单独授权 |
| P0-D2 | P0 Prompt 引用的 `scripts/load_env.sh` 在本仓不存在（监理跨项目惯例误植，自报）；会话按分支规则正确走路径 B。另记 `deploy/install_deepseek_secret.sh` 含 ECS host/user/key 路径明文默认值 | 低 | Prompt 误植已记错误档案；连接参数收敛与否由 EXE-V1 runbook 评估 |
| P0-D3 | 分类关键词表首版与生产文案用词不对齐（整词零命中、子串广泛命中） | 观测 | 判据切换为 founder 画像评审直接结论（见裁决 3） |

## 四、画像评审收口补记（2026-08-07）

- 取证方式：监理在 founder 在场的本会话中直读私有证据目录（founder 追问"是否已贴过"
  即视为交接授权），三段 `content_territories` 原文仅在会话内展示，**不进仓库**。
- **人工评审结论（取代关键词表判定）**：三段画像方向正确、三账号分工清晰
  （门店现场号 / 总部图文号 / 总部主号），"生活/工作"字样均为语境而非题材，
  自带反鸡汤红线；账号 3（v2）与 onboarding 定稿方向逐句同源。
  `overall_classification` 人工修正为 **family_apparel_focused**——
  P0 的 `mixed_or_ambiguous` 系关键词表口径假象（判词取自仓库两份非生效旧文本）。
- **founder 裁决（2026-08-07，AskUserQuestion）：画像维持现状，不做生产写操作。**
  根因确认闭环：泛生活内容的病根在缺价值引擎（EXE-V0 已建成合入），不在画像。
- 后续：`production_profile_calibration` 核销挪至 EXE-V1 生产冒烟（对真实画像跑
  固定样本，产出入私有证据不进仓库）；EXE-V1 的"founder 画像评审"前置自此关闭。
