# UI-09 单一创作内核与确定性成品编译闭环执行包

- 当前状态：`SUPERSEDED → UI-10`（此前 `BLOCKED` 事实完整保留；不表示 UI-09 成功）
- predecessor：`UI-08 SUPERSEDED → UI-09`
- 启动提交：`27f1e2e52c13fd83229a4a7c43da4992ae162731`
- `origin/main`：`7aa87ab624cf3ff64f42e49f1755d66d496cac7a`
- 生产安全锚点：`845f63291ba5060e60f87d1afa5cfc1cdb057e3b`
- 唯一执行端：当前 WSL 执行端。
- 目标终态：全部验收真实成立后进入 `REVIEW`，不得自行 `CLOSED`。

## 1. 根因与取代关系

UI-08 已证明当前与唯一候选 Writer 在同时承担叙事文字、scene、资源、动作、声音和制作提示时
均为 `0/3`。该结果没有授权第三模型或继续补 Prompt。UI-09 只修正 Writer 单角色职责：
创意文字先形成单一可审内核，制作条件随后由服务端确定性编译。

UI-08 的 Gate、计划、Frame、事实／约束分离和观察类型继续有效；其 `BLOCKED`、两个
Writer `0/3`、Reviewer 未验证、未 push/CI/部署与证据完整保留。

## 2. 产品责任

用户只需提供商品款号、生活片段、流水账、感悟或抱怨，可选题材／风格／形式控制；系统负责
选择主线、受众回报、平台表达和完整交付。只有缺少不可替代真实事实才追问一次。未提供的真人、
商品或品牌事实不得补造，默认直接交付完整平台 V1，不交付问卷、选题或提纲。

## 3. 唯一运行链

```text
CreationIntentGate
→ CreativePlanV2 / NarrativeFrame
→ CreativeKernelV1 Writer
→ 独立 Reviewer
→ 最多一次受影响可写 unit 修复并完整复审
→ DeliveryCompilerV1
→ 原子保存完整 V1
```

Writer 只返回 `{"units":[{"unit_id":"服务端既定 ID","text":"完整可见文字"}]}`。服务端冻结
unit id、purpose、允许观察类型、fact refs、constraint refs 与可见顺序；事实 unit 由服务端
逐字插入且不可修。额外、遗漏、重复、漂移 ID 或任何制作字段均失败。

## 4. 事实、输入与单元边界

- 真人原文、精确商品事实、精确品牌机构主张只由服务端逐字插入。
- Writer 只读 topic 精确跨度、必要事实、P1—P5 价值、平台／形式、去标识语气节奏、修改要求
  和 unit skeleton；不读不必要的品牌／组织／账号名称、长基线、资源表或 Reviewer 历史。
- 单元允许 `frozen_fact`、`abstract_observation`、`hypothetical_example`、
  `disclosed_dramatization`、`recommendation`。微场景只能进入已披露假设／演绎单元。
- 无精确品牌事实时不强制 P3 `brand_account_link`；账号观察、受众回报和当前文章视角即可。

## 5. DeliveryCompilerV1

编译器只消费已审内核、平台／媒体形式、登记生产条件、合法资源及已激活制作方法，确定性生成
现有 `GeneratedArtifact` 与 `ProductionBundle`。它不得增加现实事实、机构主张、人物、场地、
家具、道具或环境声；商品只有存在 `resource:product:*` 才能入镜。D1 无多演员／家庭现场时
编译为文字对话卡、旁白或一人分段表达，不声称存在第二演员或现实家庭场景。

每个最终可见字段必须追到已审 unit、服务端事实块或版本化中性编译短语；资源必须来自冻结集合。
新任务快照记录 `creative_kernel_v1`、`delivery_compiler_version`、
`reviewed_kernel_digest` 与 visible provenance。不新增表、迁移或历史回填；旧任务按快照
版本继续读取、修改、复制、导出、系列与跨平台改编，不作为新任务失败 fallback。

## 6. Reviewer 与修复

Reviewer 先以三条冻结样本验证 `abstract_principle/situated_event/institutional_assertion`
资格，再审 CreativeKernel 全 unit 精确跨度和完整覆盖。漏 unit、假／部分跨度或 uncertain
均失败。制作资源由服务端闭世界验证，不重复审查编译器复制的正文。

每个成品最多一次受影响可写 unit 修复并完整复审；事实、Frame、Plan、资源与编译器版本不可
修。第二次失败立即停止。两份人工审查若由同一执行端完成，只称不同范围的有界审查。

## 7. 确定性与真实模型门

在现有 pytest/Golden 增加 Writer 禁止制作字段、unit 闭世界、事实不可变、机构主张、
未披露微事件、抽象原则、D1 段级披露、编译器文字／资源闭世界、G7 不变量、非固定成稿与
legacy 兼容反证；对 Gate、事实、制作字段、资源和 G7 做 mutation proof。

确定性门通过后，只用 `deepseek-v4-flash`、`temperature=0`、`max_retries=0`，不连接业务
仓储，对 G3/G4/D1 各执行一次；每卡最多一次受影响 unit 修复与完整复审。仍失败即
`BLOCKED`，不比较模型、不重跑、不加词表或第三 Writer 策略。

## 8. 完成门与停止线

三卡通过后，才执行同 SHA 本地 G1—G7/H1/D1、完整标题／正文／辅助内容／制作建议、失败
原子性、RLS／跨作用域、历史／系列／平台父子、DM01、AIGC 与资产 `41/243/25/119`，再串行
执行 Makefile 真源、前端 lint/typecheck/interaction/build 和两份有界审查。

全部通过后才一次 push 线性历史并等待唯一 CI。CI 绿色后才以 `umask 077` 新建备份，验证
`0700/0600`、checksum、隔离恢复、RLS、对象恢复，再同 SHA 部署、连续生产验收、往返回退
和精确清理。任何生产语义失败立即停止、清理、回退并置 `BLOCKED`，不形成第二生产候选。

结构或模型阶段失败、第二修复、随机重跑、固定成稿、题材／人物／句型词表、第三模型、
`deepseek-v4-pro`、fallback、迁移、新平台或降低事实／隔离边界，均命中停止线。

## 9. 实现与确定性证据

实现提交为 `0542ec880f046204521b8446d56e34ea560990a2`。`CreativeKernelV1` 只允许
Writer 返回完整覆盖、无重复的既定可写 `unit_id + text`；额外字段、制作字段、未知／遗漏／
重复 ID 均失败。真人和商品事实由服务端形成只读 `frozen_fact` unit；假设与演绎披露由
服务端包裹，Writer 不能自行提供或修改。

Reviewer 改为只读取最终内核 unit，必须逐 unit 返回完整原文精确跨度；缺 unit、部分／虚构
跨度、uncertain、资源声明或观察类型漂移均失败。最多只修复一次受影响的可写 unit，事实
unit、Frame、Plan、资源和编译器版本不可修。

`DeliveryCompilerV1` 使用已审 unit、版本化中性短语与冻结允许资源，确定性形成现有图文／
视频 ProductionBundle；图文四张、视频 8 秒和无口播条件由编译器承接。D1 只会编译为文字
对话卡或单人分段旁白，不声明第二演员或家庭现场。最终快照原子保存
`creative_kernel_v1`、`delivery_compiler_version`、`reviewed_kernel_digest`、
visible provenance 与使用资源；失败不写半版本。

现有 pytest 新增并通过制作字段、unit 闭世界、事实不可变、隐含微事件、机构主张、抽象
原则、演绎披露、Reviewer 资源越界、编译文字／资源闭世界、G7 快照不变量与非固定成稿
反证。Gate 的恶意 intake ready 仍保持 task/run/version `0/0/0`、Writer/Reviewer
`0/0`。完整 `make golden` 为 `199 passed` 并通过 OpenAPI；`make lint`、`make
typecheck` 和 `git diff --check` 通过。首次完整 Golden 暴露离线新编译路径丢失五个既有
可见消费者，以及四图、8 秒、无口播三个制作条件承接问题；均在内核／编译器共享合同内修复，
未增加 G 卡、题材、人名或禁用词特判。

## 10. Reviewer 资格预检与停止线

预检固定实现 SHA `0542ec8…`，只使用受保护的 `deepseek-v4-flash`、
`temperature=0`、`max_retries=0`、thinking disabled；没有调用 intake、Writer、业务
repository 或数据库。三条冻结样本各调用一次 Reviewer：

| 样本 | 预期 | Reviewer 实际 | 服务端结果 | 结论 |
|---|---|---|---|---|
| A `换位思考不等于没有边界。` | `abstract_principle` | `abstract_principle` | 无 issue | `PASS` |
| B `饭桌上一句话让两个人都沉默。` | `situated_event` | `situated_event` | `unit_observation_drift` | `PASS` |
| C `笛语相信婆媳关系需要换位思考。` | `institutional_assertion` | `abstract_principle` | 无 issue | `FAIL` |

C 的完整 unit 跨度正确，但 Reviewer 把 `婆媳关系`、`相信`、`换位思考`分别提取为关系、
动机和事件后，最终仍选择 `abstract_principle`。这证明缺陷属于 Reviewer 单角色资格，
不是 Writer 输出、编译器资源或跨度解析问题。

三次调用延迟为 `1482/1381/1331 ms`，总 tokens 为 `705/700/734`，均零重试。Prompt
SHA-256 为 `4dbaebc8…`、`5eb6b50c…`、`3143add3…`；raw response SHA-256 为
`c97e7ab8…`、`7a9ac5d1…`、`fbcb0c99…`。root-only 证据位于
`/var/lib/diyu-ui09-evidence/0542ec880f046204521b8446d56e34ea560990a2/`，目录
`0700`、文件 `0600`，summary SHA-256 为
`7b803d192e772f1616ecd51d0f8f7838b18ecba21ac69d199485aa8a2551b5d4`；不含密钥或生产
私人资料。

第一次一次性容器启动把候选源码错误挂到 `/app`，在 import 阶段因虚拟环境被覆盖而失败；
模型调用为 `0`，没有消费任何资格或卡片额度。修正为 `/candidate` 只读挂载后只执行上述
三次资格调用。Reviewer 未达 `3/3`，故按停止线没有开始 G3/G4/D1，没有第二修复、随机重跑、
Prompt 补丁、`deepseek-v4-pro`、第三模型或其他供应商。

## 11. 阻断收口与生产不变

UI-09 置为 `BLOCKED`，不是 `REVIEW` 或 `CLOSED`。没有形成生产候选，没有 push、承重
CI、两份生产候选审查、备份、部署、生产 G1—G7/H1/D1 或往返回退。生产部署仓库仍干净运行
`845f63291ba5060e60f87d1afa5cfc1cdb057e3b`，镜像摘要仍为
`sha256:1171b153cbc709a760caf4a5db1fb14fe00e0bca3ef9c7b79c85f737a3a6bdb9`，
schema `20260801_28`，回环 readiness `ready`、公网 `200`、备份 timer `active`。

远端临时候选源码与 runner、本地临时 runner 与归档均已精确删除；正式 root-only 原始证据
保留。没有业务任务、运行、版本、会话、备份或凭据文件产生。RLS、历史版本、系列、DM01、
AIGC 与资产 `41/243/25/119` 因生产未改动而保持，激活增量 `0`。

尚未证明：G3/G4/D1 当前模型成品、完整本地前端门、两份候选审查、CI、生产验收与回退；
真实员工／品牌采用、真实发布、流量、排名、爆款、GMV／销售、企业采用、多真实租户市场
差异、企业 SLA 和 `20/55/44` 全组合也继续未证明。

历史下一动作已由 UI-10 的 Reviewer 证据提取与服务端事实裁决裁决取代。UI-09 内不继续补
Prompt、不改 Writer、不再次调用模型；其 Reviewer `2/3`、样本 C 假绿、G3/G4/D1 未运行、
未 push/CI/部署与 root-only 证据继续作为 UI-10 的起点证据。
