# UI-11 一般情境与真人事实隔离闭环执行包

- 当前状态：`BLOCKED`
- predecessor：`UI-10 SUPERSEDED → UI-11`
- 启动提交：`9cc96ab8e756f16add2179e0add9e51d3447445a`
- `origin/main`：`7aa87ab624cf3ff64f42e49f1755d66d496cac7a`
- 本地归档引用：
  `refs/tags/archive/ui-10-blocked-20260728=9cc96ab8e756f16add2179e0add9e51d3447445a`
- 生产安全锚点：`845f63291ba5060e60f87d1afa5cfc1cdb057e3b`
- schema：`20260801_28`
- 唯一执行端：当前 WSL 执行端。
- 目标终态：全部完成门真实成立后进入 `REVIEW`，不得自行 `CLOSED`。

## 1. 真实缺口与产品合同

UI-10 已证明 Reviewer exact evidence 与服务端事实裁决能够拒绝无依据机构主张和未标识
微事件；其 G3 阻断也证明现有 abstract-only 单元合同把题材型内容压缩成纯观点，无法合法
承载一般创作情境。

UI-11 不修改 Writer Prompt 来遮盖该缺陷，也不放宽真人事实边界。用户给出题材和限制已经
足以直接创作；系统自主选择纯观察或“观察＋明确假设情境”。没有真人事实时可创造一般、
假设性人物与动作，但不得绑定用户、真实家庭、品牌、员工、顾客、门店事件或经营历史。
真人事实仍由服务端逐字冻结；明确情境演绎仍走 dramatization。

## 2. 唯一最小结构修复

复用 CreativeKernelV1，只增加服务端所有、版本化且封闭的叙事程序：

- `observation_only_v1`
- `observation_with_hypothetical_example_v1`

第二种程序按可见顺序冻结抽象开篇、服务端假设范围、Writer hypothesis 情境和抽象收束。
Writer 仍只返回既定 `unit_id + text`；服务端使用一条版本化中性短语包裹 hypothesis。
Reviewer 必须审查包裹后的最终可见 clause，DeliveryCompiler 只能投影审查通过的文字。
该程序不得扩展为自由 DSL、题材表、人物表或第二套工作流。

## 3. 裁决与资源边界

- abstract 单元出现人物微事件时失败；
- hypothesis 只有在服务端完整可见范围内才允许一般虚构人物、动作与关系；
- hypothesis 绑定用户、真实家庭、品牌、真实员工、顾客、门店或经营历史时失败；
- 无依据机构主张继续失败，constraint 不能作事实许可证；
- 商品、品牌和真人事实只能来自 frozen fact，且逐字不变；
- 假设人物、场地、道具和声音不是生产资源；
- DeliveryCompiler 资源不足时只使用文字卡、旁白或已登记的一人条件；
- 新任务冻结 program id、Frame、事实单元、来源、reviewed digest 与 compiler version；
- 旧快照不回填、不改写，按既有 legacy 路径读取、修改、复制、导出、系列续写与跨平台改编。

## 4. 验证与停止

现有 pytest/Golden 直接覆盖纯抽象、未标识微事件、服务端包裹 hypothesis、删除标识、真实
主体绑定、机构主张、资源污染、Reviewer A/B/C、UI-09 C 重放、G4 事实不可变、G7
program/Frame/fact/source/compiler 不变量、旧快照兼容和非固定成稿。对假设标识、真实主体、
资源污染和 G7 执行实际 mutation proof。

确定性门通过后冻结一个功能 SHA，只用当前 `deepseek-v4-flash`、温度 0、零重试且无
repository／数据库写入，依次执行 G3、G4、D1。G3 最多一次受影响可写单元修复；仍失败
立即 `BLOCKED`，不重跑、不换模、不形成第二策略。

三卡通过后才运行同 SHA G1—G7/H1/D1、仓库完整门、前端门与两份有界审查。全部通过后才
允许一次 push、唯一 CI、root-only 备份、同 SHA 部署、连续生产验收、回退与清理。

## 5. 启动现场

本地 HEAD、origin 和线性领先数与任务给定一致，启动工作树干净；UI-07—UI-10 历史完整。
生产部署仓和镜像仍为 `845f632…`，镜像摘要
`sha256:1171b153cbc709a760caf4a5db1fb14fe00e0bca3ef9c7b79c85f737a3a6bdb9`，
schema `20260801_28`，回环 `/health/ready` 返回 ready，生产主机经公开 DNS/HTTPS 返回
`200`，备份 timer `active`。本执行环境直连公网请求超时，已如实记录；没有据此改变生产
结论，也没有任何生产写入。

## 6. 确定性实现与反证

服务端已实现两个封闭 program id。无冻结事实的 `general_observation` 选择
`observation_with_hypothetical_example_v1`；带真人／商品／品牌事实、显式 hypothesis 或
dramatization 仍使用 `observation_only_v1`。修改和重编译必须保留既有 program id。
假设程序冻结开篇、假设示例和收束三个 body 单元，服务端使用“假设有这样一幕：”包裹
示例后才交给 Reviewer；program id 与单元形状不一致时失败关闭。

Reviewer 仍只提取 exact evidence。服务端允许已完整包裹 hypothesis 中的一般虚构动作，
但拒绝第一人称现实占有、当前门店、明确真实员工／顾客以及受保护机构主体。该识别仅使用
极小的指示／机构语法类和当前可信主体，不含题材、人物或失败句表。DeliveryCompiler
按可见顺序投影多个已审 body 单元，资源仍只能来自冻结允许集合。

确定性证据：

- `make lint`：通过；
- `make typecheck`：94 个源文件通过；
- `make golden`：235 项通过；
- UI-11、UI-10、UI-09、DeepSeek adapter 和 UI-06 API 直接回归：82 项通过；
- 删除服务端假设标识后，对应反证变红；
- 绕过真实主体绑定后，三项现实绑定反证变红；
- 将假设人物注入制作资源后，资源闭世界反证变红；
- 修改时不再保留原 program 后，G7 program 不变量反证变红；
- 四处故意 mutation 均已恢复，恢复后的 82 项直接回归再次全绿。

这些是本地确定性证据，不是实际模型、CI 或生产证明。

## 7. 无持久化 G3 真实预检

功能候选为 `6b4a50a39669a67cb62ce7b03c50fe26e0800156`。在 ECS 隔离、只读候选源码
容器中只使用 `deepseek-v4-flash`、`temperature=0`、`max_retries=0`、thinking disabled，
未连接 repository、数据库、对象存储或业务 API，也没有生成 task、run、version、session
或生产备份。

G3 按允许上限执行初稿、Reviewer、一次 affected-unit 修复和完整复审，共四次模型调用。
Writer 初稿形成完整六单元候选，hypothesis 由服务端可见包裹：

> 假设有这样一幕：

假设单元中的婆婆／媳妇、早餐和动作没有绑定用户、品牌、员工、顾客或门店，也没有成为制作
资源。Reviewer 两轮均完整覆盖全部 clause，exact text 与 span 真实存在，
`uncertain=false`。Raw evidence 的关键观察为：

- 服务端标识 clause 被提取为 `implicit_subject=current_speaker`、谓词“假设”；
- 抽象开篇中的泛指关系判断被提取出原因“是因为一开始就期待太高”和动作“强行亲密”；
- 抽象收束中的泛指建议被提取出动作“尊重彼此的生活习惯”“不要求对方改变”；
- hypothesis 内的具体动作均位于完整服务端范围内，未产生真实主体绑定。

服务端最终初审和复审都产生五项 issue：

1. 把服务端自己的假设标识误判为 `unsupported_institutional_assertion`；
2. 把上述四段泛指原则／建议证据一律判成 `situated_event_in_observation`。

唯一修复请求覆盖开篇、hypothesis 和收束三个可写单元；模型返回三单元原文，三个单元均未
改变。完整复审得到同样五项 issue，最终
`GenerationFailed("内容边界无法在一次 CreativeKernel unit 修复内满足")`。

这次失败不是未标识 hypothesis、真实主体绑定、Reviewer 漏审、虚构跨度、uncertain、
资源污染或机构事实放宽。真实偏差是服务端裁决仍把“服务端所有的范围标识”和“泛指
原则／建议中的动作词”当成现实事实信号；确定性夹具没有覆盖真实 Reviewer 对服务端标识
返回 `current_speaker` 的合法证据形态。

四次调用延迟为 `4756/10610/2031/10478 ms`，tokens 为
`1201/3033/799/3035`，全部零重试。输入 SHA-256 为
`b4274c2aa5c825803633249c12d2ec2f54e5b0bd01432b547ea9867b4bbeb590`；
四份 raw response SHA-256 分别为
`81113951…/2e33df45…/4180ad18…/3a064f92…`。root-only 证据目录为
`/var/lib/diyu-ui11-evidence/6b4a50a39669a67cb62ce7b03c50fe26e0800156/`，
目录／文件为 `0700/0600 root:root`，summary SHA-256 为
`548d0b4a71b311bd96e3ddfcba95c5ecedabd71dac3c4fa5e54b79ec67a77881`。
证据不含密钥或生产私人资料。

## 8. 停止线与未执行项

G3 在唯一修复和完整复审后仍未通过，UI-11 因而为 `BLOCKED`，不是 `REVIEW` 或
`CLOSED`。没有随机重跑、第二修复、Prompt 补丁、Writer 策略变更、模型比较、第二
Reviewer 或 fallback。G4、D1 与完整 G1—G7/H1/D1 未开始；最终前端门、两份候选审查、
push、CI、备份、部署、生产卡片、人工成品终审和往返回退均未执行。

临时 runner、候选源码、归档、模型凭据投影和本地分析副本已精确删除；正式 root-only
失败证据保留。生产没有修改，仍运行 `845f632…`、schema `20260801_28`；本轮没有产生
业务数据。RLS、历史版本、系列、DM01、AIGC 和资产 `41/243/25/119` 只获得本地 Golden
不回退证据，不能冒充 UI-11 生产证明。

仍未证明：G3 合法 V1、G4/D1、完整 G1—G7/H1/D1、CI、同 SHA 生产验收、回退，以及真实
员工／品牌采用、真实发布、流量、排名、爆款、GMV／销售、企业采用、跨真实租户市场差异、
企业 SLA 和 `20/55/44` 全组合。

唯一下一动作：主控只裁决一个**服务端裁决单角色 successor**，使服务端所有的假设标识不被
当作机构主张，并以确定性结构区分泛指原则／建议和已发生微事件；不得在 UI-11 重跑 G3、
修改 Writer 策略、换模型或增加 Reviewer。
