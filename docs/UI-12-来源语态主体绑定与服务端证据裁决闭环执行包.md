# UI-12：来源、语态、主体绑定与服务端证据裁决闭环执行包

> 状态：`ACTIVE`
> 启动日期：2026-07-28
> 唯一执行端：当前 WSL 主仓写入执行端
> 规范真源：[ADR-028](架构决策/ADR-028-来源语态主体绑定与证据裁决矩阵.md)
> 诊断真源：[UI11-D1](UI-11-语义裁决收敛诊断.md)

## 1. 状态交接与起点

- UI-11：`SUPERSEDED → UI-12`。UI-11 的 `BLOCKED`、G3 初稿和唯一修复失败、
  Reviewer evidence、G4/D1 未运行以及未 push/CI/部署事实完整保留；
  `SUPERSEDED` 不表示 UI-11 成功。
- UI-12：`ACTIVE`。启动前规范性检查确认 SDR-001—SDR-042 恰好 42 个唯一 ID，
  每行只有一个 `unit_contract`，recommendation 与歧义裸动作不存在冲突。
- 启动现场：local `13f7b667fe777bb3aa784618b86ed96cab4b4a59`，
  origin/main `7aa87ab624cf3ff64f42e49f1755d66d496cac7a`，本地领先 15 个线性提交，
  工作树在规范订正前干净。
- 生产只读现场：部署仓和镜像均为 `845f63291ba5060e60f87d1afa5cfc1cdb057e3b`，
  镜像摘要
  `sha256:1171b153cbc709a760caf4a5db1fb14fe00e0bca3ef9c7b79c85f737a3a6bdb9`，
  应用数据库 `diyu_m5_4` 的 schema 为 `20260801_28`，容器回环与生产主机公网
  `/health/ready` 均为 `200`，`diyu-m5-4-backup.timer` 为 active。
- 现场偏差：真实部署仓为 `/opt/diyu-saas/repo`，应用容器监听 `127.0.0.1:18000`，
  PostgreSQL 容器为 `diyu-infra-postgres-1`，应用数据库为 `diyu_m5_4`；不得继续使用
  旧路径、旧数据库、`/readyz` 或 `diyu-backup.timer`。

## 2. 唯一结果

建立单一、服务端所有的 clause 权限链：

```text
NarrativeFrame + program + unit skeleton + fact kind + speaker kind
→ ClauseContextV2
→ occurrence-aware ReviewEvidenceV2
→ 服务端 subject / contract / grammatical evidence 裁决
→ 最多一次合法 affected-unit 修复
→ DeliveryCompilerV1
```

Writer 继续只返回 `unit_id + text`；Reviewer 继续只提取 evidence；DeliveryCompiler
继续只投影已审文字、中性短语和登记资源。模型、Writer 策略、事务边界与生产职责不改变。

## 3. 实现合同

1. `ClauseContextV2`
   - 保存 `clause_id/unit_id/exact_text/visible_order/text_source/unit_contract/speaker_kind/fact_ref`；
   - 每个 clause 恰好一个 contract；
   - program validator 与 semantic reconcile 共同消费同一 sidecar；
   - Writer、Kernel 自报类型和 Reviewer 类型均无授权权；
   - 未知 program/unit 或映射缺失结构失败。
2. `ReviewEvidenceV2`
   - 每个 span 使用 `text/start/end`，索引为 Python Unicode 字符位置；
   - 每个 clause 内增加 `grammatical_marker_spans.modality/aspect`；
   - 漏、重、额外 clause、原文/顺序/offset 不一致、伪 span 或 uncertain 均失败关闭；
   - V1 只作 legacy/历史 raw 读取，不回填。
3. speaker kind
   - 只接受 `institutional_account/personal_ip_account/unknown`；
   - 来自 ContentRole 结构化真源并冻结进新任务快照；
   - 旧行与旧快照保持 unknown，禁止按名称或画像散文猜测；
   - 个人 IP 可表达当前观点/建议，不能无事实声称经历、已发生、长期做法或具体对白。
4. 服务端裁决顺序
   - sidecar → evidence 资格 → wrapper/frozen fact 结构 → writer-owned subject →
     单值 contract + grammatical/event evidence → compiler provenance/resource；
   - frozen fact 和 server wrapper 不被普通语义规则二次否决；
   - `insufficient_evidence` 及结构、来源、事实、program、compiler、resource 错误不得送
     Writer 修复。

## 4. 确定性门

- 离线重放 16 个历史 raw bundle、20 份 raw response；UI-07 四个缺失 raw 只登记状态，
  G1 只重放恶意-ready seam。
- 为 SDR-001—SDR-042 建立直接消费者，覆盖 wrapper、三类 frozen fact、抽象原则、
  泛指因果、建议、完成体、条件、已发生事件、机构/个人 IP、H1/D1、evidence 错误、
  资源闭世界及 G4/G7。
- 四句最低边界：
  - “婆婆要尊重儿媳。”→ recommendation allow；
  - “婆婆尊重了儿媳。”→ reject；
  - “如果婆婆愿意先停一下。”→ hypothesis allow；
  - “昨天婆婆停下来了。”→ reject；
  - “婆婆尊重儿媳。”→ insufficient_evidence。
- 六项 mutation proof：丢 source、信任 Kernel contract、缺席即安全、去 offset、放宽
  机构/个人现实绑定、把 insufficient 送修复。错误实现必须变红，恢复后转绿。

## 5. 模型与本地业务门

确定性门全绿后，固定单一功能 SHA，只使用当前 DeepSeek、temperature 0、
max_retries 0、无 repository/database：

1. Reviewer V2 资格：UI-10 A/B/C、四句与歧义句、wrapper current-speaker 反证、
   个人 IP 正负、coverage/伪 span/重复 occurrence/uncertain；
2. 依次一次执行 G3、G4、H1、D1；仅语义违规可使用一次既有 affected-unit 修复；
3. 资格或 evidence 失败立即 `BLOCKED`，不改 Writer、不换模型、不重跑。

四卡通过后，使用正式 API 与本地 PostgreSQL 连续执行 G1—G7/H1/D1，并验证失败原子性、
RLS、作用域、历史/系列/平台父子、DM01、AIGC、legacy 与资产 `41/243/25/119`。

## 6. 工程门、CI 与生产

串行执行 `git diff --check`、`make lint`、`make typecheck`、`make golden`；若 Golden
已完整包含 pytest，不机械重复 `make test`；并执行前端 lint、typecheck、interaction、build。
只做两份有界审查：产品与内容语义；工程、安全、兼容与原子性。

全部本地门通过后才允许一次 push 保留线性历史并触发唯一承重 CI。CI 绿色后：

1. `umask 077` 创建 root-only 新鲜备份并验证 0700/0600、checksum、隔离恢复、RLS、
   对象恢复和 readiness；
2. 部署 CI 对应同一 SHA；speaker kind 只允许 expand-first，不 downgrade 数据库；
3. 连续执行生产 G1—G7/H1/D1，人工读取全部正文和制作字段；
4. 完成旧健康镜像的不降级数据库往返回退；
5. 精确清理验收任务、运行、版本、会话与临时材料。

## 7. 停止线与完成

只有矩阵无法表达新的产品语义，或只能依赖关键词表、第二模型/Reviewer、fallback、随机重跑、
第二修复、固定成稿或降低事实/隔离边界时，才置 UI-12 `BLOCKED`。同一矩阵内的普通服务端
算法错误在 UI-12 内修正和重放，不再按失败句拆 successor。

所有离线、确定性、模型、本地 API/PostgreSQL、工程门、两审、CI、生产、备份、回退和清理
同时成立后，UI-12 进入 `REVIEW`，不得自行 `CLOSED`。

当前唯一下一动作：实现 `ClauseContextV2`、ReviewEvidenceV2、speaker kind 与服务端裁决。
