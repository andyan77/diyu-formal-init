# M7-1 · 真实门店 DM01 最小闭环执行包

- 状态：`REVIEW`
- 起点：M6-3 已诚实置为 `SUPERSEDED`，后继指向 M7-2B；M7-1 与内容生产解耦
- 当前唯一执行端：本 WSL 执行端
- 工程原则：遵循 `AGENTS.md` 与
  `docs/工程规范/UNIVERSAL_ENGINEERING_EXECUTION_STANDARD.md`
- 业务上游：M4-1、M4-2、M5-K、M5-4、M6-1、M6-2、ADR-018

## 一、唯一用户结果

只完成：

> 笛语服饰一个门店 × 一组双层墙面挂杆 × 一次任务库存输入
> → 确定性文字**参考执行方案** V1
> → 用户自然提出实际差异时，只修订受影响部分形成 V2，并保留 V1。

系统只交付参考建议，不自动执行陈列，也不承担监管责任。没有修改意见时停在 V1 即为正常
完成；真实现场采用与效果不是 M7-1 的软件验收条件。

## 二、固定边界

- 不调用 DeepSeek；
- 不消费 P1—P5、发布账号或 `ContentRole`；
- 不接 ERP/POS，不核验库存；
- 不识图、不评分、不巡检、不下整改任务；
- 不扩展橱窗、中岛、入口、模特、全店空间或其他陈列场景；
- 不建设通用陈列优化器、LLM 陈列路径、审批状态机或第二编译器。

租户、品牌、门店、执行组织和登录操作人必须来自可信服务端作用域。账号与数据访问权限是
**一次性的系统资格**，不是每次生成任务的授权流程。任务库存只在本次任务内成立；方案是
参考建议，不表示总部批准、系统核验或门店已经执行。

## 三、产品语义裁决（2026-07-25，取代原确认流程）

用户裁定：系统根据租户、品牌、账号、用户输入和可用知识交付最佳参考建议。因此：

- 不需要用户授权系统“给建议”；
- 不需要确认人、确认日期、系统代录人、复核人；
- 不设“业务指定／系统建议”二选一；
- 不要求用户实际执行后回复，才承认 V1 成立；
- 后台自动记录真实登录操作者、作用域、输入、版本和时间，但不展示成用户任务负担。

被撤销的表述（原写在本文件、`MILESTONE.md`、候选资料和项目记忆中）：**阿丹已现场确认、
现场差异为无、商品当前在店且可售、`2026-07-25` 为现场确认日期、`007+006` 与 `005` 已被
确认为业务指定、`confirmed_by` / `confirmed_at` / `system_submitted_by` 等业务确认语义。**
它们没有可靠用户来源，不得继续作为真实事实引用；原 V1 保留为历史开发证据，但不再称为
“阿丹现场确认后的真实门店方案”。

## 四、当前输入口径

- 当前资料统一定义为：**用户提供的本次任务快照，用于生成参考方案；其中现场条件和库存
  仍未证明为真实现场事实。**
- `007+006` 是系统根据本次资料形成的主焦点建议，`005` 是较弱回应建议；只有用户在自然
  输入中明确写出“必须、固定、不可改变”等表达时，才在本次任务内成为硬要求。
- 门店固定结构（上下杆、容量、正挂位、黄金视线、来客方向、硬限制）与本次任务数据
  （主题、密度、焦点建议、商品轻量属性、数量）分开保存；本次库存不写成长期真实库存，
  本次焦点建议不写成长期品牌政策。
- 不再用旧库存快照做 `enforce_exact` 比对阻止新任务：新的自然输入即是本次真源。
- 生产 `dm01-rule-compiler-v1` 保持同一确定性执行器和上下/左右/焦点/库存守恒合同；
  真实路径不查询 `ZX-*`，`ZX-*` 只保留在合成回归夹具中。

## 五、缺口处理与版本规则

1. 信息足够时直接生成；
2. 非关键缺失（缺主题、缺焦点建议商品、个别商品没有陈列属性资料）通过收窄方案处理，
   并在正文说明；
3. 只有缺少无法替代的 SKU 或数量、导致方案无法成立时，才允许**一次合并的最小追问**；
   追问不得涉及确认人、授权或责任归属；
4. V1 是正常完整成果；没有修改意见时停在 V1 即为正常完成；
5. 用户自然提出实际差异或修改要求时，才生成局部 V2 并保留 V1；“现场无差异”只保留 V1，
   不为状态流转强造 V2。

## 六、本次实现（数据驱动、无确认语义）

1. 迁移 `20260725_18`：`display_stores` 增加 `current_task_input`；把旧
   `rail_profile.inventory_snapshot` 迁入本次任务输入，并删除旧 `confirmation` 块；
2. `src/infrastructure/dm01_task_input.py`（原 `confirmed_dm01.py`）：只写门店结构与
   本次任务输入，不写 `display_policies`、不写品牌长期商品、不记录确认人/代录人；
3. `src/composition/activate_dm01_task_input.py`（原 `activate_confirmed_dm01.py`）：
   每次运行都生成当前参考方案，不再用旧快照短路；
4. 编译器：焦点建议不可用时按确定性规则重新选择主焦点（`focus_source=system_narrowed`），
   没有陈列资料的商品只计入库存对账；`parse_hard_requirements` 只从本次自然输入识别
   “必须/务必/固定/不可改变”类硬要求；
5. 可见正文：标题为“{门店}墙面挂杆参考执行方案”，删除确认人、确认日期、代录人和授权
   字段，间距口径改为“侧挂保持正常可抽取间距，主正挂两侧各留约一个衣架宽的视觉边界。”

## 七、当前参考方案 V1（修正后 SHA，本地端到端运行）

在修正后的代码上以真实任务输入
[`config/task_inputs/diyu_clothing_keqiao_dm01_v1.json`](../config/task_inputs/diyu_clothing_keqiao_dm01_v1.json)
通过 `src.composition.activate_dm01_task_input` 完成一次端到端运行（本地 PostgreSQL，
非生产 ECS）：

- 执行器 `dm01-rule-compiler-v1`，`provider_usage=null`，未调用 DeepSeek；
- DM01 任务/运行/版本 `1/1/1`，`display_policies=0`、`brand_products=0`；
- 30 件任务库存中 18 件上墙、12 件不上墙，逐项守恒；
- `rail_profile` 只剩结构键，`current_task_input.source=user_task_snapshot`（11 项商品）；
- `input_receipt.operator` 为真实登录操作者，不含 `field_executor` / `submitted_by`。

方案正文（节选自该次运行；“本次操作人”一栏渲染的是真实登录操作者，本地运行中为该本地
夹具管理员，取证后已随探针数据一并清理）：

> 笛语柯桥店墙面挂杆参考执行方案
>
> 适用范围：笛语服饰｜笛语柯桥店｜浙江分公司；本次操作人 笛语服饰负责人。
>
> 主题：夏末初秋·自然机能与城市松弛。密度：中低密度；只做一个主焦点和最多一个较弱回应。
>
> 本次任务库存共 30 件；建议 18 件上墙，12 件不上墙。上杆、下杆分别不超过 16 / 14 件舒适容量。
>
> 来客主要阅读顺序：右侧 → 中间 → 左侧。黄金视线：上杆右侧约三分之一，是从右侧进入时的第一视觉落点。
>
> 右侧（主焦点）：上杆 女童自然度假白色连衣裙（DIYU-CSPU-006）×1（正挂）、女童浅绿机能叠穿马甲
> （DIYU-CSPU-007）×1（与本组主件叠穿同一正挂）、女童浅绿机能叠穿马甲（DIYU-CSPU-007）×1（侧挂）；
> 下杆 明确留空。
>
> 中间（中性承接）：上杆 女童灰色松弛针织开衫（DIYU-CSPU-008）×2（侧挂）、男童海岸休闲牛仔外套
> （DIYU-CSPU-011）×2（侧挂）、男童灰色自然工装宽松短袖（DIYU-CSPU-002）×2（侧挂）、男童藏蓝街头
> 工装宽松短袖（DIYU-CSPU-003）×2（侧挂）、男童亮黄城市工装宽松短袖（DIYU-CSPU-001）×1（侧挂）；
> 下杆 男童工装长裤组（DIYU-CGRP-001）×2（侧挂）、儿童宽松牛仔裤组（DIYU-CGRP-002）×2（侧挂）、
> 儿童棕色松弛长裤组（DIYU-CGRP-003）×1（侧挂）。
>
> 左侧（较弱回应）：上杆 男童深蓝轻学院长款衬衫（DIYU-CSPU-005）×1（正挂）；下杆 明确留空。
>
> 下杆留空：左侧、右侧；用于避开长款上下重叠、立柱影响或保留补货操作空间。
>
> 正挂、侧挂与间距：侧挂保持正常可抽取间距，主正挂两侧各留约一个衣架宽的视觉边界。
>
> 这是系统根据本次任务输入给出的参考建议，门店自行决定是否采用；系统不代表总部批准，
> 不核验库存，也不表示现场已经执行。

## 八、验证边界与结果

| # | 边界 | 结果 |
|---|---|---|
| 1 | 当前任务快照生成库存守恒的完整 V1 | 通过（端到端运行 + `test_task_snapshot_runs_end_to_end_and_a_new_inventory_is_never_blocked`） |
| 2 | 用户可见成品不出现确认、授权、代录、业务指定二选一 | 通过（正文禁词断言） |
| 3 | 新库存输入不被旧快照阻断 | 通过（改动数量、减少 SKU、加入无资料 SKU 均直接生成） |
| 4 | 一条受控自然反馈生成局部 V2 并保留 V1 | 通过（反馈显式标注“测试夹具·非真实门店反馈”） |
| 5 | 无反馈时 V1 正常结束 | 通过（不存在 V2，读取 V2 失败关闭） |
| 6 | 匿名、错误租户/品牌/门店作用域失败关闭 | 通过（`401`/`403`、错误作用域读不到上下文、跨租户读取报错） |
| 7 | 失败不产生半版本 | 通过（`test_a_failed_generation_leaves_no_half_version`：版本 `0`，运行 `failed`） |
| 8 | 既有 lint / typecheck / golden / test / 前端检查 / 唯一远程 CI | 通过；本地 Ruff、mypy、Golden/OpenAPI、后端 `94 passed`、前端 lint/typecheck/build 绿色，唯一远程 CI run `30153302046`（实现提交 `3bf07d3`）绿色 |

未部署项：本轮纠偏实现尚未上生产 ECS。当前执行环境没有 `.env`、SSH 私钥和外网通道，
无法登录 `8.217.175.36`；生产切换需在具备凭据的环境按既有 `deploy/deploy.sh` 完成，并包含
`alembic upgrade head`（迁移 `20260725_18`）与一次
`docker compose --profile maintenance run --rm dm01-task-input`。

## 九、停止线

- M7-1 收口进入 `REVIEW` 后立即停止，不启动 M7-2A/M7-2B；
- 不再请求用户或门店人员确认、授权或现场回复；
- 真实现场执行、采用、陈列效果和销售结果尚未证明，如实记录，但不构成 M7-1 软件验收阻断。
