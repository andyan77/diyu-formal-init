# M7-1 · 真实门店 DM01 最小闭环执行包

- 状态：`ACTIVE`
- 起点：M6-3 已诚实置为 `SUPERSEDED`，后继指向 M7-2B；M7-1 与内容生产解耦
- 当前唯一执行端：本 WSL Codex
- 工程原则：遵循 `AGENTS.md` 与
  `docs/工程规范/UNIVERSAL_ENGINEERING_EXECUTION_STANDARD.md`
- 业务上游：M4-1、M4-2、M5-K、M5-4、M6-1、M6-2、ADR-018

## 一、唯一用户结果

只完成：

> 笛语服饰一个真实门店 × 一组真实双层墙面挂杆 × 一次真实库存输入
> → 确定性文字陈列方案 V1
> → 门店人工执行
> → 自然反馈现场差异
> → 只修订受影响部分形成 V2，并保留 V1。

没有真实门店执行反馈，不得称为闭环完成或进入 `REVIEW`。

## 二、固定边界

- 不调用 DeepSeek；
- 不消费 P1—P5、发布账号或 `ContentRole`；
- 不接 ERP/POS，不核验库存；
- 不识图、不评分、不巡检、不下整改任务；
- 不扩展橱窗、中岛、入口、模特、全店空间或其他陈列场景；
- 不建设通用陈列优化器、LLM 陈列路径或新的审批状态机。

租户、品牌、门店、执行组织和自然操作人必须来自可信服务端作用域。人工库存只在本次任务
内成立；方案是内部执行建议，不表示总部批准、系统核验或门店已经执行。

## 三、当前现场真值

2026-07-25 只读核对生产数据库：

- 真实租户：笛语服饰；
- 当前品牌：笛语服饰；
- 当前组织只有“笛语服饰管理组织”，不能冒充真实门店、门店执行组织或实际执行人；
- 已确认品牌商品：`0`；
- 陈列政策：`0`；
- 真实门店挂杆档案：`0`；
- 陈列任务、运行、版本：`0/0/0`。

现有 `review_candidate` 只给出十个待确认字段，没有可升级为事实的具体值。生产
`dm01-rule-compiler-v1` 仍把六个 `ZX-*` 合成商品、A/B/C 固定布局及唯一马甲反馈写死在
编译器、输入解析、结构合同和可见正文中。它只证明“折线之间”合成黄金闭环，不能用于
笛语服饰真实路径。

因真实门店、挂杆和商品资料均未确认，本轮不猜造数据，也不先做脱离真实输入的通用优化器。
数据确认后，最小改造必须让真实品牌路径只读取当前租户的已确认门店、挂杆条件、商品事实
和本次数量；`ZX-*` 仅保留在合成回归夹具中。

## 四、一次合并确认节点

请品牌方一次用自然语言回复下面四组信息。无需填写完整门店档案、精确厘米表、灯光档案、
视频或全店陈列资料；大致容量、左右方向和实际库存清单已经足够。现有库存表可以直接粘贴；
若在当前协作窗口附文件，由执行端读取整理，不要求品牌方重填技术字段，也不把它描述成
生产系统已经具备的库存文件上传能力。

1. **门店和执行人**
   - 真实试点门店名称：（未提供）
   - 门店所属实际组织：（当前只有“笛语服饰管理组织”，请确认真实门店所属组织；不能默认沿用）
   - 这次实际照方案挂货的人：（未提供）
2. **这组双层墙面挂杆**
   - 顾客面对墙时，上杆和下杆怎样分布：（未提供）
   - 上杆、下杆各自舒适容量大约多少件：（未提供）
   - 固定正挂位有几个、分别在哪里：（未提供）
   - 黄金视线区在哪里；上杆是否就是黄金视线区：（未提供）
   - 顾客通常从左边还是右边进入这组墙：（未提供）
   - 现场不能改变的限制，例如柱子、消防、插座或取货空间：（未提供）
3. **笛语服饰对这组墙的最小陈列表达**
   - 待确认候选：一个主焦点 + 最多一个较弱回应；
   - 待确认候选：中等偏低密度、焦点清楚、商品关系可读；
   - 待确认拒绝项：多个强焦点同时争夺注意、只按SKU/品类堆仓或用高密度遮盖商品关系；
   - 色彩、轮廓和上下呼应由本次真实商品决定，不要求品牌方先填抽象规则。
4. **本次真实可用商品**
   - 每款商品的真实名称或内部编号及件数：（未提供）
   - 每款用于本次陈列所必需的最小事实，例如品类、主要颜色/轮廓、厚薄或必须正挂的限制：
     （未提供；不需要补与本次挂杆无关的完整商品档案）

上述内容一次确认或修改即可；第3组可以只回复“接受候选表达”或直接改一句。不得拆成多轮
技术问题，不得自行把 `ZX-*`、南城店或未确认候选值填入真实租户。

## 五、数据确认后的最小施工

1. 以已确认的真实门店、执行组织、挂杆档案和商品事实建立当前租户数据；
2. 将 DM01 编译器改为只使用当前上下文和本次库存，保留确定性规则和结构合同；
3. V1 必须明确上下、左右、黄金视线区、焦点、商品分组、数量、间距、替代和执行步骤；
4. 门店实际执行后，只接收一段自然反馈；
5. V2 只修改反馈影响的区域或条件，V1 保持可读取；
6. 核验真实品牌路径不读取 `ZX-*` 合成商品，双应用和租户/品牌/门店隔离保持成立。

## 六、停止线

- 当前停止于上述一次合并确认节点，M7-1 保持 `ACTIVE`；
- 数据确认后才允许进行最小数据驱动改造和真实 V1；
- 没有真实执行反馈不得冒充 V2 或闭环；
- 真实 V1→执行反馈→局部 V2 成立后进入 `REVIEW` 并立即停止，不启动 M7-2。
