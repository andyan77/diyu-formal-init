# 导入校验报告 v0.1

- 日期：2026-07-22
- 双应用资产：243
- 双应用夹具：123
- 双应用来源：62
- 新增陈列资产：73
- 阻断错误：**0**
- 非阻断提醒：**4**

## 1. 校验结果

| 检查 | 严重度 | 结果 | 数量 | 说明 |
|---|---|---|---:|---|
| asset_id 全局唯一 | `ok` | `pass` | 0 |  |
| fixture_id 全局唯一 | `ok` | `pass` | 0 |  |
| source_id 全局唯一 | `ok` | `pass` | 0 |  |
| 资产必填字段与条件字段 | `ok` | `pass` | 0 |  |
| basis_refs 来源完整性 | `ok` | `pass` | 0 |  |
| gold_fixture_refs 完整性 | `ok` | `pass` | 0 |  |
| related_asset_ids 完整性 | `ok` | `pass` | 0 |  |
| category 值域 | `ok` | `pass` | 0 |  |
| asset_type 值域 | `ok` | `pass` | 0 |  |
| lifecycle status 值域 | `ok` | `pass` | 0 |  |
| G 类不伪装为 P1—P5 消费者 | `ok` | `pass` | 0 |  |
| G 类声明 DM01 消费合同 | `ok` | `pass` | 0 |  |
| 陈列 H 规则适用 DM01 | `ok` | `pass` | 0 |  |
| 黄金库存与方案算术 | `ok` | `pass` | 0 |  |
| 来源注册字段 | `ok` | `pass` | 0 |  |
| ADR-018 实际文件路径绑定 | `warning` | `pending` | 1 | 当前运行目录未提供 ADR-018 文件本体；本包依据用户确认摘要，项目仓合并时需绑定实际路径。 |
| G 类专业激活复核 | `warning` | `pending` | 46 | 32 条 G 知识与 14 条陈列方法仍为 review_candidate，需要零售陈列专家和真实门店执行样本复核。 |
| D-016 物理实现 | `warning` | `deferred` | 1 | DisplayArtifact 字段、表/JSON/投影、事件持久化和状态机未由本包冻结。 |
| 历史项目状态同步 | `warning` | `pending` | 1 | 旧 TODO/项目记忆可能仍显示 D-005 待决或 P5 4/5；需由项目维护者依据最新 ADR 同步。 |
| 夹具注册字段 | `ok` | `pass` | 0 |  |

## 2. 关键统计

- 类别分布：{'A': 22, 'B': 26, 'C': 19, 'D': 16, 'E': 28, 'F': 8, 'G': 46, 'H': 78}
- 资产类型分布：{'domain_knowledge': 99, 'generation_method': 58, 'industry_role_template': 8, 'evaluation_rule': 78}
- 新增陈列规则类型：{'hard_gate': 11, 'quality_criterion': 8, 'offline_test': 8}
- 新增陈列规则执行范围：{'online_lightweight': 19, 'offline_gold': 4, 'offline_regression': 4}

## 3. 实际完成的归一化

- 双应用合并文件统一标记为 `diyu_global_asset_v0.2`；未改写第一至五批业务语义。
- 新增类别 `G`，用于零售经营陈列知识与编译方法。
- G 类通过 `supported_display_contracts` 声明消费者，不伪装成 P1—P5。
- 陈列方法继续沿用 `asset_type=generation_method`，增加 `method_family=display_merchandising`，避免破坏 v0.1 资产类型。
- H 规则的 `applies_to` 允许引用 P1—P5 或 `DM01_wall_hanging_rail_execution`。
- 原有 8 个外部黄金桩允许 `input_or_change` 为空，因为其详细输入保留在项目原文件；本包不猜补。

## 4. 黄金算术核对

- 本次人工库存合计：20 件；
- 首次方案合计：15 件；
- 首次未上墙：5 件；
- 自然反馈后修订：ZX-V113 2→1；
- 修订方案合计：14 件；
- 所有 SKU 使用数量均未超过用户输入。

## 5. 非阻断提醒解释

### ADR-018 路径绑定

当前运行目录没有 ADR-018 文件本体。本包只使用用户本轮提供的确认摘要；项目仓正式导入时必须把 `PRJ-ADR-018` 绑定到实际文件，不能把本包当作替代 ADR。

### G 类专业复核

结构校验通过只证明数据形态、引用和黄金边界一致，不证明 32 条 G 知识已经成为行业硬标准。确定性条目需要口径复核，条件性规律需要真实门店反例与适用边界复核。

### D-016 后置

`DisplayArtifact` 的物理表、JSON、投影、事件持久化与状态派生尚未冻结。本包提供逻辑合同，不能被直接解释为数据库迁移脚本。

### 历史状态同步

旧版 TODO、项目记忆和母稿可能仍保存 D-005 待决、P5 4/5 或 G 类延后的历史状态。该问题不影响本包结构导入，但会影响开发者理解，项目维护者应根据最新 ADR 同步。

## 6. 验收结论

> 本包达到“逻辑资产可进入候选区、交叉引用完整、黄金边界可回归、导入阻断错误为 0”的交付条件；新增资产保持 `review_candidate`，不得自动激活为生产真源。
