# 当前里程碑

- 里程碑：`M5-4` 共享 ECS SaaS 上线
- 状态：`ACTIVE`
- 起点：M5-K 已由主控关闭；关闭提交 `8191d52`
- 唯一执行端：当前 WSL Codex 或 Claude Code 执行端；同一时间只允许一个写入者
- 当前任务包：`docs/M5-4-共享ECS-SaaS上线执行包.md`
- 当前 Git 基线：`851e33e`；主仓干净，已完成一次 ECS/域名/证书/容器/磁盘的只读预检
- 正式产品入口：`https://diyuai.cc`

## 已满足的进入条件

- M3-1 至 M5-K 已全部关闭；
- P1—P5、三平台视频/图文文字成品、DM01、正式 React 工作壳、系列与素材入口已经在本地软件成立；
- PostgreSQL RLS、可信作用域、版本/事件、41 条数据驱动激活和远程 CI 已存在；
- 用户确认第一阶段取消 DIFY，采用模块化单体、直连 DeepSeek、共享 PostgreSQL + 强制 RLS 的轻量多租户 SaaS；
- 用户确认 `diyuai.cc` 是当前项目正式入口，历史 `dify.diyuai.cc` 与当前项目完全解耦；
- 用户确认首期按预计 2—3 个、最多 5 个租户、全平台不超过 200 个已启用账号设计，不把它解释为 200 并发；
- 用户确认三个正式入口、租户管理员创建用户、一次性激活链接、用户自行设密/改密、首期不发短信，以及平台运维独立入口；
- 现有只读证据表明 ECS、根域名 DNS 和证书具备 M5-4 接入基础；真实现场仍由执行端在部署前做一次廉价复核。

## 本里程碑唯一结果

把当前租户中立版本部署到现有 ECS 的当前项目独立边界，以 `https://diyuai.cc` 对外提供：

1. 租户用户、租户管理员和笛语平台运维三个正式认证入口；
2. “折线之间”合成演示租户在正式账号下的 P1—P5、媒体文字包、版本/保存/承接和 DM01；
3. 当前项目独立 PostgreSQL/角色/迁移、强制 RLS、独立对象存储边界和真实 DeepSeek；
4. 最小运行日志、模型用量/限流、健康检查、备份恢复与安全回退；
5. 可供 M6 在同一平台创建真实租户、无需重新部署的共享 SaaS 基础。

## 明确边界

- M5-4 可以只上线“折线之间”，不等待笛语服饰或其他真实品牌；
- 不创建真实租户，不导入笛语服饰候选资料、26 条视频或个人私有素材；
- 不复用历史 DIFY/RAG 的工作流、数据、用户、知识、素材、提示词、schema 或运行状态；
- 不建设微服务、Kubernetes、消息队列、向量库、GraphRAG、完整计费/监控/灾备平台或自动 CD；
- 不把 200 个已启用账号变成 200 并发压测；
- 不修改 P1—P5、DM01 或知识激活语义；若部署不改变生成策略，不重跑真实模型六卡；
- 生产上线只能证明共享 SaaS 在线，不证明真实品牌入驻、真实试点、流量、销售或采用。

## 状态流转

```text
READY → ACTIVE → REVIEW → CLOSED
```

- 用户已将完整执行包交给唯一执行端，M5-4 已进入 `ACTIVE`；
- 执行包已对范围内 ECS、域名、Nginx、数据库、对象存储、密钥、真实模型、部署、备份和回退给出明确授权，普通工程问题不得反复停工询问；
- 只有可能损害历史系统、无法形成独立数据边界、证书/对象存储/真实模型全部安全路径失效，或只能降低认证、租户隔离、密钥与恢复安全时才构成真实阻断；
- 达到在线验收、一次必要回归、一次 CI 和两份有界审查后进入 `REVIEW`，不得自动启动 M6-1。

当前剩余验收：正式认证与三入口、独立生产数据库/对象存储/请求角色、限流与运行观测、容器部署、备份恢复和有界在线验收；不启动 M6-1。

唯一下一动作：在当前项目独立边界内完成正式认证与部署实现，再执行一次稳定候选回归和正式上线验收。
