# 当前里程碑

- 里程碑：`M5-K` 知识完整性与运行治理收口
- 状态：`READY`
- 起点：M5-3 已由主控关闭；实现提交 `fb8459c`、状态提交 `71c5215`，远程 CI 均绿色
- 唯一执行端：当前 WSL Codex 或 Claude Code 执行端；同一时间只允许一个写入者
- 当前任务包：`docs/M5-K-知识完整性与运行治理收口执行包.md`

## 已满足的进入条件

- M3-1 至 M5-3 已关闭，P1—P5、三平台视频/图文文字成品、DM01、正式前端工作壳、可信作用域、版本、事件、PostgreSQL RLS、最小知识激活和远程 CI 已存在；
- 用户确认在 M5-3 后插入 M5-K，M5-K 关闭后才进入 M5-4；
- 用户确认19份重复陈列文件只属于领域通用库；26条云盘原视频首期不导入、不逐素材登记授权并遵守 D-020；
- 用户确认笛语服饰是首个真实品牌候选，并授权在产品演示、客户交流和公开宣传中使用品牌名称、经其确认的品牌/商品/门店资料及相应文字成品；未确认候选数据、26条原视频和个人私有素材不在授权范围；
- M5-K 没有待用户确认的承重裁决，也不依赖 DeepSeek、ECS、云盘、真实门店测量或真实租户资料。

## 本里程碑唯一结果

在不增加业务功能、不批量激活候选知识、不创建真实租户的前提下：

1. 修复品牌候选资料的文件名、文档编号和引用错位；
2. 将当前41条运行激活及适用条件从 Python 硬编码迁入一个版本化数据真源；
3. 用结构、唯一标识、schema、引用和来源快照完整性替代固定243魔数；
4. 落实最小 `review_candidate / active / deprecated`、`valid_until` 和 `superseded_by` 运行语义；
5. 只把引用完整性、作用域、DM01算术和内部标识防泄露四类确定性 H/夹具绑定既有 CI；
6. 完成 `ContentBrief`、第二批缺失文件和生命周期枚举的最小文档对账；
7. 保持现有41条资产及全部软件闭环无损。

## 明确边界

- 不激活新的领域或品牌知识；
- 不把笛语服饰候选资料写入生产数据库或模型上下文；
- 不导入26条视频，不扩展首期陈列范围；
- 不建设向量库、GraphRAG、知识图谱、RAG/评测平台、知识后台、权利图谱或MD编译器；
- 不调用真实模型，不部署ECS，不修改域名、Nginx、证书、历史DIFY或生产数据；
- 当前只能形成可供M6-1消费的真实品牌候选，不能冒充已完成真实入驻、M6-3验收或M7试点。

## 状态流转

```text
READY → ACTIVE → REVIEW → CLOSED
```

- 执行端开始工作后置为 `ACTIVE`；
- 完成任务包全部验收、提交推送且同 SHA 远程 CI 绿色后置为 `REVIEW`；
- 只有主控终审后才能置为 `CLOSED`；
- M5-K 未关闭前不得启动 M5-4。

唯一下一动作：将 `docs/M5-K-知识完整性与运行治理收口执行包.md` 全文原样交给唯一 WSL 执行端，一次推进到 `REVIEW`。
