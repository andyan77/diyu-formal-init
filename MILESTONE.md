# 当前里程碑

- 里程碑：`M3-1` P1 弱种子内容生产可用闭环
- 状态：`BLOCKED`
- 当前基线：承接 `main@d40bb90` 的本地 M3-1 收口提交
- 唯一执行端：Codex（当前 WSL Git 主仓）
- 前置：M0—M2 已关闭；WSL Git 主仓迁移已完成

## 当前承接

- `M3-1-D1` 已关闭：第一阶段取消 DIFY；应用核心经极薄单模型适配器直连已确认的 `DeepSeek-V4-Flash`。`M3-1-D2` 已关闭：当前项目独立 PostgreSQL、共享 schema、服务端显式作用域和强制 RLS；请求角色与迁移角色分离，RLS 守住租户根。
- `M3-1-C1` 已确认并已落盘：系统公共目录可重复登记 243 条候选，保留来源、类型、版本、结构化正文与适用边界；仅 `B-TPO-001`、`C-COMMUTE-001`、`D-DIRECT-001`、`D-CRAFT-001` 在当前 P1 按条件进入上下文。其余 239 条保持 `review_candidate`，其中 `B-TPO-002`、`D-DIRECT-002` 明确保留候选。H 类约束由可信会话、应用作用域、RLS、上下文编译、最小结果合同和人工审阅承接，不作为普通知识批量输入模型。
- 已实现并通过本地验证：自然 `hello` 交流不创建任务；同页 P1 生成、V1→V2、V1 可读、主动保存和原用户/品牌/账号内显式复用；生成、版本、事件和实际资产版本留痕；失败不写半成品；Cookie 可信会话和 OpenAPI；同租户其他品牌、账号和用户的读取、修改、保存、复用拒绝；四项 RLS 反证；合成顾客标识不进入文字；非固定的两条弱种子替身结果；统一 `make golden` 不受 Windows `TEMP`/`TMP` 继承影响。
- 已验证不等于真实生产：本地页面使用明确标识的确定性替身，不能证明 DeepSeek 的创作质量、供应商连通性、真实试点或生产隔离。

## 唯一阻断与下一动作

当前唯一阻断是：**缺少已授权且可核验的 DeepSeek 兼容连接配置，因而不能核验准确 `DeepSeek-V4-Flash` 模型参数、完成两条真实同页生成、一次修改保存和人工 P1 语义审阅。** 凭据来源仍为 `unknown`；未读取、输出、校验或记录密钥正文，也不猜写模型 ID。

唯一下一动作：在主控获得一份已授权、可安全引用的 DeepSeek 连接配置来源后，只完成上述真实同页验收与人工语义审阅；此前不启动 M3-2，也不把替身结果写成 `REVIEW` 或 `CLOSED`。

## 关闭线

真实同页路径须以可信会话、当前作用域和当前适用四条资产完成两条不同弱种子生成、一次自然修改及保存；人工按 P1 合同审阅。通过后才可由主控将 M3-1 进入 `REVIEW`，执行端绝不自行写 `CLOSED`。
